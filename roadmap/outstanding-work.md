# Outstanding Work — the one schedule

**Added 2026-09-02.** Everything left to build across the [PromptCadence arc](promptcadence-roadmap.md)
(M10–M13) and the [Adapter arc](adapter-roadmap.md) (LA0–LA3), in execution order, one row per
model session. The roadmaps stay authoritative for *rationale*; the
[model-assignment guide](model-assignment.md) (§2.10–2.12) stays authoritative for *model choice*
— and since 2026-09-04 a phase gets **one** model, the stronger of any pair that used to be a split
([§3.5](model-assignment.md) records why, from row F1's evidence);
this document is the schedule view that answers "what do I run next, on which model".

**How to use a row.** Copy the row into an Opus session and ask it to draft the kickoff prompt for
the working session. The reading column names everything phase-specific; every session's prompt
should additionally include the standing preamble in §2 below. Batch letters group rows that are
adjacent *and* on the same model, so consecutive rows with one letter can be run back-to-back in
one sitting; letters are dependency-ordered, and §3 lists which orderings are load-bearing versus
merely convenient. All paths are relative to `~/ai/suite/docs/`.

## 1. The master table

| # | Phase → ships | Model · effort | Runs after | Work overview — and required reading | Why this model |
|---|---|---|---|---|---|
| **A1** | **Phase 0 + LA0** → ADRs accepted, docs amended | **Opus 5 · xhigh** | — (gates everything) | Write and accept ADRs D-1…D-13 and A-1…A-10 (numbered 0045+); amend master architecture per the checklists; update docs README index, gold-standards §2, traceability matrix, risk register, master-roadmap pointer; retire `harness.md` with a pointer. **Read:** `roadmap/promptcadence-roadmap.md` (§2, §7), `roadmap/adapter-roadmap.md` (§2, §9), `architecture/adapter-identity-and-serving.md`, the ADR index + ADR-0038 as the amendment precedent | Permanent contracts, but every decision is already made and argued in the roadmaps — expansion, not invention. Fable would be 2× to transcribe settled judgment |
| **A2** | BaseAiCore **0.4.1** → PyPI | **Opus 5 · high** | A1 | `DataClassification` (ordered, three levels, D-2) + `AdapterIdentity` and the canonical `+name@sha256:…` suffix (A-1); goldens; fold the deltas into the BaseAiCore spec/plan docs; publish. **Read:** accepted D-2 + A-1 ADRs, `packages/baseaicore/spec.md`, `architecture/adapter-identity-and-serving.md` §§2–3 | §3.3's BaseAiCore rule: wrong here is a data-compatibility break in every repo. Tiny phase — the premium costs almost nothing |
| **B1** | SetSpec **0.5.0** (Phase 6) → PyPI | Sonnet 5 · high | A2 | `governance.egress_decision` 1.0 (D-7/D-10), `model.adapter_manifest` 1.0 + `CapabilityEvidence` v1.1 optional adapter fields (A-1/A-4), JSON Schemas, ≥ 3 goldens each; non-adapter evidence must round-trip byte-identically (I15); fold a Phase 6 section into the SetSpec docs; publish. **Read:** `packages/setspec/spec.md`, `packages/commissioner/spec.md` (payload shape), `roadmap/adapter-roadmap.md` §3 LA0 + §7 I15, accepted D-7/D-10/A-1/A-4 | The SetSpec-P2 precedent: normative field tables → transcription with validation. Goldens get an Opus review pass in the morning |
| **B2** | LoadLedger P1 | Sonnet 5 · high | A2 | Ceilings, debits, verdicts, windows, `InMemoryLedger` — dual token+money ceilings per ADR-0030, the `unpriced_debit_count` that lets PromptCadence raise `UNPRICED_EGRESS_REFUSED` (the code itself is PromptCadence's, never this package's — ADR-0047), UTC-midnight window edges. **Read:** `packages/loadledger/spec.md`, `packages/loadledger/development-plan.md` Phase 1, ADR-0030 | §3.4 exactly: integer arithmetic with hand-computable fixtures; the UTC-midnight trap is named three times in the docs |
| **B3** | Commissioner P1 | Sonnet 5 · high | B1 | Vocabulary binding, `OrderedClassificationPolicy` (fail closed on undeclared ceiling), decision verdicts, payload round-trip against the published `setspec` 0.5.0. **Read:** `packages/commissioner/spec.md`, `packages/commissioner/development-plan.md` Phase 1, accepted D-10 | Enum-parametrized matrix, exhaustive by construction; the fail-closed row is spec'd |
| **B4** | PromptCadence P1 | Sonnet 5 · high | A2 | Repo skeleton, config, database, health endpoint, CLI stub — the standard app shape. **Read:** `apps/promptcadence/spec.md` §§1–8, `apps/promptcadence/development-plan.md` Phase 1; LoadCoach's repo layout as the precedent | Identical in shape to LoadCoach P1; boilerplate against a precise spec |
| **B5** | SetSpec **0.6.0** (Phase 7) → PyPI | Sonnet 5 · standard | B1 | `benchmark.evidence_bundle` **1.1**: the bundle nests `CapabilityEvidenceV1_1Fields` so adapter-bearing evidence can travel *inside* an exported bundle, which `1.0` cannot carry — it nests the frozen `capability.evidence` `1.0` shape by reference and does not move when that payload gains a minor ([ADR-0068](../adr/0068-a-post-freeze-minor-is-a-sibling-class.md) rule 5). Same sibling-class mechanism as B1's `capability.evidence` `1.1`: `EvidenceBundleV1_1Fields` beside an untouched `EvidenceBundleFields`, bare `EvidenceBundleOut`/`In` keep meaning `1.0`, `1.0` goldens byte-unchanged. JSON Schema, ≥ 3 goldens, docs section, publish. **Read:** ADR-0068, `packages/setspec/spec.md` §11 rule 9 + §19, B1's `capability.evidence` `1.1` as the worked example | A second application of a mechanism now written down and demonstrated once — transcription against a precedent in the same repo, which is what `standard` is for |
| **C1** | CutCtx P1 | **Opus 5 · xhigh** | A2 | Transcript model, the `_invariants` module, plan/executor split, `DropOldestPolicy` — property tests whose choice of properties is the deliverable. **Read:** `packages/cutctx/spec.md`, `packages/cutctx/development-plan.md` Phase 1, accepted D-8 | The invariants gate every future policy routes through (first-instance rule); the property choice is the judgment |
| **C2** | ToolYard P1 | **Opus 5 · xhigh** | A2 | Tool specs, registry, executor discipline, structured refusal paths — "model input never raises", fixed refusal order, fuzzed. **Read:** `packages/toolyard/spec.md`, `packages/toolyard/development-plan.md` Phase 1, accepted D-9, ADR-0018, ADR-0026 | The discipline every later tool inherits; the FakeProvider lesson — stricter than its first consumer needs |
| **C3** | LoadLedger P2 → **0.1.0** on PyPI | **Opus 5 · high** | B2 | `loadledger.sql`, `SqlLedger`, `mount_ledger_tables`, kill-mid-debit atomicity on both dialects, the miniature-host Alembic test (I14). **Read:** `packages/loadledger/development-plan.md` Phase 2, accepted D-6, ADR-0006 | First mounting implementation (first-instance rule) + atomicity on money rows — the §3.3 failure shape |
| **C4** | PromptCadence P2 | **Opus 5 · xhigh** | B4 | Domain core: threads, tiers, classification, plans, **`ExecutionIntent`**, deviation matrix, state machine from the T1–T17 tables — with the LA0 optional adapter fields born in. **Read:** `apps/promptcadence/spec.md` §9, `apps/promptcadence/lifecycle.md` §§2–5 + §8, `apps/promptcadence/development-plan.md` Phase 2, accepted D-2/D-3/D-12 | Mostly normative-table transcription — but this phase freezes the shape of the design's load-bearing wall |
| **C5** | **ModelRack: the ADR-0070 usage rule** → next minor | Sonnet 5 · high | A1 (flexible — must land before D3) | Both real adapters read usage to the per-response rule: cache detail present → disjoint classes; absent → cache classes `0`; no usage object → every class `UNSUPPORTED`. `FakeProvider` defaults to `0` with scripted `UNSUPPORTED` still possible; recorded fixtures re-annotated; the three-case conformance test; the Ollama `prompt_eval_count` fixture check. Spec contract 2 is already amended. **Read:** ADR-0070, ADR-0069 §"Not decided here", `packages/modelrack/spec.md` §11 + §18, the two `_read_usage` functions and their fixtures | Two small functions against a stated rule, but in a package three apps call and with a named fabricated-zero failure mode — high, not standard |
| **C6** | **LoadCoach: four token classes on the wire** → next LoadCoach minor | Sonnet 5 · standard | C5 | `cache_write_tokens` + `cache_read_tokens` on the attempts and jobs rows (one migration) and on the job document's `usage`, `"unsupported"` per ADR-0016 rule 4; `apps/loadcoach/data-model.md` and `api.md` amended first; additive within `/api/v1`. **Read:** ADR-0070 decision 7, `apps/loadcoach/data-model.md`, `apps/loadcoach/api.md`, `services/queue.py`'s `JobView` and the worker's usage capture | Additive columns and fields transcribed from an ADR; the migration is the only judgment |
| **D1** | **ToolYard P2** (sandbox) | **Fable 5 · xhigh — daytime, reviewed** | C2 | Containment + tiered isolation (container → bwrap → refuse), TOCTOU and prefix-collision cases; marked tests need the reference machine's bwrap. **Read:** `packages/toolyard/development-plan.md` Phase 2, accepted D-9, ADR-0018 | Adversarial with a silent-failure class — an escape that works is quiet. Never overnight |
| **D2** | **PromptCadence P3** | **Fable 5 · xhigh — daytime, reviewed** | C4 | LoadCoach client, bypass loop, lease/claim, kill −9 recovery, one-write transitions (ADR-0044), events — **plus the fake LoadCoach every later phase trusts** (I10 contract tests against the OpenAPI snapshot). **Read:** `apps/promptcadence/development-plan.md` Phase 3, `apps/promptcadence/lifecycle.md` §8.3 + §10, ADR-0036/0044, `roadmap/promptcadence-roadmap.md` §9 I10 | The LoadCoach-P5-shaped phase: no feedback loop for the interesting failures, and the fake doubles the stakes |
| **D3** | **ModelRack P6** (LA1 start) | **Fable 5 · xhigh — daytime, reviewed** | A1 + C5 (flexible — placed here for model adjacency) | `LlamaCppProvider` process supervision: spawn/health-wait/terminate, kill-tree, pid files, port range, GGUF discovery + hashing, generation + streaming, version-annotated recorded fixtures; usage read to the ADR-0070 rule from the start. **Read:** `roadmap/adapter-roadmap.md` §4.1 P6, `architecture/adapter-identity-and-serving.md` §§4–6, accepted A-5, ADR-0070, `packages/modelrack/spec.md` (Provider protocol) | Concurrency with orphan/leak guarantees, no useful feedback loop, in a package three apps call — the §3.3 class |
| **E1** | CutCtx P2 → **0.1.0** on PyPI | Sonnet 5 · high | C1 (flexible until I1) | `ObservationMaskingPolicy`, `SummarizingPolicy` (planned requests, never executed — D-8), `PolicyChain`; the contiguous-group dict-ordering trap is golden-tested. **Read:** `packages/cutctx/development-plan.md` Phase 2, accepted D-8 | Policies against locked invariants; the one risky spot is named in the plan |
| **E2** | ToolYard P3 → **0.1.0** on PyPI | Sonnet 5 · high | D1 | Built-in tools; `http_fetch` implements ADR-0026 §3 with the vectors byte-shared with LoadCoach's suite. **From D1 (2026-09-03; `D1_HANDOFF.md` §8 and §13 at the workspace root):** `run_command_tool(sandbox: Sandbox)` takes the **same** `TieredSandbox` instance the executor holds (spec §7 as amended) and declares `requires_isolation`; it passes an explicit `PATH` allowlist such as `{"PATH": "/usr/local/bin:/usr/bin:/bin"}` — under bwrap `--clearenv` leaves none, and `os.environ` is never the answer; env is caller-owned, never model-supplied; it never passes `network=True` (refused in v1); it renders both `SubprocessResult.output_truncated` and the exit code (an output bomb is killed at the cap); its description says that a backgrounded child keeps the call alive until the timeout; document a floor for `ResourceLimits.process_count` (bwrap's count includes `bwrap` and `prlimit`, so 1 or 2 refuses everything). **Before publishing 0.1.0:** run `pytest -m isolation -rs` on a host with podman — the rung is in the ladder but unverified on the reference machine (§4). **Read:** `packages/toolyard/development-plan.md` Phase 3, ADR-0026 §3, `packages/toolyard/spec.md` §7 and §14 as amended at D1 | Fixture/vector work; Opus reviews the fetch-handler diff |
| **E3** | Commissioner P2 → **0.1.0** on PyPI | Sonnet 5 · standard | B3 + C3 | Append-only ledgers + mounting — copies LoadLedger P2's proven pattern verbatim. **Read:** `packages/commissioner/development-plan.md` Phase 2, LoadLedger P2's implementation as the template | The "rest of the repeated thing" — the pattern is proven twice by now |
| **E4** | PromptCadence P4 | Sonnet 5 · high | D2 + E2 | ToolYard integration, workspace lifecycle, `undeclared_tool` deviation wiring; hostile-model journey tests are specified. **From D1:** build one `TieredSandbox` per executor and hand the same instance to `run_command_tool`; the container image is a constructor argument that must be present locally (`--pull=never`), so configuration names it and `doctor` shows `TierReport.reason`; `SandboxPaths` now refuses relative or overlapping roots at construction, so the workspace lifecycle builds absolute, disjoint roots. **From D2 (2026-09-03; `D2_HANDOFF.2.md` §4 and §7 at the workspace root) — the tier defaults, decided by interview and implemented here:** PromptCadence's shipped defaults name `tools.agent.local_fast` / `tools.agent.local_large` (spec §12, ADR-0047 §1) and LoadCoach ships one `tools.agent`, so the live journey only ran with `PROMPTCADENCE_TIERS__*` overrides. E4 closes this as its **first commit, in the LoadCoach repository**: the five harness profiles go into LoadCoach's shipped `src/loadcoach/config/task_profiles.toml` as namespaced specializations of `tools.agent` — configuration, no LoadCoach code, changelog under Unreleased, no bump. (1) `tools.agent.local_fast`: `tools.agent`'s weights and execution; constraints `min_context_tokens = 16384`, `requires_capabilities = ["tool_use"]`, `max_latency_p95_seconds = 60`, `min_capability_scores = { tool_use = 0.3 }`, `allow_remote_providers = false`. (2) `tools.agent.local_large`: weights leaning to reasoning (`agentic 0.30, tool_use 0.25, reasoning 0.30, reliability 0.15`); `min_context_tokens = 32768`, `requires_capabilities = ["tool_use"]`, `max_latency_p95_seconds = 300`, `min_capability_scores = { tool_use = 0.4, reasoning = 0.4 }`, `allow_remote_providers = false`; `max_output_tokens = 8192`. The minimum context of each local profile **equals the tier's `context_budget_tokens`** (16384 / 32768) — LoadCoach cannot express model size, so latency ceiling, minimum context and minimum scores are the whole distinction, and that is the decision. (3) `tools.agent.remote_cheap` and (4) `tools.agent.remote_frontier` ship **now**, with `allow_remote_providers = true`, `min_context_tokens` 128000 / 200000 (the tier budgets), latency ceilings 120 / 300 s, `min_capability_scores` `{ tool_use = 0.4 }` / `{ tool_use = 0.5, reasoning = 0.5 }`, `max_output_tokens` 4096 / 8192; until LC-E1 registers a remote provider they route to `NO_ELIGIBLE_MODEL`, which PromptCadence reports as `TIER_UNAVAILABLE` and `tiers check` shows — visible, not silent. Whether a remote profile must *require* a remote provider is LC-E1's question (D-11), not E4's. (5) `tools.plan`: weights `reasoning 0.4, structured_output 0.3, instruction_following 0.2, reliability 0.1`; constraints `min_context_tokens = 16384`, `requires_capabilities = ["structured_output"]`, `max_latency_p95_seconds = 180`, `min_capability_scores = { structured_output = 0.4 }`, `allow_remote_providers = false`; execution `temperature = 0.1`, `max_output_tokens = 4096`, `response_format = "json"`, `max_attempts = 3`, `fallback_depth = 1`; validation `require_valid_json = true`, `max_output_chars = 50000`, and **no schema** — the plan shape stays PromptCadence-internal (D-7) and G1 validates the document; G1 may reopen this if it wants LoadCoach's corrective retry against a schema. Every "fifteen" pin in LoadCoach moves to twenty: `tests/unit/test_task_profile_validation.py::test_all_fifteen_shipped_profiles_load_and_validate`, `tests/unit/test_cli.py` (two asserts), `tests/e2e/test_models_and_task_profiles.py`, `docs/quickstart.md`, `apps/loadcoach/routing.md` (the profile list), `apps/loadcoach/development-plan.md` Phase 2 AC1 — workspace `docs/` first, mirrored byte-identically. **Then, in PromptCadence:** `config.py`'s defaults are untouched; vendor LoadCoach's `task_profiles.toml` as `tests/contract/loadcoach_task_profiles.toml` with its SHA-256 and source commit recorded the way I10 records `loadcoach_openapi.json`, and add a contract test asserting that every default tier's profile exists there, that the agent profiles require `tool_use`, that each profile's `min_context_tokens` equals its tier's `context_budget_tokens` and its `allow_remote_providers` equals the tier's `remote`, and that `tools.plan` exists with `require_valid_json` and no schema — so a drift fails in CI, not at an operator's `tiers check`. The fake LoadCoach **stays an empty registry** (stricter than LoadCoach, by decision); add `shipped_profiles()` to `tests/fakes/loadcoach_app.py`, reading the vendored TOML so tests register the real profile shapes (`text_profile`/`schema_profile` remain for hand-shaped cases), and move the e2e, recovery and live-adjacent fixtures onto them. Amend spec §12's sentence "documented as TOML in PromptCadence's operator guide" to say the profiles ship in LoadCoach's `task_profiles.toml`, and drop the `PROMPTCADENCE_TIERS__*` overrides from `tests/live/test_loadcoach_journey.py`'s docstring. **Exit demonstration for this item:** start LoadCoach at E4's own commit on `provider.kind = fake`, then run `pytest -m live` and `promptcadence run "…" --bypass-planning --follow` with **no** tier overrides — both complete; a run against Ollama is the ops-checklist item below, because the fake provider declares `stop` for every answer. **Read:** `apps/promptcadence/development-plan.md` Phase 4, `apps/promptcadence/lifecycle.md` §5, ADR-0047 §1–2, `apps/loadcoach/routing.md` §§2–4 (profile grammar and hard constraints), `D2_HANDOFF.2.md` §4  **Done 2026-09-04** (`E4_HANDOFF.md`; docs `da45959`+`0e0d28c`, LoadCoach `5c5aa1f`, PromptCadence `b4ac1a2`+`c22ddef`; unpushed, review owed). **Three things this row's text assumed that were not true, corrected here so F1 does not repeat them:** (1) `promptcadence tiers check` does not exist and was **not** built — it is Phase 7's in `apps/promptcadence/development-plan.md` ("Task-profile checks in `doctor` and `promptcadence tiers check`"), so this row's reference to it was forward-looking; `doctor` and `promptcadence tools list` are what an operator has, and `doctor` gained a third `tools` component carrying ToolYard's `TierReport.reason`. (2) The **two remote tiers are not shipped active** — `config._default_tiers` ships only `local_fast` and `local_large`, with the remote pair documented and commented out in `EXAMPLE_CONFIG_TOML`, because a remote tier with an empty `pricing_file` is refused at startup and would break spec §20 AC1's zero-configuration boot (B4's decision, now stated in spec §12 rather than only in a docstring). A remote tier configured the way that comment instructs halts with `TIER_UNAVAILABLE` / `loadcoach_has_no_remote_provider` — caught by `TierPolicy.availability` **before** any call, so LoadCoach's `NO_ELIGIBLE_MODEL` is never reached. (3) The exit demonstration is **GPU-dependent**, see row E6. | ToolYard did the hard part. Opus reviews the workspace-lifecycle diff |
| **E5** | **MirrorWall 0.2.2** → PyPI, then the `setspec` pin sweep | Sonnet 5 · standard | E4 (flexible — but **before F2 and H4**) | **The widen itself was already built and committed** on MirrorWall `main` before this row ran (2026-09-04, `f8dd81a` + `092af6e`; since pushed and tagged `v0.2.2`): `setspec>=0.4,<0.7`, `ci.lock` recompiled, full gate green in a lock-installed venv on Python 3.13.15 (311 passed, 1 skipped). What was left was the release and the sweep, and both are now done. **Publish** `mirrorwall 0.2.2` (tag, `pypi` approval, install check — the operator's steps in §4), then move `setspec>=0.4,<0.5` → `>=0.4,<0.7` in **PromptCadence**, **IdeaPress** and **FreeWeight**, regenerating each `requirements/ci.lock` the same way — `pip-compile … -P setspec -P baseaicore`, the second because `setspec` 0.6.0 requires `baseaicore>=0.4.1` and without it the resolver **silently falls back to `setspec` 0.4.0 rather than failing** — and delete the now-stale "this pin cannot move yet" comment in `PromptCadence/pyproject.toml`. **LoadCoach is the exception and does not move here.** At `setspec` 0.6.0 its `tests/contract/test_evidence_import.py` enumerates the new `capability.evidence` `1.1` goldens and validates each stored record against the bare `CapabilityEvidenceOut` — which means `1.0`, `extra="forbid"`, per [ADR-0068](../adr/0068-a-post-freeze-minor-is-a-sibling-class.md) rule 3 — so three tests fail: locally today (verified 2026-09-04: 3 failed, 855 passed), and in CI the moment the pin moves. Adopting `CapabilityEvidenceV1_1Out`/`In` is **H4**'s import change, so **LoadCoach's pin moves at H4, with that adoption, not here.** FreeWeight's `tests/contract/test_evidence_schema.py` was checked against 0.6.0 and passes. **Read:** `py/MirrorWall/CHANGELOG.md` 0.2.2 and its `requirements/README.md` §"Why `setspec` moved to 0.6.0 at 0.2.2", ADR-0068 rule 3, `roadmap/adapter-roadmap.md` §4.3 **Done 2026-09-04** (`E5_HANDOFF.md` for the release half and `F2_HANDOFF.md` §"E5 resumed" for the sweep; MirrorWall `f8dd81a`+`092af6e` and tag `v0.2.2`, PromptCadence `1c1eb33`, IdeaPress `e2138fd`, FreeWeight `2b93132`, docs this commit). Ran in two sittings: the release half stopped correctly at Gate A5 because `mirrorwall 0.2.2` was still parked on the `pypi` approval, and the sweep resumed once the operator approved it. **Five things this row's text said that turned out not to be true, or that it did not say:** (1) **PromptCadence's floor is `>=0.5`, not `>=0.4`** — the row said `>=0.4,<0.7` uniformly, but `apps/promptcadence/spec.md` §5 requires `governance.egress_decision`, which `setspec` 0.5 introduced, and a floor below a component's own stated spec is drift that stays invisible until someone installs the wrong thing. IdeaPress and FreeWeight keep the 0.4 floor, which is correct for them: `setspec.prompts` plus `GeneratorInfo` is their whole surface and it has not moved. (2) **A widened pin does not move an existing venv, and looks exactly like success when it does not.** PromptCadence's editable reinstall jumped to `setspec` 0.6.0 on its own, but IdeaPress's did not — because a floor of `>=0.4` is still satisfied by the installed 0.4.0, so pip had no reason to act. **The 0.5 floor self-enforces; a 0.4 floor does not.** Where the floor did not move, the three packages were upgraded by name and the result confirmed with `pip show` before the gate was believed. (3) **FreeWeight's `ci.lock` needs three `-P` flags, not two**, and the reason is worth keeping: `mirrorwall 0.2.0` in that lock *itself* required `setspec<0.5`, so `-P setspec -P baseaicore` alone holds the lock at 0.4.0 while reporting success. (4) **`pip-compile` 7.6.1 no longer accepts `--no-index` as the alias for `--no-emit-index-url`** — it now means "ignore the package index", which fails build isolation with *No matching distribution found for hatchling*. Every lock header in the suite still records the old spelling; the flag that reproduces those files today is `--no-emit-index-url`. (5) **A repository's own venv can be the wrong oracle.** FreeWeight's carries *editable* installs of the `setspec` and `baseaicore` workspace checkouts, whose `dist-info` still reads 0.4.0, so its green gate proves nothing about what a consumer resolves; the proof is a clean venv installed `--require-hashes` from the recompiled lock (2508 passed, 28 skipped, Python 3.14.4). **LoadCoach was not touched**, as required — its pin rides H4 with the `CapabilityEvidenceV1_1Out`/`In` adoption, and moving it here would turn three local-only reds into CI reds. | Mechanical, and proven once already: a pin, a lock recompile and a gate per repository. The judgment was the ordering and the LoadCoach exception, and both are written down here |
| **E6** | **LoadCoach: the fake provider must not be gated on real VRAM** | Sonnet 5 · standard | E4 (**before F1**) | Found at E4, 2026-09-04, and it cost one exit-demonstration run. LoadCoach's **fake** provider declares an 8.5 GB model (`weights_bytes` 8 967 000 000, `parameter_count` 8.03 B, `size_bytes` 8 540 000 000), so routing's `insufficient_vram` hard constraint rejects the **only** candidate whenever the host GPU is busy — with an Ollama model resident the estimate was 11.4 GB against 1.2 GB free, and **every** tier reported `NO_ELIGIBLE_MODEL`, including unchanged `tools.agent`. A provider that exists so the suite can be exercised without hardware is currently gated on hardware, and the failure reads as a routing verdict rather than as "your GPU is full". F1's budget demonstrations run the same fake-provider journeys and will hit the same wall, which is why this lands first. **Two requirements, and the second is the point:** (1) the **default** declared size must be small enough that the fake provider never trips the VRAM constraint on any machine, so a fake-provider journey is reproducible rather than GPU-dependent; (2) the declared size must be **configurable**, so an operator can deliberately provoke `insufficient_vram` and watch the rejection path — the rejection detail (`estimated_bytes`, `free_bytes_by_gpu`, `headroom_bytes`, the whole `estimate` block) is one of the more valuable diagnostics LoadCoach produces and there is currently **no way to exercise it on purpose**. Requirement 2 is not optional and not a nice-to-have: without it, fixing (1) removes the only way anyone has ever seen this path fire. Prefer a fake-provider configuration key over exempting the provider kind from the constraint — an exemption would make the fake stop modelling the constraint at all, and a suite that cannot reproduce a real rejection is the thing this row exists to fix. **Read:** `E4_HANDOFF.md` §5 (the diagnosis and the numbers), `apps/loadcoach/routing.md` §4 (the constraint order and `insufficient_vram`'s inputs) **Done 2026-09-04** (`E6_HANDOFF.md`; LoadCoach `dfbf2d8`, docs commit below). **Two things the row's own text got wrong, corrected in the kickoff before this ran and folded in here so the next reader inherits them:** (1) **the numbers are ModelRack's, not LoadCoach's** — `DEFAULT_MODEL` in `py/ModelRack/src/modelrack/providers/_fake_script.py` declares the 8.5 GB model; LoadCoach's factory only accepted it by zero-argument construction, and the fix stays in LoadCoach's own composition root (`infrastructure/providers/factory.py`) rather than touching ModelRack's published contract. (2) **`size_bytes` alone cannot provoke the rejection** — the KV term (`2 × layers × kv_heads × head_dim × 2 bytes` for the assumed f16 precision, times the served context) dominates it at any interesting context length; `served_context` defaults to the model's own `max_context` (32 768) whenever a task profile leaves context unconfigured, so the KV term alone is already tens of megabytes even for a tiny model. `[provider.fake]` therefore requires `size_bytes`, `layers`, `kv_heads` and `head_dim` together, refusing a partial set with a named `ConfigurationError`, so an operator cannot set one field and wrongly conclude the diagnostic is unreachable. ModelRack untouched; the fake provider kind is not exempted from `insufficient_vram`. | One provider's declared numbers plus one configuration key. The judgment — that the *rejection* must stay reachable on purpose — is written down here |
| **F1** | PromptCadence P5 | **Opus 5 · high** | C3 + C6 + E4 | Budget wiring: LoadLedger mounted, crash-reconciliation of debits idempotent by `source_ref` (the Opus half); estimator with labeled sources, ceilings (per-trajectory, per-day with the `awaiting_window` park, per-project), config, CLI (the Sonnet half). **Read:** `apps/promptcadence/development-plan.md` Phase 5, `apps/promptcadence/lifecycle.md` §6, ADR-0030, accepted D-3 **Done 2026-09-04** (`F1_HANDOFF.md`; PromptCadence `f84ddf2`+`f6fdf38`+`0dc15b1`+`399803a`+`bd60bc3`+`c7fda2d`, docs this commit). **Run single-session at Opus 5 · high** — the split was not taken, because the estimator and the surfaces turned out to depend on decisions the reconciliation half made (see below), and handing them over would have meant re-deriving those. That is the evidence behind [model-assignment §3.5](model-assignment.md): as of 2026-09-04 a phase gets one model, the stronger of any pair, and this row's Model column has been rewritten accordingly rather than kept as a split with a deviation beside it. **Six things this row's text or the kickoff's §0 assumed that were not true, recorded so F2 does not repeat them:** (1) There is **no** `daily_token_ceiling` gap to close. The kickoff told the session to look for one; spec §11.5 and lifecycle §6 make the per-day ceiling money-only *by design* — local work is unpriced and never counts against it, which is what lets any amount of work run while only so much is spent — so adding one would have contradicted the spec. The real config gap was the estimator's `configured_default` rung, which lifecycle §6 names and `[tiers.<name>]` had no field for; added as `default_step_input_tokens`/`default_step_output_tokens`, two numbers rather than one total because the classes price differently. (2) **PromptCadence is the suite's first consumer of `baseaicore.ModelPricing`** — nothing anywhere defines a pricing-file format, so P5 defined one (spec §12). (3) **A remote tier cannot run in this build**, so no test can reach a money ceiling through the loop by the intended route: `TierPolicy.availability` reports `loadcoach_has_no_remote_provider` for every remote tier until LC-E1, hardcoded in `LoopController._tier_policy`. The money, floor/strict and window tests price the *local* tier through a hand-built `PricingCatalog` — one visible fiction, with `test_from_settings_never_prices_a_local_tier` holding the shipped rule (ADR-0016). (4) **Money ceilings bind priced usage only**, which ADR-0047 §3 decides and this row had to implement: LoadLedger reports every ceiling's verdict and the *application* decides which refuses a step, so a money cap someone's priced work exhausted must not stop a local step. Without this a shared per-day cap would halt every local trajectory in the installation. (5) T16's guard is "the ceiling now **admits the next step**", not "is the ceiling exceeded" — the cheaper question wakes a parked trajectory every day edge only for its pre-flight to park it again. (6) `trajectories.budget_money_nanos` was `Integer` and held whole nanos: the shipped $5.00 default is 5 000 000 000, past a 4-byte integer, so every trajectory with a cap over $2.14 would have failed to insert on PostgreSQL while SQLite stored it silently. Widened in migration 0005. **One LoadLedger row this found (§0.4):** LoadLedger reports a balance only *through a ceiling and for a named run*, so a view of a scope that has no cap — per-tier spend, which spec §7.2's `ledger show --scope tier` asks for — cannot be answered without either summing entries in the application (ledger arithmetic where ADR-0050 forbids it) or inventing an unreachable ceiling to read a number through. `--scope tier` therefore reports debit **counts** and says why; a `balances(scope, window_key)` read is the LoadLedger row. The same limitation makes `GET /ledger` name an arbitrary known run as its reference, which is sound because `per_day` and `per_tag` windows are ledger-wide, but it is a signature working around a missing read. **What F2 inherits and must not relitigate:** the mount shape (`LEDGER_TABLES = mount_ledger_tables(Base.metadata)` at module import in `infrastructure/db/models.py`, migration autogenerated against it, a test proving upgrade-from-empty equals the mounted shapes and downgrade is clean — copy it for `egress_decisions`); the debit-before-turn-row ordering and `reconcile_debits` idempotent by `source_ref`; and `render_money`/`render_tokens` as the one place a floor or an unpriced amount is written down. | Reconciliation is money-adjacent crash logic; the rest is plumbing against spec'd maths |
| **F2** | PromptCadence P6 | **Opus 5 · high** | E3 + F1 | Egress evaluation via Commissioner, verification, category-typed deviations, the violation→halt path, re-approval feed. **Read:** `apps/promptcadence/development-plan.md` Phase 6, `apps/promptcadence/lifecycle.md` §5, `packages/commissioner/spec.md`, accepted D-4/D-12 **Done 2026-09-04** (`F2_HANDOFF.md`; PromptCadence `f93e912`+`f7e66ea`+`7ce31ad`+`bb601d3`+this gate's commit, docs this commit). **Four things this row's text or the kickoff assumed that were not true, recorded so G1 does not repeat them:** (1) **The pre-flight order is the guarantee, and egress had to move ahead of tier availability.** A remote tier reports `loadcoach_has_no_remote_provider` until LC-E1, so a `confidential` trajectory aimed at one halted on `TIER_UNAVAILABLE` before any egress evaluation ran and spec §20 #4's "the refusal is a queryable `EgressDecision`" was unreachable. `TierRouter.resolve` splits into `tier_of` and `ensure_available`: a tier's egress class is a property of its configuration and its availability is a property of the deployment, and deciding governance on the second would mean the recorded reason changes the day a provider is registered. The four pre-flights now run egress → pricing → availability → budget, all before `turn.started`. Recorded as [ADR-0073](../adr/0073-egress-is-decided-on-configuration-before-availability.md) by operator decision after the build, because a build that reordered the checks would still pass every unit test of the policy itself. (2) **Spec §20 #5 cannot be about the `pricing_file` field.** Startup validation already refuses a remote tier naming no file (B4's decision), so a field check is a dead branch; contract 5 is about the `ModelPricing` **record**, and what reaches the pre-flight is an expired or empty price list. Both clarifications are now in spec §11 contracts 4 and 5 and §20. (3) **The deviation machinery was already built.** `domain/deviation.py` has carried the closed six-category taxonomy, both severities, the disposition matrix and `TurnFacts` since P2, and P4 wired `compare` end to end into the loop. Gate D was therefore a *golden over the bypass-minted intent*, not a build — and that golden is the baseline G1's contract-1 invariance diff is written against. Nothing was added to `TurnFacts`. (4) **Spec §7.2's `promptcadence egress list [--denied-only]` cannot express the whole vocabulary**: a violation is neither an approval nor a denial, so `--verdict` ships beside the documented flag and the spec now names both. **Also recorded:** `commissioner 0.1.0` pins `setspec>=0.5,<0.6`, so adding it pulled PromptCadence's resolved `setspec` from 0.6.0 back to 0.5.0 — expected, costing nothing today, and needing to be lifted before PromptCadence can adopt any 0.6 payload (§3's ordering note). | Fail-closed semantics and the violation path — adversarial-adjacent |
| **F3** | ModelRack P7 | **Opus 5 · xhigh** | D3 (flexible — model adjacency) | Launch-time adapter registration, per-request selection, `adapter_hot_swap`, `AdapterNotFound`, digest-verified compatibility, `pending_restart`, **the cache-correctness conformance test** (prefix under A never reused for B, I17). **Read:** `roadmap/adapter-roadmap.md` §4.1 P7 + §7 I17, accepted A-1/A-5/A-6 **Done 2026-09-04** (`F3_HANDOFF.md`; ModelRack `51bc009`+`52565b4`+`5e8aa0e`+`6c686f5`, docs `986ab25`+`9196580`). **Not pushed** — per the standing instruction, both repos are committed and left for the operator to push. **The row had no plan section**, which §0.1 of its kickoff made Gate 0: `packages/modelrack/development-plan.md` ended at Phase 6, so P7's acceptance criteria had nothing demonstrable behind them. Phases 7 **and** 8 are now written in the house shape (`986ab25`), so H1 inherits a plan rather than repeating this. **The boundary question is settled and recorded**: ModelRack defines its own frozen `AdapterRegistration` and the **application** converts `setspec.artifacts.AdapterManifestOut` into it. ADR-0061 rule 3 already said ModelRack never reads the directory; master architecture §2 permits only `mirrorwall` and `commissioner` to import `setspec`. The runtime dependency set is still exactly `baseaicore` + `httpx` and `.importlinter` was not touched — only the `baseaicore` *floor* moved, to `>=0.4.1`, which is where `AdapterIdentity` lives. **Five things this row's text or its kickoff assumed that were not true, recorded so H1 does not repeat them:** (1) **llama-server does not zero unnamed adapters when a request carries no `lora` field.** `launch_slot_with_task` takes an `else` branch that restores `params_base.lora_adapters` — at the launch scales, which `--lora` sets to `1.0` and `--lora-init-without-apply` does **not** change — and it takes that branch *without consulting* `lora_should_clear_cache`. A bare-base request to an adapter-registered server would therefore have run with **every** adapter applied, against the previous request's cached prefix: I17's own defect, in the shipped server. Every request now states the complete configuration. This is the single most valuable thing the row found, and it was found by reading the server's source rather than by a test. (2) **The `Provider` protocol had to grow after all.** ADR-0062 decision 1 celebrates that it does not change; that claim holds for the *load/unload* seam and is untouched, but `list_adapters()` and `register_adapters()` were added, because otherwise LoadCoach must `isinstance`-check `LlamaCppProvider` to show an adapter row or fold in a rescan — importing a concrete adapter is exactly what the protocol exists to prevent. Recorded here rather than quietly done; **if the architect prefers it otherwise, that is a small revert plus a downcast in LA2.** (3) The venv is **Python 3.13.15**, not 3.14.4 as §0 stated. (4) `baseaicore` **0.4.1 is on PyPI** but the venv held 0.4.0, which the `>=0.4,<0.5` pin permitted; the code would have imported and then failed at the first adapter. The floor is now `>=0.4.1`. (5) **No LLM adapter GGUF exists on this machine** — the only LoRAs here are Wan 2.2 video diffusion safetensors — so I17's semantic canary is written and **skips visibly**, naming exactly what an operator must produce. That is an H1 blocker for the LA1 exit, not a P7 gap. **Also decided, and open to reversal:** an entry at scale `0.0` is a *disable*, not a second adapter, so the complete `lora` list does not violate ADR-0063 rule 1's "at most one entry" — read as governing *enabled* entries. And a restart required for **correctness** (a profile change) now refuses with `ProviderUnavailable(restart_pending)` while work is in flight rather than killing a server mid-stream, which is a behaviour change to Phase 6's immediate restart. | The prefix-reuse property is exactly the intermittent-defect shape; the conformance-test design is the judgment |
| **F4** | LoadLedger P3 → **0.2.0** on PyPI | Sonnet 5 · high | F1 | **A balance read that names no run.** LoadLedger 0.1.0 reports a balance only *through a ceiling and for a named `run_id`* — `remaining(run_id)` and `would_exceed(run_id, …)` are the whole of it — so a scope that has no cap has no balance anyone can ask for. F1 met that twice (`F1_HANDOFF.md` §7): `promptcadence ledger show --scope tier` reports debit **counts** instead of spend, because summing `entries()` in the application would be ledger arithmetic exactly where ADR-0050 forbids it and inventing an unreachable ceiling to read a number through would put a magic figure in the record; and `GET /ledger` names an arbitrary known trajectory as its reference run, which is *sound* — a `per_day` or `per_tag` window is ledger-wide, so the answer is identical whichever known run is named — but is a signature working around a missing read, and it degrades to "the configured caps with nothing spent" on an empty ledger. **Add** `balances(*, scope, window_key)` (naming, and whether it returns a small value object or a `CeilingVerdict`-shaped one with no ceiling on it, are the row's to decide) returning the accumulated tokens, the per-currency money and the three honesty counts for one window, with **no** `run_id` and **no** ceiling. **It needs no new storage**: `{prefix}balances` is already keyed `(scope, window_key)` and `{prefix}balance_money` `(scope, window_key, currency)`, which is exactly this query. Implement on `InMemoryLedger` and `SqlLedger` through the one `BalanceBook`, as every other method is, so the deterministic double keeps answering what production answers. Keep the honesty counts on it — a balance returned without `unpriced_debit_count` is a floor presenting itself as a total, which is the one thing spec contract 2 forbids. Add the tag-listing this implies if it is cheap (`window_keys(scope)`), so a caller can ask "which tiers have spent anything" without reading every entry; leave it out if it is not. **Then, in PromptCadence** (same row, second commit): drop `TrajectoryService.most_recent_id()`'s use as a ledger reference, make `--scope tier` and `GET /ledger`'s `tiers` array report real spend rendered through the existing `render_money`/`render_remaining_money`, and amend `apps/promptcadence/spec.md` §12 and `lifecycle.md` §6 where they currently say a tier has a history and not a balance. **Not in this row:** a tier *ceiling* — lifecycle §6 is deliberate that none is configured, and this row adds a **view**, not a cap. **Read:** `packages/loadledger/spec.md` §7 and §10, `py/LoadLedger/src/loadledger/core.py` (`BalanceBook.verdicts`/`_verdict_for` — the counts come from there), `py/LoadLedger/src/loadledger/sql.py` (`_seed_from_rows`), ADR-0050, `F1_HANDOFF.md` §7 **Gate A and Gate B done 2026-09-04, Gate C blocked on the publish** (`F4_HANDOFF.md`; LoadLedger `9e1d15a`+`e579f15`, docs `478973b`+this commit). **Not pushed, not tagged, not published** — the standing instruction. `loadledger 0.2.0` is prepared and its wheel was built and install-checked locally in both shapes (pure core with only `baseaicore`; `[sql]` resolving SQLAlchemy); locks needed no recompile and that was proven, not assumed (`git diff v0.1.0 -- pyproject.toml requirements/` is empty). **PromptCadence is untouched at `2bfc692` and must stay so until 0.2.0 is on PyPI**: it has no `requirements/` locks and its CI installs `-e ".[dev]"` against PyPI, so moving the pin first is a red CI run. **The row took §0.2's route (a):** `balances(*, scope, window_key) -> WindowBalance` **and** `position()`, because `balances()` alone fixes only `--scope tier` — `--scope day` and `--scope project` need headroom against a configured ceiling, and deriving that outside the package would put the floor rule and the `exceeded` decision in a consumer. So the row's "drop `most_recent_id()`" claim stands, and it is `position()` that delivers it. A `PER_RUN` ceiling passed to a run-free read is **refused** with `InvalidCeiling` rather than omitted, because omitting silently shortens a tuple whose positional correspondence with `ceilings` is documented API. `window_keys(scope)` was **not** shipped — no consumer asks. **This row's text is wrong in one place:** a tier has no ceiling and therefore no *remaining*, so the tier half renders through `render_money` / `render_tokens` (the "at least" pair) and **never** `render_remaining_money`, which qualifies in the opposite direction. **§3's soft edge is NOT yet satisfied — G1 must not start until Gate C lands**, or its dashboard is built against the debit count this row exists to retire. | Additive, against a spec that already describes the storage; the shape is decided and the arithmetic already exists |
| **G1** | **PromptCadence P7** → **0.9.0b0 — M11 beta** | **Fable 5 · xhigh — daytime, reviewed** | F2 | Planner, approval modes (`auto`/`hybrid`/`manual`), intent minting in both paths, DAG ready-set dispatch, **the governance-invariance diff (contract 1, I11)**, plan-schema resilience against real local models. **Read:** `apps/promptcadence/development-plan.md` Phase 7, `apps/promptcadence/lifecycle.md` §4 + §8.4, accepted D-3/D-4/D-5/D-12, `roadmap/promptcadence-roadmap.md` §9 I11 | Design-dense, live-model interaction, and the arc's central proof — a failed pass invalidates the design claim, not just the code |
| **H1** | ModelRack P8 → publish (LA1 done) | **Opus 5 · high** | F3 | Cancellation under supervision + leak tests (20 cycles, no orphan, flat memory) — Opus; conformance suite green across all four providers, docs, publish — Sonnet. LA1 exit needs a GPU session: 20 alternating-adapter generations, zero base loads (I16). **Also, and before publication: [ADR-0074](../adr/0074-adapter-enabled-serving-is-a-runtime-profile-field.md).** F3 built adapter serving and found ADR-0060's "serving mode lives in the runtime profile" had no mechanism: a base on an adapter-registered server and the same base on a clean one hash to the same `profile_hash` and would merge. Decided by the operator on 2026-09-04 as a **real field**, not a `provider_options` convention. Two small pieces: **BaseAiCore** gains `RuntimeProfile.adapters_registered: bool | None = None` (additive inside `>=0.4,<0.5`; the hash already drops `None` fields, so every stored hash is unchanged — a `False` default would move all of them, which is why it is tri-state), and **ModelRack** refuses a request whose profile disagrees with the server it would use, the `context_configurable` discipline one level up. It belongs here rather than later because 0.7.0 publishing without it ships a known silent-merge gap, and because evidence recorded before the field exists is **not separable afterwards**. LoadCoach sets it at H2; FreeWeight's A/B consumes it at H4/B5. **Read:** `roadmap/adapter-roadmap.md` §4.1 P8 + §3 LA1 exit, ADR-0074, `F3_HANDOFF.md` §7.8 | Leak/cancellation reasoning vs. publication mechanics — a clean split |
| **H2** | **LoadCoach 1.1** (LA2) | **Opus 5 · xhigh** | H1 | Generalized LC-E1 (`[providers.<name>]`, D-11), adapter registry + `adapters scan|list|show`, subject expansion + compatibility/classification filters, `require_adapter_evidence` gate, pins, two-level residency (`base_switch_penalty`, `ignore_residency`), per-subject reliability, explanations/UI. Also carries the unreleased `2c7d740` fix. Exit: the IdeaPress three-stage demo — one base load (I16), classification denial recorded (I19). **Read:** `roadmap/adapter-roadmap.md` §4.2 + §7 I13/I16/I19, accepted D-11/A-4/A-7/A-8/A-9/A-10, `apps/loadcoach/spec.md` | Routing semantics frozen into persisted explanations — the LoadCoach-P3 precedent, extended |
| **H3** | IdeaPress adapter pins → minor release | Sonnet 5 · standard | H2 | Per-stage adapter pins in config passed as the LoadCoach override; provenance columns on every attempt; direct/Ollama mode stays adapter-free. **Read:** `roadmap/adapter-roadmap.md` §4.4, accepted A-7 | Config + override passthrough against a settled contract |
| **H4** | FreeWeight 1.1 (LA3) | Sonnet 5 · high | H2 + B5 | `[adapters] directory`, subject enumeration, `--adapter` flag, the A-2 panel policy (declared + regression + performance), serving-mode A/B, evidence export with v1.1 fields (an import change — `CapabilityEvidenceV1_1Out`/`In`; the bare names still mean `1.0`, ADR-0068 rule 3 — and bundled export needs B5's `benchmark.evidence_bundle` `1.1`), comparison UI grouped by base. Exit I18 needs a GPU session. **This row also carries LoadCoach's `setspec` pin** (E5's deliberate exception: moving it without the `CapabilityEvidenceV1_1Out`/`In` adoption turns three local-only reds in `tests/contract/test_evidence_import.py` into CI reds) **and, folded in here by decision on 2026-09-04, Commissioner's.** `commissioner 0.1.0` pins `setspec>=0.5,<0.6`, which since F2 holds PromptCadence's resolved `setspec` at 0.5.x even though PromptCadence's own range admits 0.6 — the same shape as the `mirrorwall<0.5` cap E5 spent a whole row removing, and harmless only for as long as nothing in PromptCadence consumes a 0.6 payload. This row is where all the 0.6 movement happens, so widen Commissioner to `setspec>=0.5,<0.7` here, **as its own release** (`commissioner 0.1.1` — a second repository and a second publish inside a row otherwise scoped to FreeWeight, which is the cost of keeping the 0.6 movement together and is accepted deliberately). Commissioner adopts no payload in doing so: it owns the Python form of `governance.egress_decision` 1.0 and that is unchanged, so this is a range widen and a gate, the same shape MirrorWall's was. Its `docs/packages/commissioner/spec.md` §56 states the range deliberately and moves with it. Then re-resolve PromptCadence and confirm with `pip show` that it reaches 0.6.0 — the widen is only real if something actually resolves past 0.5.x. **Read:** `roadmap/adapter-roadmap.md` §4.3 + §7 I18, accepted A-2/A-3, `apps/freeweight/spec.md`, `F2_HANDOFF.md` §7 (the cap, and the evidence it binds) | Measurement plumbing on an existing engine; panel composition is spec'd. The two pin widens are mechanical and proven twice |
| **I1** | PromptCadence P8 | **Opus 5 · high** | G1 + E1 | Materialization/invalidation of explanation revisions with the `materialize(rows) == compose_live(rows)` golden (D-13) — Opus; CutCtx wiring and the MirrorWall operator UI volume — Sonnet. **Read:** `apps/promptcadence/development-plan.md` Phase 8, `apps/promptcadence/lifecycle.md` §7 + §9.1, accepted D-8/D-13 | Cache-consistency reasoning vs. UI volume — a clean split |
| **I2** | **PromptCadence P9** → **1.0.0 — M12** | **Opus 5 · xhigh** (consider Fable 5 for the injection corpus) | I1 + H2 | Hardening: the prompt-injection corpus as a release gate, spec §20 criteria, overhead budget (≤ 25 ms/turn), live remote-tier trajectory via LC-E1 (I13) *or* the recorded release-scope decision to ship with remote tiers refusing honestly. **Read:** `apps/promptcadence/development-plan.md` Phase 9, `apps/promptcadence/spec.md` §20, `roadmap/promptcadence-roadmap.md` §9 I13 + §10 | The security half is pure adversarial and never overnight; measurement and docs are not |
| **J1** | IdeaPress 1.1: IP-A1 LoadLedger | Sonnet 5 · high | I2 | Mount the tables, debit per attempt with `pricing_hash`; a per-output ceiling (`PER_RUN`, the unit id) and a per-project ceiling (`PER_TAG`, `project:<id>`, lifetime) from IdeaPress's configuration, resolved per operation (LoadLedger plan Phase 2); per-unit/per-project cost and position in the UI (`—` + reason for unpriced local; "at least" for a floor). Delete the in-app equivalent. **Read:** `roadmap/promptcadence-roadmap.md` §6.1, `packages/loadledger/spec.md` §2 + §21, ADR-0030, ADR-0069 | Third mounting by now; the shape is adopt-delete-prove |
| **J2** | IdeaPress 1.1: IP-A2 Commissioner | Sonnet 5 · standard | I2 | The S4 egress badge reads recorded decisions; denials visible in project history; badge backed by rows, not a flag. **Read:** `roadmap/promptcadence-roadmap.md` §6.2, `packages/commissioner/spec.md` | Reading rows into an existing badge — small and spec'd |
| **J3** | IdeaPress 1.1: IP-A3 CutCtx → **1.1.0** | Sonnet 5 · high | I2 | `project_review` context assembly as a policy chain; in-app reduction code deleted; golden-tested identical output on fixture projects. **Read:** `roadmap/promptcadence-roadmap.md` §6.3, `packages/cutctx/spec.md` | Golden-parity refactor — the safest kind, but worth `high` since it deletes working code |

### 1.1 M9 — Suite 1.0, outside the arcs

One outstanding milestone predates both arcs and appears in neither: **M9, the professional
delivery checklist** ([master-roadmap §7](master-roadmap.md)) over the nine existing components.
It neither gates nor is gated by any row above — it can run any time, in parallel with anything.
Suggested treatment: one **Opus 5 · high** audit session that walks §7 against the repos as they
stand and emits a gap list (many boxes are likely already true and just unchecked), then ordinary
**Sonnet 5 · high** sessions per gap (docs generation, backup/restore and migration-path tests,
compatibility matrix); the declaration itself is a human step. Note for the A1 session: master
roadmap §9's "current state" is frozen at end-of-M7 and should be corrected to the true state
(all nine components tagged and published) in the same pass that adds the M10–M13 pointer row.

## 2. The standing preamble for every kickoff prompt

Every session's prompt, regardless of row, should state:

* The component directory to work in (never the workspace root), its venv, and
  `pip install -e ".[dev]"` if the repo is fresh.
* The finish line: `ruff format --check . && ruff check . && mypy src tests && lint-imports &&
  pytest -m "not live and not performance"` green, `CHANGELOG.md` updated, one Conventional
  Commit per phase. Name the interpreter in the report (M5C-13).
* The house method: docstring-first, `from __future__ import annotations`, units in names,
  keyword-only optionals, injected clocks/providers/roots.
* Read `architecture/master-architecture.md` §§1–3 and the component's gold-standards section
  before the row's reading list.
* New packages pin unpublished suite deps as local path/editable installs with a
  `TODO: re-pin on publish` until the PyPI release lands.
* Overnight rules ([model-assignment §2.12](model-assignment.md)): Sonnet rows run at effort
  **high** overnight; **batches D and G and I2's security half never run overnight** — they are
  won by review and fail silently.

## 3. Which orderings are load-bearing

Hard edges — never reorder across these:

* **A1 before all code** — ADRs before implementation, the standing rule.
* **B1 before B3** — the payload publishes before its consumer pins it (the FreeWeight-P11 lesson).
* **B5 before H4** — a bundle cannot carry adapter evidence until the bundle has a minor that
  nests it; the same rule, one payload out (ADR-0068 rule 5).
* **C5 before D3** — the usage rule (ADR-0070) before the third adapter is written; a retrofit
  is a fixture re-annotation nobody wants.
* **C6 before F1** — all four token classes on LoadCoach's wire before the harness prices them;
  without it a strict money ceiling trips on every remote turn (ADR-0069 §"Not decided here").
* **D1/E2 before E4** — no tool executes in PromptCadence before the discipline is published
  (a security ordering).
* ~~**E5 before F2 and H4**~~ — **satisfied 2026-09-04 for F2.** `mirrorwall 0.2.1` pinned
  `setspec<0.5` and every application depends on mirrorwall, so no application could reach
  `governance.egress_decision` (setspec 0.5, needed at F2) or `capability.evidence` `1.1`
  (setspec 0.6, needed at H4) until MirrorWall widened and published. It has; PromptCadence,
  IdeaPress and FreeWeight are swept, and **F2 is unblocked** — `pip install -e PromptCadence
  commissioner==0.1.0` resolves, to `setspec 0.5.0`. LoadCoach's own pin remains the exception:
  it rides H4, because moving it turns three evidence-golden tests red until H4 adopts
  `CapabilityEvidenceV1_1Out`, **so H4 is still gated on that adoption, not on MirrorWall.**
  **Note for every later PromptCadence row:** `commissioner 0.1.0` pins `setspec>=0.5,<0.6`, so
  adding it at F2 pinned the resolved `setspec` to 0.5.x even though PromptCadence's own range
  admits 0.6. That costs nothing today — nothing in PromptCadence consumes a 0.6 payload — but it
  is the same shape as the `mirrorwall<0.5` cap this row removed. **Scheduled: H4 widens
  Commissioner to `setspec>=0.5,<0.7` and publishes `commissioner 0.1.1`**, alongside LoadCoach's
  own pin, so that all the 0.6 movement happens in one row. Until then PromptCadence resolves
  0.5.x by design, and reading that as a regression is the mistake this note exists to prevent.
* **PromptCadence P1→…→P9 strictly in order**; each package's phases in order.
* **H2 before I2's live-remote verification** — both touch LoadCoach; land LA2, then verify once.
* **J-rows after I2** — adoption targets a released 1.0, never a moving target (ADR-0011).

* **F4 before G1** — G1 builds the M11 beta's dashboard, which spec §17 says shows "today's
  ledger position against the daily ceiling and against each configured project's ceiling". That
  works today through F1's reference-run workaround; the per-tier half of it does not, and a
  dashboard shipped in a beta with a column reading "1 debit(s) recorded" is a column somebody has
  to explain. Soft edge, not hard: nothing in G1 fails without it.
  **Status 2026-09-04: not yet satisfied.** F4's Gate A and Gate B are done and `loadledger 0.2.0`
  is prepared, but its Gate C — the PromptCadence half, which is the half that changes the
  column — cannot start until the wheel is on PyPI. Until then `--scope tier` still reports a
  count, and G1 would build that column twice.

Everything else is convenience. Genuinely flexible rows, placed for model adjacency: **D3/F3/H1**
(the ModelRack stream can slot anywhere after C5, whenever harness work blocks), **E1** (only
I1 needs it), **B1** (only B3 needs it), **B5** (only H4 needs it), **H3** (any time after H2),
**F4** (any time after F1, but see the soft edge above). One resource rule: LA1/LA2
GPU sessions never share the machine with FreeWeight benchmark runs.

## 4. Not model sessions — the human/ops checklist

* Create the five repos (cutctx, toolyard, loadledger, commissioner, promptcadence) with the
  standard toolchain + CI; reserve the PyPI names; mirror each component's docs byte-identically
  on repo creation.
* Per release: tag, Release-workflow `pypi` environment approval, post-publish install check.
* ~~**`mirrorwall 0.2.2` is prepared and waiting**~~ — **done 2026-09-04.** Pushed
  (`6d50c3f..092af6e`), tagged `v0.2.2`, the `pypi` environment approved by the operator, and
  `mirrorwall 0.2.2` is on PyPI. Post-publish install check passed in two clean Python 3.13.15
  venvs, the second being the proof the cap is gone: `mirrorwall==0.2.2` and `setspec==0.6.0`
  install together. The pin sweep then ran — see the E5 row. **One thing this step taught, for
  every future release:** a single `git push origin <tag>` fired **two** identical Release runs
  one second apart, both waiting on the same environment. Approve exactly one and cancel the
  other; the second to reach `twine upload` fails on *file already exists* and leaves a red run
  against the tag for nothing.
* Review the morning diffs for every overnight batch; review Fable batch output same-day.
* After D1 (done and reviewed 2026-09-03, unpushed): push ToolYard `main` and docs `main`, confirm
  CI green (the `isolation` tests skip on the runner — a skip, visible, not a pass).
* **Podman is unverified and is now a blocking item for M11.** It is *first* in ToolYard's ladder,
  so it is the rung that will actually run commands on any podman host, and no canary has ever run
  on one: `E2` published `toolyard 0.1.0` without it (the step was written before E2 and did not
  block it), and E4 exercised the container rung through **docker** on the reference machine.
  `pytest -m isolation -rs` on a real podman host, green, is an M11 exit condition — a skip is
  visible, and it is not a pass.
* GPU sessions on the reference machine for the LA1 exit (I16 zero-base-loads run), the LA2 exit
  (the three-stage one-base-load demo), and H4's serving-mode A/B — never beside a FreeWeight
  benchmark.
* Re-run the IdeaPress standalone journey and read the output (the M8 leftover) before IdeaPress
  1.1 work begins.
* After D2 (done 2026-09-03, unpushed): push LoadCoach `main` (`846348b`, the `finish_reason`
  render), PromptCadence `main` and docs `main`; confirm CI. After E4 ships the five harness
  profiles: run `pytest -m live` and `promptcadence run "…" --bypass-planning --follow` against a
  LoadCoach on **Ollama** with the shipped defaults and no `PROMPTCADENCE_TIERS__*` overrides —
  the fake provider declares `stop` for every answer, so `length` and `content_filter` on a real
  model are unexercised until this run.
* After D3 (done 2026-09-03, pushed, CI green; `D3_HANDOFF.md` at the workspace root): review
  the diff same-day (handoff §11 and §13). llama.cpp `b10792` is **installed** on the reference
  machine (`~/ai/tools/llama.cpp`, CUDA 13.1, `~/.local/bin/llama-server`; a user-local CUDA
  include overlay works around the CUDA-13.1/glibc-2.43 `rsqrt` clash, llama.cpp #19100) and the
  live journey has **run and passed** — load, generate, stream with a shared prefix reported as
  `cache_read`, native completion, unload with no process left. Re-run it after any llama.cpp
  upgrade: `MODELRACK_LLAMACPP_MODELS=~/ai/models/llm MODELRACK_REQUIRE_LLAMACPP=1 pytest -m live
  tests/live/test_llamacpp_live.py -rs -s`. ModelRack stays at `0.6.0` until H1.
* Optional: extend `suite-flowchart.drawio` with PromptCadence's final shape and the adapter arc.

## 5. Milestone map

| Milestone | Rows | Declared when |
|---|---|---|
| **M9** — Suite 1.0 (pre-arc) | §1.1 — audit + gap sessions, any time | Master-roadmap §7 checklist complete over the nine existing components |
| **M10** — foundations | A1–F2 package rows (A1, A2, B1–B3, C1–C3, C5, C6, D1, E1–E3) | All four packages at 0.1.0, clean-venv acceptance scripts pass |
| **M11** — beta | B4, C4, D2, E4, F1, F2, G1 | The planned-vs-bypassed demo on real LoadCoach; **`pytest -m isolation -rs` green on a real podman host** (see §4 — podman is first in ToolYard's ladder and has never been exercised; the reference machine has docker only, and E4 ran the container rung through docker); `0.9.0b0` |
| **LA1/LA2/LA3** | D3, F3, H1 / H2, H3 / B5, H4 | Adapter-roadmap §3 exit conditions |
| **M12** — 1.0 | I1, I2 (+ H2 as the LC-E1 dependency) | Spec §20; `promptcadence 1.0.0` published |
| **M13** — adoption | J1–J3 | IdeaPress 1.1; every package has two real consumers |
