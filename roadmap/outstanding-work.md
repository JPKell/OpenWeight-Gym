# Outstanding Work — the one schedule

**Added 2026-09-02.** Everything left to build across the [PromptCadence arc](promptcadence-roadmap.md)
(M10–M13) and the [Adapter arc](adapter-roadmap.md) (LA0–LA3), in execution order, one row per
model session. The roadmaps stay authoritative for *rationale*; the
[model-assignment guide](model-assignment.md) (§2.10–2.12) stays authoritative for *model choice*
— and since 2026-09-04 a phase gets **one** model, the stronger of any pair that used to be a split
([§3.5](model-assignment.md) records why, from row F1's evidence);
this document is the schedule view that answers "what do I run next, on which model".

**How to use a row.** Copy the row into an Opus session and ask it to draft the kickoff prompt for
the working session. The reading column names everything phase-specific; every session's prompt
should additionally include the standing preamble in §2 below. Batch letters group rows that are
adjacent *and* on the same model, so consecutive rows with one letter can be run back-to-back in
one sitting; letters are dependency-ordered, and §3 lists which orderings are load-bearing versus
merely convenient. All paths are relative to `~/ai/suite/docs/`.

## 1. The master table

| # | Phase → ships | Model · effort | Runs after | Work overview — and required reading | Why this model |
|---|---|---|---|---|---|
| **A1** | **Phase 0 + LA0** → ADRs accepted, docs amended | **Opus 5 · xhigh** | — (gates everything) | Write and accept ADRs D-1…D-13 and A-1…A-10 (numbered 0045+); amend master architecture per the checklists; update docs README index, gold-standards §2, traceability matrix, risk register, master-roadmap pointer; retire `harness.md` with a pointer. **Read:** `roadmap/promptcadence-roadmap.md` (§2, §7), `roadmap/adapter-roadmap.md` (§2, §9), `architecture/adapter-identity-and-serving.md`, the ADR index + ADR-0038 as the amendment precedent | Permanent contracts, but every decision is already made and argued in the roadmaps — expansion, not invention. Fable would be 2× to transcribe settled judgment |
| **A2** | BaseAiCore **0.4.1** → PyPI | **Opus 5 · high** | A1 | `DataClassification` (ordered, three levels, D-2) + `AdapterIdentity` and the canonical `+name@sha256:…` suffix (A-1); goldens; fold the deltas into the BaseAiCore spec/plan docs; publish. **Read:** accepted D-2 + A-1 ADRs, `packages/baseaicore/spec.md`, `architecture/adapter-identity-and-serving.md` §§2–3 | §3.3's BaseAiCore rule: wrong here is a data-compatibility break in every repo. Tiny phase — the premium costs almost nothing |
| **B1** | SetSpec **0.5.0** (Phase 6) → PyPI | Sonnet 5 · high | A2 | `governance.egress_decision` 1.0 (D-7/D-10), `model.adapter_manifest` 1.0 + `CapabilityEvidence` v1.1 optional adapter fields (A-1/A-4), JSON Schemas, ≥ 3 goldens each; non-adapter evidence must round-trip byte-identically (I15); fold a Phase 6 section into the SetSpec docs; publish. **Read:** `packages/setspec/spec.md`, `packages/commissioner/spec.md` (payload shape), `roadmap/adapter-roadmap.md` §3 LA0 + §7 I15, accepted D-7/D-10/A-1/A-4 | The SetSpec-P2 precedent: normative field tables → transcription with validation. Goldens get an Opus review pass in the morning |
| **B2** | LoadLedger P1 | Sonnet 5 · high | A2 | Ceilings, debits, verdicts, windows, `InMemoryLedger` — dual token+money ceilings per ADR-0030, the `unpriced_debit_count` that lets PromptCadence raise `UNPRICED_EGRESS_REFUSED` (the code itself is PromptCadence's, never this package's — ADR-0047), UTC-midnight window edges. **Read:** `packages/loadledger/spec.md`, `packages/loadledger/development-plan.md` Phase 1, ADR-0030 | §3.4 exactly: integer arithmetic with hand-computable fixtures; the UTC-midnight trap is named three times in the docs |
| **B3** | Commissioner P1 | Sonnet 5 · high | B1 | Vocabulary binding, `OrderedClassificationPolicy` (fail closed on undeclared ceiling), decision verdicts, payload round-trip against the published `setspec` 0.5.0. **Read:** `packages/commissioner/spec.md`, `packages/commissioner/development-plan.md` Phase 1, accepted D-10 | Enum-parametrized matrix, exhaustive by construction; the fail-closed row is spec'd |
| **B4** | PromptCadence P1 | Sonnet 5 · high | A2 | Repo skeleton, config, database, health endpoint, CLI stub — the standard app shape. **Read:** `apps/promptcadence/spec.md` §§1–8, `apps/promptcadence/development-plan.md` Phase 1; LoadCoach's repo layout as the precedent | Identical in shape to LoadCoach P1; boilerplate against a precise spec |
| **B5** | SetSpec **0.6.0** (Phase 7) → PyPI | Sonnet 5 · standard | B1 | `benchmark.evidence_bundle` **1.1**: the bundle nests `CapabilityEvidenceV1_1Fields` so adapter-bearing evidence can travel *inside* an exported bundle, which `1.0` cannot carry — it nests the frozen `capability.evidence` `1.0` shape by reference and does not move when that payload gains a minor ([ADR-0068](../adr/0068-a-post-freeze-minor-is-a-sibling-class.md) rule 5). Same sibling-class mechanism as B1's `capability.evidence` `1.1`: `EvidenceBundleV1_1Fields` beside an untouched `EvidenceBundleFields`, bare `EvidenceBundleOut`/`In` keep meaning `1.0`, `1.0` goldens byte-unchanged. JSON Schema, ≥ 3 goldens, docs section, publish. **Read:** ADR-0068, `packages/setspec/spec.md` §11 rule 9 + §19, B1's `capability.evidence` `1.1` as the worked example | A second application of a mechanism now written down and demonstrated once — transcription against a precedent in the same repo, which is what `standard` is for |
| **C1** | CutCtx P1 | **Opus 5 · xhigh** | A2 | Transcript model, the `_invariants` module, plan/executor split, `DropOldestPolicy` — property tests whose choice of properties is the deliverable. **Read:** `packages/cutctx/spec.md`, `packages/cutctx/development-plan.md` Phase 1, accepted D-8 | The invariants gate every future policy routes through (first-instance rule); the property choice is the judgment |
| **C2** | ToolYard P1 | **Opus 5 · xhigh** | A2 | Tool specs, registry, executor discipline, structured refusal paths — "model input never raises", fixed refusal order, fuzzed. **Read:** `packages/toolyard/spec.md`, `packages/toolyard/development-plan.md` Phase 1, accepted D-9, ADR-0018, ADR-0026 | The discipline every later tool inherits; the FakeProvider lesson — stricter than its first consumer needs |
| **C3** | LoadLedger P2 → **0.1.0** on PyPI | **Opus 5 · high** | B2 | `loadledger.sql`, `SqlLedger`, `mount_ledger_tables`, kill-mid-debit atomicity on both dialects, the miniature-host Alembic test (I14). **Read:** `packages/loadledger/development-plan.md` Phase 2, accepted D-6, ADR-0006 | First mounting implementation (first-instance rule) + atomicity on money rows — the §3.3 failure shape |
| **C4** | PromptCadence P2 | **Opus 5 · xhigh** | B4 | Domain core: threads, tiers, classification, plans, **`ExecutionIntent`**, deviation matrix, state machine from the T1–T17 tables — with the LA0 optional adapter fields born in. **Read:** `apps/promptcadence/spec.md` §9, `apps/promptcadence/lifecycle.md` §§2–5 + §8, `apps/promptcadence/development-plan.md` Phase 2, accepted D-2/D-3/D-12 | Mostly normative-table transcription — but this phase freezes the shape of the design's load-bearing wall |
| **C5** | **ModelRack: the ADR-0070 usage rule** → next minor | Sonnet 5 · high | A1 (flexible — must land before D3) | Both real adapters read usage to the per-response rule: cache detail present → disjoint classes; absent → cache classes `0`; no usage object → every class `UNSUPPORTED`. `FakeProvider` defaults to `0` with scripted `UNSUPPORTED` still possible; recorded fixtures re-annotated; the three-case conformance test; the Ollama `prompt_eval_count` fixture check. Spec contract 2 is already amended. **Read:** ADR-0070, ADR-0069 §"Not decided here", `packages/modelrack/spec.md` §11 + §18, the two `_read_usage` functions and their fixtures | Two small functions against a stated rule, but in a package three apps call and with a named fabricated-zero failure mode — high, not standard |
| **C6** | **LoadCoach: four token classes on the wire** → next LoadCoach minor | Sonnet 5 · standard | C5 | `cache_write_tokens` + `cache_read_tokens` on the attempts and jobs rows (one migration) and on the job document's `usage`, `"unsupported"` per ADR-0016 rule 4; `apps/loadcoach/data-model.md` and `api.md` amended first; additive within `/api/v1`. **Read:** ADR-0070 decision 7, `apps/loadcoach/data-model.md`, `apps/loadcoach/api.md`, `services/queue.py`'s `JobView` and the worker's usage capture | Additive columns and fields transcribed from an ADR; the migration is the only judgment |
| **D1** | **ToolYard P2** (sandbox) | **Fable 5 · xhigh — daytime, reviewed** | C2 | Containment + tiered isolation (container → bwrap → refuse), TOCTOU and prefix-collision cases; marked tests need the reference machine's bwrap. **Read:** `packages/toolyard/development-plan.md` Phase 2, accepted D-9, ADR-0018 | Adversarial with a silent-failure class — an escape that works is quiet. Never overnight |
| **D2** | **PromptCadence P3** | **Fable 5 · xhigh — daytime, reviewed** | C4 | LoadCoach client, bypass loop, lease/claim, kill −9 recovery, one-write transitions (ADR-0044), events — **plus the fake LoadCoach every later phase trusts** (I10 contract tests against the OpenAPI snapshot). **Read:** `apps/promptcadence/development-plan.md` Phase 3, `apps/promptcadence/lifecycle.md` §8.3 + §10, ADR-0036/0044, `roadmap/promptcadence-roadmap.md` §9 I10 | The LoadCoach-P5-shaped phase: no feedback loop for the interesting failures, and the fake doubles the stakes |
| **D3** | **ModelRack P6** (LA1 start) | **Fable 5 · xhigh — daytime, reviewed** | A1 + C5 (flexible — placed here for model adjacency) | `LlamaCppProvider` process supervision: spawn/health-wait/terminate, kill-tree, pid files, port range, GGUF discovery + hashing, generation + streaming, version-annotated recorded fixtures; usage read to the ADR-0070 rule from the start. **Read:** `roadmap/adapter-roadmap.md` §4.1 P6, `architecture/adapter-identity-and-serving.md` §§4–6, accepted A-5, ADR-0070, `packages/modelrack/spec.md` (Provider protocol) | Concurrency with orphan/leak guarantees, no useful feedback loop, in a package three apps call — the §3.3 class |
| **E1** | CutCtx P2 → **0.1.0** on PyPI | Sonnet 5 · high | C1 (flexible until I1) | `ObservationMaskingPolicy`, `SummarizingPolicy` (planned requests, never executed — D-8), `PolicyChain`; the contiguous-group dict-ordering trap is golden-tested. **Read:** `packages/cutctx/development-plan.md` Phase 2, accepted D-8 | Policies against locked invariants; the one risky spot is named in the plan |
| **E2** | ToolYard P3 → **0.1.0** on PyPI | Sonnet 5 · high | D1 | Built-in tools; `http_fetch` implements ADR-0026 §3 with the vectors byte-shared with LoadCoach's suite. **From D1 (2026-09-03; `docs/history/D1_HANDOFF.md` §8 and §13):** `run_command_tool(sandbox: Sandbox)` takes the **same** `TieredSandbox` instance the executor holds (spec §7 as amended) and declares `requires_isolation`; it passes an explicit `PATH` allowlist such as `{"PATH": "/usr/local/bin:/usr/bin:/bin"}` — under bwrap `--clearenv` leaves none, and `os.environ` is never the answer; env is caller-owned, never model-supplied; it never passes `network=True` (refused in v1); it renders both `SubprocessResult.output_truncated` and the exit code (an output bomb is killed at the cap); its description says that a backgrounded child keeps the call alive until the timeout; document a floor for `ResourceLimits.process_count` (bwrap's count includes `bwrap` and `prlimit`, so 1 or 2 refuses everything). **Before publishing 0.1.0:** run `pytest -m isolation -rs` on a host with podman — the rung is in the ladder but unverified on the reference machine (§4). **Read:** `packages/toolyard/development-plan.md` Phase 3, ADR-0026 §3, `packages/toolyard/spec.md` §7 and §14 as amended at D1 | Fixture/vector work; Opus reviews the fetch-handler diff |
| **E3** | Commissioner P2 → **0.1.0** on PyPI | Sonnet 5 · standard | B3 + C3 | Append-only ledgers + mounting — copies LoadLedger P2's proven pattern verbatim. **Read:** `packages/commissioner/development-plan.md` Phase 2, LoadLedger P2's implementation as the template | The "rest of the repeated thing" — the pattern is proven twice by now |
| **E4** | PromptCadence P4 | Sonnet 5 · high | D2 + E2 | ToolYard integration, workspace lifecycle, `undeclared_tool` deviation wiring; hostile-model journey tests are specified. **From D1:** build one `TieredSandbox` per executor and hand the same instance to `run_command_tool`; the container image is a constructor argument that must be present locally (`--pull=never`), so configuration names it and `doctor` shows `TierReport.reason`; `SandboxPaths` now refuses relative or overlapping roots at construction, so the workspace lifecycle builds absolute, disjoint roots. **From D2 (2026-09-03; `docs/history/D2_HANDOFF.2.md` §4 and §7) — the tier defaults, decided by interview and implemented here:** PromptCadence's shipped defaults name `tools.agent.local_fast` / `tools.agent.local_large` (spec §12, ADR-0047 §1) and LoadCoach ships one `tools.agent`, so the live journey only ran with `PROMPTCADENCE_TIERS__*` overrides. E4 closes this as its **first commit, in the LoadCoach repository**: the five harness profiles go into LoadCoach's shipped `src/loadcoach/config/task_profiles.toml` as namespaced specializations of `tools.agent` — configuration, no LoadCoach code, changelog under Unreleased, no bump. (1) `tools.agent.local_fast`: `tools.agent`'s weights and execution; constraints `min_context_tokens = 16384`, `requires_capabilities = ["tool_use"]`, `max_latency_p95_seconds = 60`, `min_capability_scores = { tool_use = 0.3 }`, `allow_remote_providers = false`. (2) `tools.agent.local_large`: weights leaning to reasoning (`agentic 0.30, tool_use 0.25, reasoning 0.30, reliability 0.15`); `min_context_tokens = 32768`, `requires_capabilities = ["tool_use"]`, `max_latency_p95_seconds = 300`, `min_capability_scores = { tool_use = 0.4, reasoning = 0.4 }`, `allow_remote_providers = false`; `max_output_tokens = 8192`. The minimum context of each local profile **equals the tier's `context_budget_tokens`** (16384 / 32768) — LoadCoach cannot express model size, so latency ceiling, minimum context and minimum scores are the whole distinction, and that is the decision. (3) `tools.agent.remote_cheap` and (4) `tools.agent.remote_frontier` ship **now**, with `allow_remote_providers = true`, `min_context_tokens` 128000 / 200000 (the tier budgets), latency ceilings 120 / 300 s, `min_capability_scores` `{ tool_use = 0.4 }` / `{ tool_use = 0.5, reasoning = 0.5 }`, `max_output_tokens` 4096 / 8192; until LC-E1 registers a remote provider they route to `NO_ELIGIBLE_MODEL`, which PromptCadence reports as `TIER_UNAVAILABLE` and `tiers check` shows — visible, not silent. Whether a remote profile must *require* a remote provider is LC-E1's question (D-11), not E4's. (5) `tools.plan`: weights `reasoning 0.4, structured_output 0.3, instruction_following 0.2, reliability 0.1`; constraints `min_context_tokens = 16384`, `requires_capabilities = ["structured_output"]`, `max_latency_p95_seconds = 180`, `min_capability_scores = { structured_output = 0.4 }`, `allow_remote_providers = false`; execution `temperature = 0.1`, `max_output_tokens = 4096`, `response_format = "json"`, `max_attempts = 3`, `fallback_depth = 1`; validation `require_valid_json = true`, `max_output_chars = 50000`, and **no schema** — the plan shape stays PromptCadence-internal (D-7) and G1 validates the document; G1 may reopen this if it wants LoadCoach's corrective retry against a schema. Every "fifteen" pin in LoadCoach moves to twenty: `tests/unit/test_task_profile_validation.py::test_all_fifteen_shipped_profiles_load_and_validate`, `tests/unit/test_cli.py` (two asserts), `tests/e2e/test_models_and_task_profiles.py`, `docs/quickstart.md`, `apps/loadcoach/routing.md` (the profile list), `apps/loadcoach/development-plan.md` Phase 2 AC1 — workspace `docs/` first, mirrored byte-identically. **Then, in PromptCadence:** `config.py`'s defaults are untouched; vendor LoadCoach's `task_profiles.toml` as `tests/contract/loadcoach_task_profiles.toml` with its SHA-256 and source commit recorded the way I10 records `loadcoach_openapi.json`, and add a contract test asserting that every default tier's profile exists there, that the agent profiles require `tool_use`, that each profile's `min_context_tokens` equals its tier's `context_budget_tokens` and its `allow_remote_providers` equals the tier's `remote`, and that `tools.plan` exists with `require_valid_json` and no schema — so a drift fails in CI, not at an operator's `tiers check`. The fake LoadCoach **stays an empty registry** (stricter than LoadCoach, by decision); add `shipped_profiles()` to `tests/fakes/loadcoach_app.py`, reading the vendored TOML so tests register the real profile shapes (`text_profile`/`schema_profile` remain for hand-shaped cases), and move the e2e, recovery and live-adjacent fixtures onto them. Amend spec §12's sentence "documented as TOML in PromptCadence's operator guide" to say the profiles ship in LoadCoach's `task_profiles.toml`, and drop the `PROMPTCADENCE_TIERS__*` overrides from `tests/live/test_loadcoach_journey.py`'s docstring. **Exit demonstration for this item:** start LoadCoach at E4's own commit on `provider.kind = fake`, then run `pytest -m live` and `promptcadence run "…" --bypass-planning --follow` with **no** tier overrides — both complete; a run against Ollama is the ops-checklist item below, because the fake provider declares `stop` for every answer. **Read:** `apps/promptcadence/development-plan.md` Phase 4, `apps/promptcadence/lifecycle.md` §5, ADR-0047 §1–2, `apps/loadcoach/routing.md` §§2–4 (profile grammar and hard constraints), `docs/history/D2_HANDOFF.2.md` §4  **Done 2026-09-04** (`docs/history/E4_HANDOFF.md`; docs `da45959`+`0e0d28c`, LoadCoach `5c5aa1f`, PromptCadence `b4ac1a2`+`c22ddef`; unpushed, review owed). **Three things this row's text assumed that were not true, corrected here so F1 does not repeat them:** (1) `promptcadence tiers check` does not exist and was **not** built — it is Phase 7's in `apps/promptcadence/development-plan.md` ("Task-profile checks in `doctor` and `promptcadence tiers check`"), so this row's reference to it was forward-looking; `doctor` and `promptcadence tools list` are what an operator has, and `doctor` gained a third `tools` component carrying ToolYard's `TierReport.reason`. (2) The **two remote tiers are not shipped active** — `config._default_tiers` ships only `local_fast` and `local_large`, with the remote pair documented and commented out in `EXAMPLE_CONFIG_TOML`, because a remote tier with an empty `pricing_file` is refused at startup and would break spec §20 AC1's zero-configuration boot (B4's decision, now stated in spec §12 rather than only in a docstring). A remote tier configured the way that comment instructs halts with `TIER_UNAVAILABLE` / `loadcoach_has_no_remote_provider` — caught by `TierPolicy.availability` **before** any call, so LoadCoach's `NO_ELIGIBLE_MODEL` is never reached. (3) The exit demonstration is **GPU-dependent**, see row E6. | ToolYard did the hard part. Opus reviews the workspace-lifecycle diff |
| **E5** | **MirrorWall 0.2.2** → PyPI, then the `setspec` pin sweep | Sonnet 5 · standard | E4 (flexible — but **before F2 and H4**) | **The widen itself was already built and committed** on MirrorWall `main` before this row ran (2026-09-04, `f8dd81a` + `092af6e`; since pushed and tagged `v0.2.2`): `setspec>=0.4,<0.7`, `ci.lock` recompiled, full gate green in a lock-installed venv on Python 3.13.15 (311 passed, 1 skipped). What was left was the release and the sweep, and both are now done. **Publish** `mirrorwall 0.2.2` (tag, `pypi` approval, install check — the operator's steps in §4), then move `setspec>=0.4,<0.5` → `>=0.4,<0.7` in **PromptCadence**, **IdeaPress** and **FreeWeight**, regenerating each `requirements/ci.lock` the same way — `pip-compile … -P setspec -P baseaicore`, the second because `setspec` 0.6.0 requires `baseaicore>=0.4.1` and without it the resolver **silently falls back to `setspec` 0.4.0 rather than failing** — and delete the now-stale "this pin cannot move yet" comment in `PromptCadence/pyproject.toml`. **LoadCoach is the exception and does not move here.** At `setspec` 0.6.0 its `tests/contract/test_evidence_import.py` enumerates the new `capability.evidence` `1.1` goldens and validates each stored record against the bare `CapabilityEvidenceOut` — which means `1.0`, `extra="forbid"`, per [ADR-0068](../adr/0068-a-post-freeze-minor-is-a-sibling-class.md) rule 3 — so three tests fail: locally today (verified 2026-09-04: 3 failed, 855 passed), and in CI the moment the pin moves. Adopting `CapabilityEvidenceV1_1Out`/`In` is **H4**'s import change, so **LoadCoach's pin moves at H4, with that adoption, not here.** FreeWeight's `tests/contract/test_evidence_schema.py` was checked against 0.6.0 and passes. **Read:** `py/MirrorWall/CHANGELOG.md` 0.2.2 and its `requirements/README.md` §"Why `setspec` moved to 0.6.0 at 0.2.2", ADR-0068 rule 3, `roadmap/adapter-roadmap.md` §4.3 **Done 2026-09-04** (`docs/history/E5_HANDOFF.md` for the release half and `docs/history/F2_HANDOFF.md` §"E5 resumed" for the sweep; MirrorWall `f8dd81a`+`092af6e` and tag `v0.2.2`, PromptCadence `1c1eb33`, IdeaPress `e2138fd`, FreeWeight `2b93132`, docs this commit). Ran in two sittings: the release half stopped correctly at Gate A5 because `mirrorwall 0.2.2` was still parked on the `pypi` approval, and the sweep resumed once the operator approved it. **Five things this row's text said that turned out not to be true, or that it did not say:** (1) **PromptCadence's floor is `>=0.5`, not `>=0.4`** — the row said `>=0.4,<0.7` uniformly, but `apps/promptcadence/spec.md` §5 requires `governance.egress_decision`, which `setspec` 0.5 introduced, and a floor below a component's own stated spec is drift that stays invisible until someone installs the wrong thing. IdeaPress and FreeWeight keep the 0.4 floor, which is correct for them: `setspec.prompts` plus `GeneratorInfo` is their whole surface and it has not moved. (2) **A widened pin does not move an existing venv, and looks exactly like success when it does not.** PromptCadence's editable reinstall jumped to `setspec` 0.6.0 on its own, but IdeaPress's did not — because a floor of `>=0.4` is still satisfied by the installed 0.4.0, so pip had no reason to act. **The 0.5 floor self-enforces; a 0.4 floor does not.** Where the floor did not move, the three packages were upgraded by name and the result confirmed with `pip show` before the gate was believed. (3) **FreeWeight's `ci.lock` needs three `-P` flags, not two**, and the reason is worth keeping: `mirrorwall 0.2.0` in that lock *itself* required `setspec<0.5`, so `-P setspec -P baseaicore` alone holds the lock at 0.4.0 while reporting success. (4) **`pip-compile` 7.6.1 no longer accepts `--no-index` as the alias for `--no-emit-index-url`** — it now means "ignore the package index", which fails build isolation with *No matching distribution found for hatchling*. Every lock header in the suite still records the old spelling; the flag that reproduces those files today is `--no-emit-index-url`. (5) **A repository's own venv can be the wrong oracle.** FreeWeight's carries *editable* installs of the `setspec` and `baseaicore` workspace checkouts, whose `dist-info` still reads 0.4.0, so its green gate proves nothing about what a consumer resolves; the proof is a clean venv installed `--require-hashes` from the recompiled lock (2508 passed, 28 skipped, Python 3.14.4). **LoadCoach was not touched**, as required — its pin rides H4 with the `CapabilityEvidenceV1_1Out`/`In` adoption, and moving it here would turn three local-only reds into CI reds. | Mechanical, and proven once already: a pin, a lock recompile and a gate per repository. The judgment was the ordering and the LoadCoach exception, and both are written down here |
| **E6** | **LoadCoach: the fake provider must not be gated on real VRAM** | Sonnet 5 · standard | E4 (**before F1**) | Found at E4, 2026-09-04, and it cost one exit-demonstration run. LoadCoach's **fake** provider declares an 8.5 GB model (`weights_bytes` 8 967 000 000, `parameter_count` 8.03 B, `size_bytes` 8 540 000 000), so routing's `insufficient_vram` hard constraint rejects the **only** candidate whenever the host GPU is busy — with an Ollama model resident the estimate was 11.4 GB against 1.2 GB free, and **every** tier reported `NO_ELIGIBLE_MODEL`, including unchanged `tools.agent`. A provider that exists so the suite can be exercised without hardware is currently gated on hardware, and the failure reads as a routing verdict rather than as "your GPU is full". F1's budget demonstrations run the same fake-provider journeys and will hit the same wall, which is why this lands first. **Two requirements, and the second is the point:** (1) the **default** declared size must be small enough that the fake provider never trips the VRAM constraint on any machine, so a fake-provider journey is reproducible rather than GPU-dependent; (2) the declared size must be **configurable**, so an operator can deliberately provoke `insufficient_vram` and watch the rejection path — the rejection detail (`estimated_bytes`, `free_bytes_by_gpu`, `headroom_bytes`, the whole `estimate` block) is one of the more valuable diagnostics LoadCoach produces and there is currently **no way to exercise it on purpose**. Requirement 2 is not optional and not a nice-to-have: without it, fixing (1) removes the only way anyone has ever seen this path fire. Prefer a fake-provider configuration key over exempting the provider kind from the constraint — an exemption would make the fake stop modelling the constraint at all, and a suite that cannot reproduce a real rejection is the thing this row exists to fix. **Read:** `docs/history/E4_HANDOFF.md` §5 (the diagnosis and the numbers), `apps/loadcoach/routing.md` §4 (the constraint order and `insufficient_vram`'s inputs) **Done 2026-09-04** (`docs/history/E6_HANDOFF.md`; LoadCoach `dfbf2d8`, docs commit below). **Two things the row's own text got wrong, corrected in the kickoff before this ran and folded in here so the next reader inherits them:** (1) **the numbers are ModelRack's, not LoadCoach's** — `DEFAULT_MODEL` in `py/ModelRack/src/modelrack/providers/_fake_script.py` declares the 8.5 GB model; LoadCoach's factory only accepted it by zero-argument construction, and the fix stays in LoadCoach's own composition root (`infrastructure/providers/factory.py`) rather than touching ModelRack's published contract. (2) **`size_bytes` alone cannot provoke the rejection** — the KV term (`2 × layers × kv_heads × head_dim × 2 bytes` for the assumed f16 precision, times the served context) dominates it at any interesting context length; `served_context` defaults to the model's own `max_context` (32 768) whenever a task profile leaves context unconfigured, so the KV term alone is already tens of megabytes even for a tiny model. `[provider.fake]` therefore requires `size_bytes`, `layers`, `kv_heads` and `head_dim` together, refusing a partial set with a named `ConfigurationError`, so an operator cannot set one field and wrongly conclude the diagnostic is unreachable. ModelRack untouched; the fake provider kind is not exempted from `insufficient_vram`. | One provider's declared numbers plus one configuration key. The judgment — that the *rejection* must stay reachable on purpose — is written down here |
| **F1** | PromptCadence P5 | **Opus 5 · high** | C3 + C6 + E4 | Budget wiring: LoadLedger mounted, crash-reconciliation of debits idempotent by `source_ref` (the Opus half); estimator with labeled sources, ceilings (per-trajectory, per-day with the `awaiting_window` park, per-project), config, CLI (the Sonnet half). **Read:** `apps/promptcadence/development-plan.md` Phase 5, `apps/promptcadence/lifecycle.md` §6, ADR-0030, accepted D-3 **Done 2026-09-04** (`docs/history/F1_HANDOFF.md`; PromptCadence `f84ddf2`+`f6fdf38`+`0dc15b1`+`399803a`+`bd60bc3`+`c7fda2d`, docs this commit). **Run single-session at Opus 5 · high** — the split was not taken, because the estimator and the surfaces turned out to depend on decisions the reconciliation half made (see below), and handing them over would have meant re-deriving those. That is the evidence behind [model-assignment §3.5](model-assignment.md): as of 2026-09-04 a phase gets one model, the stronger of any pair, and this row's Model column has been rewritten accordingly rather than kept as a split with a deviation beside it. **Six things this row's text or the kickoff's §0 assumed that were not true, recorded so F2 does not repeat them:** (1) There is **no** `daily_token_ceiling` gap to close. The kickoff told the session to look for one; spec §11.5 and lifecycle §6 make the per-day ceiling money-only *by design* — local work is unpriced and never counts against it, which is what lets any amount of work run while only so much is spent — so adding one would have contradicted the spec. The real config gap was the estimator's `configured_default` rung, which lifecycle §6 names and `[tiers.<name>]` had no field for; added as `default_step_input_tokens`/`default_step_output_tokens`, two numbers rather than one total because the classes price differently. (2) **PromptCadence is the suite's first consumer of `baseaicore.ModelPricing`** — nothing anywhere defines a pricing-file format, so P5 defined one (spec §12). (3) **A remote tier cannot run in this build**, so no test can reach a money ceiling through the loop by the intended route: `TierPolicy.availability` reports `loadcoach_has_no_remote_provider` for every remote tier until LC-E1, hardcoded in `LoopController._tier_policy`. The money, floor/strict and window tests price the *local* tier through a hand-built `PricingCatalog` — one visible fiction, with `test_from_settings_never_prices_a_local_tier` holding the shipped rule (ADR-0016). (4) **Money ceilings bind priced usage only**, which ADR-0047 §3 decides and this row had to implement: LoadLedger reports every ceiling's verdict and the *application* decides which refuses a step, so a money cap someone's priced work exhausted must not stop a local step. Without this a shared per-day cap would halt every local trajectory in the installation. (5) T16's guard is "the ceiling now **admits the next step**", not "is the ceiling exceeded" — the cheaper question wakes a parked trajectory every day edge only for its pre-flight to park it again. (6) `trajectories.budget_money_nanos` was `Integer` and held whole nanos: the shipped $5.00 default is 5 000 000 000, past a 4-byte integer, so every trajectory with a cap over $2.14 would have failed to insert on PostgreSQL while SQLite stored it silently. Widened in migration 0005. **One LoadLedger row this found (§0.4):** LoadLedger reports a balance only *through a ceiling and for a named run*, so a view of a scope that has no cap — per-tier spend, which spec §7.2's `ledger show --scope tier` asks for — cannot be answered without either summing entries in the application (ledger arithmetic where ADR-0050 forbids it) or inventing an unreachable ceiling to read a number through. `--scope tier` therefore reports debit **counts** and says why; a `balances(scope, window_key)` read is the LoadLedger row. The same limitation makes `GET /ledger` name an arbitrary known run as its reference, which is sound because `per_day` and `per_tag` windows are ledger-wide, but it is a signature working around a missing read. **What F2 inherits and must not relitigate:** the mount shape (`LEDGER_TABLES = mount_ledger_tables(Base.metadata)` at module import in `infrastructure/db/models.py`, migration autogenerated against it, a test proving upgrade-from-empty equals the mounted shapes and downgrade is clean — copy it for `egress_decisions`); the debit-before-turn-row ordering and `reconcile_debits` idempotent by `source_ref`; and `render_money`/`render_tokens` as the one place a floor or an unpriced amount is written down. | Reconciliation is money-adjacent crash logic; the rest is plumbing against spec'd maths |
| **F2** | PromptCadence P6 | **Opus 5 · high** | E3 + F1 | Egress evaluation via Commissioner, verification, category-typed deviations, the violation→halt path, re-approval feed. **Read:** `apps/promptcadence/development-plan.md` Phase 6, `apps/promptcadence/lifecycle.md` §5, `packages/commissioner/spec.md`, accepted D-4/D-12 **Done 2026-09-04** (`docs/history/F2_HANDOFF.md`; PromptCadence `f93e912`+`f7e66ea`+`7ce31ad`+`bb601d3`+this gate's commit, docs this commit). **Four things this row's text or the kickoff assumed that were not true, recorded so G1 does not repeat them:** (1) **The pre-flight order is the guarantee, and egress had to move ahead of tier availability.** A remote tier reports `loadcoach_has_no_remote_provider` until LC-E1, so a `confidential` trajectory aimed at one halted on `TIER_UNAVAILABLE` before any egress evaluation ran and spec §20 #4's "the refusal is a queryable `EgressDecision`" was unreachable. `TierRouter.resolve` splits into `tier_of` and `ensure_available`: a tier's egress class is a property of its configuration and its availability is a property of the deployment, and deciding governance on the second would mean the recorded reason changes the day a provider is registered. The four pre-flights now run egress → pricing → availability → budget, all before `turn.started`. Recorded as [ADR-0073](../adr/0073-egress-is-decided-on-configuration-before-availability.md) by operator decision after the build, because a build that reordered the checks would still pass every unit test of the policy itself. (2) **Spec §20 #5 cannot be about the `pricing_file` field.** Startup validation already refuses a remote tier naming no file (B4's decision), so a field check is a dead branch; contract 5 is about the `ModelPricing` **record**, and what reaches the pre-flight is an expired or empty price list. Both clarifications are now in spec §11 contracts 4 and 5 and §20. (3) **The deviation machinery was already built.** `domain/deviation.py` has carried the closed six-category taxonomy, both severities, the disposition matrix and `TurnFacts` since P2, and P4 wired `compare` end to end into the loop. Gate D was therefore a *golden over the bypass-minted intent*, not a build — and that golden is the baseline G1's contract-1 invariance diff is written against. Nothing was added to `TurnFacts`. (4) **Spec §7.2's `promptcadence egress list [--denied-only]` cannot express the whole vocabulary**: a violation is neither an approval nor a denial, so `--verdict` ships beside the documented flag and the spec now names both. **Also recorded:** `commissioner 0.1.0` pins `setspec>=0.5,<0.6`, so adding it pulled PromptCadence's resolved `setspec` from 0.6.0 back to 0.5.0 — expected, costing nothing today, and needing to be lifted before PromptCadence can adopt any 0.6 payload (§3's ordering note). | Fail-closed semantics and the violation path — adversarial-adjacent |
| **F3** | ModelRack P7 | **Opus 5 · xhigh** | D3 (flexible — model adjacency) | Launch-time adapter registration, per-request selection, `adapter_hot_swap`, `AdapterNotFound`, digest-verified compatibility, `pending_restart`, **the cache-correctness conformance test** (prefix under A never reused for B, I17). **Read:** `roadmap/adapter-roadmap.md` §4.1 P7 + §7 I17, accepted A-1/A-5/A-6 **Done 2026-09-04** (`docs/history/F3_HANDOFF.md`; ModelRack `51bc009`+`52565b4`+`5e8aa0e`+`6c686f5`, docs `986ab25`+`9196580`). **Not pushed** — per the standing instruction, both repos are committed and left for the operator to push. **The row had no plan section**, which §0.1 of its kickoff made Gate 0: `packages/modelrack/development-plan.md` ended at Phase 6, so P7's acceptance criteria had nothing demonstrable behind them. Phases 7 **and** 8 are now written in the house shape (`986ab25`), so H1 inherits a plan rather than repeating this. **The boundary question is settled and recorded**: ModelRack defines its own frozen `AdapterRegistration` and the **application** converts `setspec.artifacts.AdapterManifestOut` into it. ADR-0061 rule 3 already said ModelRack never reads the directory; master architecture §2 permits only `mirrorwall` and `commissioner` to import `setspec`. The runtime dependency set is still exactly `baseaicore` + `httpx` and `.importlinter` was not touched — only the `baseaicore` *floor* moved, to `>=0.4.1`, which is where `AdapterIdentity` lives. **Five things this row's text or its kickoff assumed that were not true, recorded so H1 does not repeat them:** (1) **llama-server does not zero unnamed adapters when a request carries no `lora` field.** `launch_slot_with_task` takes an `else` branch that restores `params_base.lora_adapters` — at the launch scales, which `--lora` sets to `1.0` and `--lora-init-without-apply` does **not** change — and it takes that branch *without consulting* `lora_should_clear_cache`. A bare-base request to an adapter-registered server would therefore have run with **every** adapter applied, against the previous request's cached prefix: I17's own defect, in the shipped server. Every request now states the complete configuration. This is the single most valuable thing the row found, and it was found by reading the server's source rather than by a test. (2) **The `Provider` protocol had to grow after all.** ADR-0062 decision 1 celebrates that it does not change; that claim holds for the *load/unload* seam and is untouched, but `list_adapters()` and `register_adapters()` were added, because otherwise LoadCoach must `isinstance`-check `LlamaCppProvider` to show an adapter row or fold in a rescan — importing a concrete adapter is exactly what the protocol exists to prevent. Recorded here rather than quietly done; **if the architect prefers it otherwise, that is a small revert plus a downcast in LA2.** (3) The venv is **Python 3.13.15**, not 3.14.4 as §0 stated. (4) `baseaicore` **0.4.1 is on PyPI** but the venv held 0.4.0, which the `>=0.4,<0.5` pin permitted; the code would have imported and then failed at the first adapter. The floor is now `>=0.4.1`. (5) **No LLM adapter GGUF exists on this machine** — the only LoRAs here are Wan 2.2 video diffusion safetensors — so I17's semantic canary is written and **skips visibly**, naming exactly what an operator must produce. That is an H1 blocker for the LA1 exit, not a P7 gap. **Also decided, and open to reversal:** an entry at scale `0.0` is a *disable*, not a second adapter, so the complete `lora` list does not violate ADR-0063 rule 1's "at most one entry" — read as governing *enabled* entries. And a restart required for **correctness** (a profile change) now refuses with `ProviderUnavailable(restart_pending)` while work is in flight rather than killing a server mid-stream, which is a behaviour change to Phase 6's immediate restart. | The prefix-reuse property is exactly the intermittent-defect shape; the conformance-test design is the judgment |
| **F4** | LoadLedger P3 → **0.2.0** on PyPI | Sonnet 5 · high | F1 | **A balance read that names no run.** LoadLedger 0.1.0 reports a balance only *through a ceiling and for a named `run_id`* — `remaining(run_id)` and `would_exceed(run_id, …)` are the whole of it — so a scope that has no cap has no balance anyone can ask for. F1 met that twice (`docs/history/F1_HANDOFF.md` §7): `promptcadence ledger show --scope tier` reports debit **counts** instead of spend, because summing `entries()` in the application would be ledger arithmetic exactly where ADR-0050 forbids it and inventing an unreachable ceiling to read a number through would put a magic figure in the record; and `GET /ledger` names an arbitrary known trajectory as its reference run, which is *sound* — a `per_day` or `per_tag` window is ledger-wide, so the answer is identical whichever known run is named — but is a signature working around a missing read, and it degrades to "the configured caps with nothing spent" on an empty ledger. **Add** `balances(*, scope, window_key)` (naming, and whether it returns a small value object or a `CeilingVerdict`-shaped one with no ceiling on it, are the row's to decide) returning the accumulated tokens, the per-currency money and the three honesty counts for one window, with **no** `run_id` and **no** ceiling. **It needs no new storage**: `{prefix}balances` is already keyed `(scope, window_key)` and `{prefix}balance_money` `(scope, window_key, currency)`, which is exactly this query. Implement on `InMemoryLedger` and `SqlLedger` through the one `BalanceBook`, as every other method is, so the deterministic double keeps answering what production answers. Keep the honesty counts on it — a balance returned without `unpriced_debit_count` is a floor presenting itself as a total, which is the one thing spec contract 2 forbids. Add the tag-listing this implies if it is cheap (`window_keys(scope)`), so a caller can ask "which tiers have spent anything" without reading every entry; leave it out if it is not. **Then, in PromptCadence** (same row, second commit): drop `TrajectoryService.most_recent_id()`'s use as a ledger reference, make `--scope tier` and `GET /ledger`'s `tiers` array report real spend rendered through the existing `render_money`/`render_remaining_money`, and amend `apps/promptcadence/spec.md` §12 and `lifecycle.md` §6 where they currently say a tier has a history and not a balance. **Not in this row:** a tier *ceiling* — lifecycle §6 is deliberate that none is configured, and this row adds a **view**, not a cap. **Read:** `packages/loadledger/spec.md` §7 and §10, `py/LoadLedger/src/loadledger/core.py` (`BalanceBook.verdicts`/`_verdict_for` — the counts come from there), `py/LoadLedger/src/loadledger/sql.py` (`_seed_from_rows`), ADR-0050, `docs/history/F1_HANDOFF.md` §7 **Done 2026-09-04** (`docs/history/F4_HANDOFF.md`; LoadLedger `9e1d15a`+`e579f15` — **pushed and tagged `v0.2.0` by the operator, `loadledger 0.2.0` published**; PromptCadence `95b7719`+`de8d918`; docs `478973b`+`bb07ea4`+this commit). PromptCadence's pair and the docs commits are **not pushed** — the standing instruction. Locks needed no recompile and that was proven, not assumed (`git diff v0.1.0 -- pyproject.toml requirements/` is empty); B4's install check is green from PyPI in both shapes (pure core with only `baseaicore`, `[sql]` resolving SQLAlchemy 2.0.52). **The row took §0.2's route (a):** `balances(*, scope, window_key) -> WindowBalance` **and** `position()`, because `balances()` alone fixes only `--scope tier` — `--scope day` and `--scope project` need headroom against a configured ceiling, and deriving that outside the package would put the floor rule and the `exceeded` decision in a consumer. So the row's "drop `most_recent_id()`" claim stands, and it is `position()` that delivers it. A `PER_RUN` ceiling passed to a run-free read is **refused** with `InvalidCeiling` rather than omitted, because omitting silently shortens a tuple whose positional correspondence with `ceilings` is documented API. `window_keys(scope)` was **not** shipped — no consumer asks. **This row's text is wrong in one place:** a tier has no ceiling and therefore no *remaining*, so the tier half renders through `render_money` / `render_tokens` (the "at least" pair) and **never** `render_remaining_money`, which qualifies in the opposite direction. **§3's soft edge is satisfied:** `--scope tier` and `GET /ledger`'s `tiers` array report spend with the three honesty counts and the rendered strings, so the `"1 debit(s) recorded"` column G1 was warned about no longer exists — the verbatim output is in the handoff §12. **One thing to know for the next release:** for several minutes after twine reported `200 OK`, `pip index versions` *and* PyPI's own JSON API still listed only 0.1.0. The index was stale, not the publish broken — read the release run's log before concluding otherwise. | Additive, against a spec that already describes the storage; the shape is decided and the arithmetic already exists |
| **G1** | **PromptCadence P7** → **0.9.0b0 — M11 beta** | **Fable 5 · xhigh — daytime, reviewed** | F2 | Planner, approval modes (`auto`/`hybrid`/`manual`), intent minting in both paths, DAG ready-set dispatch, **the governance-invariance diff (contract 1, I11)**, plan-schema resilience against real local models. **Read:** `apps/promptcadence/development-plan.md` Phase 7, `apps/promptcadence/lifecycle.md` §4 + §8.4, accepted D-3/D-4/D-5/D-12, `roadmap/promptcadence-roadmap.md` §9 I11 **Done 2026-09-04** (`docs/history/G1_HANDOFF.md`; PromptCadence `8e3b2d7`…`289a38e` + the release commit, docs `463ea9c`+`18bcd73`+this commit; ran on Fable 5.1, whole, daytime). **`0.9.0b0` cut by operator decision at the interview:** M11's exit demonstration held — a bypassed and a *tool-free* planned trajectory completed on real LoadCoach + Ollama (gpt-oss:20b) with the shipped defaults and no `PROMPTCADENCE_TIERS__*` overrides, records identical minus the plan rows by the contract-1 diff, a `confidential` trajectory refused before any request left the host — while spec §20 #2's *sandboxed tools* clause did not, for a reason outside the row (G2). **Four things this row's text or the kickoff assumed that were not true:** (1) **`tools.plan` is shipped, but a 4.4k-character prompt made gpt-oss:20b return an empty document every time** — a reasoning model under JSON mode spends `max_output_tokens` thinking (the `native.plan` finding, written up in the handoff §9.1); `planner.draft` 1.1.0 no longer shows the schema block, `PLAN_SCHEMA` untouched; the profile's output and thinking levers are LoadCoach's (G2 item 4). (2) **The real model does not exercise `length` or `content_filter`; it exercises `tool_calls` with invented names**, because LoadCoach's wire carries no tool definitions and no `tool_calls` (G2). (3) **There was no authentication to hang the `approve` scope on** — tokens, scopes and a loopback-open principal (`approver:loopback`, LoadCoach's precedent) were built as the minimum, spec §14. (4) **A remote step cannot execute before LC-E1** (approval rejects it as unavailable), so the planned path's hybrid egress gate is proven with an injected `loadcoach_has_remote_provider = True` seam — default `False`, nothing infers it from a provider kind (contract 4); the parallel local+remote pair is proven at the rule and, at the loop, for two local steps under a raised cap. **Decisions taken, not to relitigate:** the bypass gate's grant **supersedes** (revision 2, `minted_by = approver:<token>`, revision 1 retained); `[planning] corrective_retries` is configuration (default 2); the planner's spend is on the `plans` row and not the ledger (it is not a turn and runs under no intent); the contract-1 allowance list is closed and includes the intent **slice** fields (`token_budget`, `money_budget`, `max_turns` — the bypass default's slice is the trajectory ceiling by ADR-0056 §2, a planned step's is estimate × 2); `turns.tool_calls_json` persists an assistant turn's requested calls so a park between the turn and its calls (§20 #8) resumes them. **LoadCoach defects found, reported, not fixed here** (G2): `corrective_turns` appends an empty assistant turn → ModelRack refuses it → `VALIDATION_ERROR`, the job stuck `executing`, its attempts never written. | Design-dense, live-model interaction, and the arc's central proof — a failed pass invalidates the design claim, not just the code |
| **G2** | **LoadCoach: the tool wire — `/generate` carries tool definitions and `tool_calls`, and the corrective retry survives an empty answer** → next LoadCoach minor (rides 1.1 at H2 if H2 lands first) | **Opus 5 · high** | G1 (**before I2**; H2 inherits the shape) | Found at G1, 2026-09-04 (`docs/history/G1_HANDOFF.md` §9, and the G1 row's "not true" list): `GenerateBody` has no `tools` and `MessageBody` has no `tool_calls`, so a model is **never told which tools exist** — gpt-oss:20b invents `repo_browser.list_dir` and `container.exec` from its own vocabulary, every call is refused and recorded by ToolYard, and Ollama's own tool-call parser then rejects the next answer (`ProviderRejected: error parsing tool call`) → `ALL_CANDIDATES_FAILED`. Model-directed sandboxed tool use — spec §20 #2's clause — is unreachable on the real stack until this lands; the beta shipped saying so. **Four items:** (1) `tools` on `GenerateBody` in ModelRack's tool-spec shape, passed through routing to the provider — a candidate without `tool_use` is a routing rejection, never a silent drop; (2) `tool_calls` on `MessageBody` and the `tool` role with `tool_call_id`, so a transcript with tool turns replays natively — and then, **as this row's last commit, in PromptCadence**, drop the `[tool_calls]` text rendering `289a38e` added (`services/loop.py::_transcript`) and replay the persisted `tool_calls_json` on the wire; (3) `services/execution.py::corrective_turns` never appends an empty assistant turn, and a corrective whose request ModelRack refuses **fails the job with its attempts written** instead of leaving it `executing` until a cancel — today the empty tries' attempts are lost, so their `finish_reason` is unrecoverable; (4) decide `tools.plan`'s `max_output_tokens` and whether a profile can ask for reduced thinking (if the Ollama provider exposes `think`) — the empty-document rate was ~50 % at a 1.6k-character prompt and 100 % at 4.4k. `apps/loadcoach/api.md` and `data-model.md` amended first; additive within `/api/v1`; PromptCadence's fake LoadCoach gains the same fields so the hostile-model journeys can script a **declared** call. **Exit:** a planned PromptCadence trajectory (`summarize the files in ./notes`) on real LoadCoach + Ollama completes with at least one declared sandboxed call in `tool_call_records`, and the `[tool_calls]` rendering is gone. **Read:** `docs/history/G1_HANDOFF.md` §9–§10, `apps/loadcoach/api.md`, ModelRack `Message`/`ToolCall`, `apps/promptcadence/spec.md` §9 + §20 #2, `apps/promptcadence/lifecycle.md` §4.3  **Done 2026-09-05** (`G2_HANDOFF.md`; docs `3d824b4`+this commit, LoadCoach `dd2112a`…`5af6b12`, PromptCadence `f85c236`+`bce7edc`; ran on Opus 5 · high, whole, daytime, no deviation). **Exit conditions all met.** The demonstration completed on real LoadCoach + Ollama with gpt-oss:20b and the shipped defaults: the planned trajectory `01M1R81X0MJ74EJ861496X9PHG` made three declared sandboxed `run_command` calls under the container isolation tier, every one `ok`, and answered; the bypassed pair made three more (`list_dir`, `read_file`×2), all `ok`; six calls, **zero deviations**, eight egress decisions all `target_not_remote`. Spec §20 #2's *sandboxed tools* clause, which the M11 beta shipped unmet, now holds. **The four decisions:** (1) ADR-0075 — a non-empty `tools` imposes `tool_use` as a hard constraint on that request, unioned with the profile's, rejecting as the existing `capability_unsupported` with a new `required_by` (`request`|`task_profile`) detail; `tools: []` ≡ `tools: null` ≡ absent; the provider-edge `CapabilityUnsupported` is now unreachable through `/generate` and a test asserts it. (2) The wire checks a transcript strictly: ModelRack's three refusals are **surfaced** (not duplicated) as `VALIDATION_ERROR` with `details.fields[{path,problem}]`, plus LoadCoach's own fourth rule — a `tool_call_id` must name a call in an earlier assistant turn — which forced PromptCadence to replay the **model's** call id instead of its own invocation ULID (a latent defect, since a provider matches results to calls by the model's id). `raw_arguments` rides `arguments` as `object|string`. (3) A construction-time refusal is `failed` + `VALIDATION_ERROR` with **its attempts written** — neither a provider nor a routing failure; no new attempt outcome was needed. (4) `tools.plan` is **unchanged, measured**: 1/6 empty at 4096 (median 58 s) vs 3/6 at 8192 (median 171 s), every empty answer `done_reason=length` with `eval_count` equal to the budget — raising the budget triples the wait and doubles the failures, so the numbers are recorded beside the value in `task_profiles.toml`. **This closes G1's open question: the finish reason behind an empty planning answer is `length`.** **Two findings.** (a) **ModelRack's Ollama adapter declares `thinking_control = True` and exposes no way to ask for it** — no `SamplingParameters.think`, no top-level `think` key, and `provider_options` merges into `options` where it does not live; the lever that would move the empty rate is therefore unavailable and **deserves a ModelRack row**, not a task-profile field for a control that cannot be sent. (b) **PromptCadence's `assemble_tool_calls` grouped fragments by `id`**, but ModelRack's Ollama adapter emits a call as two deltas — id+name first, argument text second **with no id** — so one call became two: a named call with empty arguments (`args_invalid`) and a nameless one with the arguments (`unknown_tool`). That pair is verbatim in G1 §10.4, where it was read as the model inventing names; part of it was this function. Now keyed on `call_index`. **A hostile-model journey against the fake could never have found it.** **What H2 inherits:** the request shape, the four rules and their error presentation, ADR-0075, and the write-the-attempts rule are settled contract; still open is the **response/request asymmetry** — `output.tool_calls` is still one entry per `ToolCallDelta` while a request takes assembled calls, so every caller must implement the grouping and the first one to try got it wrong; collapsing it is breaking and is H2's call. `Message.name` is still not on the wire. **Left for the operator:** push three repos; **no tag, no publish**. | A wire contract inside `/api/v1` that H2 then generalizes, plus the corrective's failure semantics — judgment on both, though the shapes already exist in ModelRack |
| **G3** | **PromptCadence: per-step retry policy** | **Opus 5 · high** | G1 (**before I1** — P8 composes step attempts) | Deferred at G1 and scheduled by operator decision on 2026-09-04: today a failed step halts the trajectory (**T12**, `executing → halted`, naming LoadCoach's code — this row originally said T7, which is `planning → failed`, the plan draft's corrective budget; corrected 2026-09-05) and the only retry in the loop is the tier escalation on a service failure within `fallback_tiers`. Decide in **one ADR**, then build: **what is retryable** (LoadCoach `ALL_CANDIDATES_FAILED` / `TIER_UNAVAILABLE` / timeouts — never a governance refusal, an egress denial or a deviation halt); **how many times** (`[execution] step_retries`, default small, the `corrective_retries` precedent — configuration, not a constant); and **whether a retry runs before or after tier escalation** — a retry on the same tier is a repeat, an escalation is a scoped re-approval, and the order changes what the record says happened. A retry is a new turn under the **same intent revision** (nothing widens, ADR-0056), on the step's thread, counted against `max_turns_per_step` and debited as any turn; recorded as `plan_steps.attempt` plus a `step.retried` event in the write that starts it (ADR-0044); the halt after the last attempt names the last cause and every attempt. **Both paths:** the bypassed loop's synthetic step retries the same way, or contract 1 fails — the diff must stay green with **no new allowance**. Lifecycle §4.3/§5/§8 and spec §13 amended first. **Read:** `apps/promptcadence/lifecycle.md` §4.3 + §5 + §8, `apps/promptcadence/spec.md` §13, `docs/history/G1_HANDOFF.md` §9 + §10.4, ADR-0044, ADR-0056 **Done 2026-09-05** (`cf75d7e`, `d0a1be4` in docs; `7267ef5`, `9aa4af4`, `422f6bd`, `0f6734f` in PromptCadence; handoff `G3_HANDOFF.md`). **[ADR-0076](../adr/0076-a-step-retry-is-a-repeat-under-the-same-intent.md)** takes all three decisions plus the record shape: *retryable* is drawn **structurally** — the repeat lives at the LoadCoach call site, so egress, pricing, availability, budget and every deviation are out of its reach rather than excluded from a list — and within reach, a code is retryable when the identical request could plausibly answer differently (an unknown code is not); *how many* is `[execution] step_retries`, default **1**, `ge=0`, **per step**; *order* is **a repeat before an escalation**, and a retryable failure stops the tier ladder rather than falling through it; *record* is **(a)** — `plan_steps.attempt` counts, `step.retried` events are the history, because the counter's table is plan-only and the events are the half both paths have. **What held:** contract 1 green with a **third scenario** (a retry on both paths) and **no new allowance**; attempts seeded from the events, so a crash cannot hand a step a fresh budget; attempts counted against `max_turns_per_step`, still distinguishable from `turn_overrun` by error code and halt text; the halt names the last cause and every attempt with its tier. **What did not:** `VALIDATION_ERROR` moved the *other* way after G2 — a cleanly-refused construction is deterministic and is now explicitly never repeated; no backoff and no `waiting` state were added, so an unreachable LoadCoach still fails at T13. **Live on the real stack** (LoadCoach 1.0.0 + Ollama): a real client timeout repeated and the budget spent, both jobs named in the halt — and the observation that a client-timeout repeat costs real provider work the ledger never sees, since the job completes after the read timeout. **Nothing shipped:** `## [Unreleased]`; `0.9.0b0`'s *Known limitations* line about the unbuilt retry comes out at the **next** release. | Lifecycle semantics that become persisted record shape — small in code, permanent in what an explanation says |
| **H1** | ModelRack P8 → publish (LA1 done) | **Opus 5 · high** | F3 | Cancellation under supervision + leak tests (20 cycles, no orphan, flat memory) — Opus; conformance suite green across all four providers, docs, publish — Sonnet. LA1 exit needs a GPU session: 20 alternating-adapter generations, zero base loads (I16). **Also, and before publication: [ADR-0074](../adr/0074-adapter-enabled-serving-is-a-runtime-profile-field.md).** F3 built adapter serving and found ADR-0060's "serving mode lives in the runtime profile" had no mechanism: a base on an adapter-registered server and the same base on a clean one hash to the same `profile_hash` and would merge. Decided by the operator on 2026-09-04 as a **real field**, not a `provider_options` convention. Two small pieces: **BaseAiCore** gains `RuntimeProfile.adapters_registered: bool | None = None` (additive inside `>=0.4,<0.5`; the hash already drops `None` fields, so every stored hash is unchanged — a `False` default would move all of them, which is why it is tri-state), and **ModelRack** refuses a request whose profile disagrees with the server it would use, the `context_configurable` discipline one level up. It belongs here rather than later because 0.7.0 publishing without it ships a known silent-merge gap, and because evidence recorded before the field exists is **not separable afterwards**. LoadCoach sets it at H2; FreeWeight's A/B consumes it at H4/B5. **Read:** `roadmap/adapter-roadmap.md` §4.1 P8 + §3 LA1 exit, ADR-0074, `docs/history/F3_HANDOFF.md` §7.8 | Leak/cancellation reasoning vs. publication mechanics — a clean split  **Also at H1, scheduled by the operator on 2026-09-05 from G2's gate-E finding: a request-side thinking control for the Ollama adapter.** `providers/ollama.py` declares `thinking_control = True` while nothing can ask for it — `SamplingParameters` has no `think` field, `_build_request` never emits Ollama's **top-level** `think` key, and `runtime_profile.provider_options` merges into `options`, where that key does not live. A capability declared and unreachable is ADR-0007 rule 2 from the request side. G2 measured what it costs: `tools.plan` on gpt-oss:20b returns an empty document 1/6 at `max_output_tokens = 4096` and 3/6 at 8192, every empty answer `done_reason=length` with `eval_count` equal to the budget, median latency 58 s → 171 s — so the output budget is **not** the lever and thinking control is. Shape: `SamplingParameters.think: bool | None = None` (tri-state, so an unset request is byte-identical to today's), sent as the top-level key by the Ollama adapter, and `CapabilityUnsupported` from every adapter declaring `thinking_control = False` (llama.cpp and openai-compatible both do). LoadCoach can then decide whether a task profile may ask for it — which is a **later** row, since a profile field for a control that cannot be sent would be configuration that lies. **Read:** `G2_HANDOFF.md` §4–§5. **Partly done 2026-09-05** (`docs/history/H1_HANDOFF.md`; BaseAiCore `0df1347`+`4492644`+`8c04208`, ModelRack `2b54bc2`+`16e7be0`+`a03aa88`+`b059989`+`8ecc1b4`+`d0d3b0a`, docs `3bc774f`+`8c37bc6`+`d1b16cb`+`146ec73`+`1cf27c9`+this commit; ran on Opus 5 · high, whole, daytime, no deviation). **Gates A–F and H's documentation are done; gate G and the publication are not, and the row stays open.** The kickoff's §0.1 made this a minute-one judgement on evidence: `find /home/jpk/ai/models -iname '*.gguf'` returns four bases and **no LLM LoRA adapter** (only Wan 2.2 video-diffusion safetensors, exactly as at F3), and `MODELRACK_LLAMACPP_ADAPTERS` is unset — so **LA1's exit (I16) and I17's semantic canary cannot be run**, and `modelrack 0.7.0` is not bumped, tagged or published: its central claim would be undemonstrated. `CHANGELOG.md` keeps everything under `## [Unreleased]` and `__about__.py` stays `0.6.0`. **`baseaicore 0.4.2` *is* prepared** (version bumped, changelog moved, release commit made, wheel built and verified in a clean venv) because it does not depend on LA1 and **H2 needs the field from PyPI** regardless — blocking it would block H2 for no reason. **What was built:** ADR-0074 in both halves — `RuntimeProfile.adapters_registered` (tri-state, additive, proved over four stored `0.4.1` hashes including `e06e92b4d4803b3b`, which is SetSpec's frozen `capability.evidence/1.0` payload's own key) and ModelRack's `ProfileMismatch` refusal, compared against what the server **was launched with** rather than what is registered now; cancellation under supervision and the twenty-cycle leak assertions; the conformance suite verified green over six bindings with every skip declared, and made enforceable; the sharded-GGUF decision; and the thinking control. **Three things this row's text or its kickoff assumed that were not true:** (1) **§0.3's fake-configuration decision was already made** — `FULL_CAPABILITIES` has declared `thinking_control = True` since Phase 2, so the capability's positive path had a non-live home without adding one. (2) **The `# pragma: no cover` at the cancellation post-loop check is reachable** — a token that flips on a chosen read reproduces the cross-thread window deterministically, so the pragma is gone and the branch is tested; its sibling (`if not completed`) stays, correctly. (3) **The tested `llama-server` cannot be pinned from `--version`** — the CUDA build reports `version: 0.3.0-dev (build 1, commit c5a5535)` because the tree was built without the git stamp; `git describe --tags` gives `b10792`, and spec §18 now pins the tag **and** the commit. **Left for the operator, in order:** push three repos; publish `baseaicore 0.4.2`; produce two LoRA adapter GGUFs for one base (`F3_HANDOFF.md` §6 says exactly what); run gate G; then bump, tag and publish `modelrack 0.7.0`. **Done 2026-09-05, second sitting** (`docs/history/H1_HANDOFF.2.md`; ModelRack `9bb3e98`+`2489872`+`bdc1331`+`4752186`, docs `672c4fe`+`30124c5`+this commit; ran on Fable 5.1, interactive, after an interview over both H handoffs). **Gate G passed live**: three LoRA adapters trained outside the suite on a fifth, small base (`Qwen2.5-1.5B-Instruct` Q8_0; recipe and probes under `~/ai/tools/lora-train/`, artefacts under `~/ai/models/adapters/llm/`), a new `TestWarmBase` I16 live test — twenty alternating generations, **one** load asserted from the pid, the `load` events and the timings, resident set settled at 778 MiB — and I17's canary answering one prompt three distinct ways; **8 live tests pass**. **What the exit found:** `llama-server` b10792's CUDA build **leaks ~14 MiB of host memory per adapter-set change while CUDA graphs are on** — unbounded over sixty switches, flat on CPU, flat for a fixed adapter, unaffected by `--cache-ram 0`, flat with `GGML_CUDA_DISABLE_GRAPHS=1` at the same speed, and absent from upstream `master` at `b10792-28`. ModelRack now passes that variable as an environment **default** (`LaunchSpec.env_defaults`, applied under the parent's environment so an operator's setting wins) on every launch that registers adapters, and nothing on a launch that does not; spec §18 records the evidence and when to drop it. **Also decided and built:** `register_adapters()` takes the **complete** set — a name absent from it is retired at the next idle; no inverse method (H1's §8 question). **`modelrack 0.7.0` is bumped, its changelog closed, its wheel verified in a clean venv with the demo, and the release commit made** — not tagged, not published, not pushed. **Two things to know before pushing:** ModelRack's `requirements/ci.lock` still pins `baseaicore==0.4.0` under a `>=0.4.2` floor and CI installs `--no-deps` over it, so ModelRack CI is red until `baseaicore 0.4.2` is published and the lock recompiled; and BaseAiCore's `origin` carries a stray `v0.5.0` tag on the 0.4.1 commit, to delete before `v0.4.2`. **Left for the operator, in order:** delete that tag; push `docs` and ModelRack; tag and publish `baseaicore 0.4.2`; recompile ModelRack's lock; tag `v0.7.0` and publish; verify the published wheel. |
| **H2** | **LoadCoach 1.1** (LA2) | **Opus 5 · xhigh** | H1 | Generalized LC-E1 (`[providers.<name>]`, D-11), adapter registry + `adapters scan|list|show`, subject expansion + compatibility/classification filters, `require_adapter_evidence` gate, pins, two-level residency (`base_switch_penalty`, `ignore_residency`), per-subject reliability, explanations/UI. Also carries the unreleased `2c7d740` fix. Exit: the IdeaPress three-stage demo — one base load (I16), classification denial recorded (I19). **Read:** `roadmap/adapter-roadmap.md` §4.2 + §7 I13/I16/I19, accepted D-11/A-4/A-7/A-8/A-9/A-10, `apps/loadcoach/spec.md` **Partly done 2026-09-05** (`H2_HANDOFF.md`; docs `2424c9a`+`4f60ce3`+this commit, LoadCoach `af64e93`+`4b25b8f`+`140a250`+`e256b24`; ran on Opus 5, daytime, no deviation). **Gates A, B and C are done and green; gates D–I are not, and the row stays open** — the split the kickoff named as the natural stopping point. The stop is artefact-forced: `find ~/ai/models -iname '*.gguf'` still returns four bases and **no LLM LoRA adapter**, so gate I's live demonstration (I16, I17's canary, I19 against a real adapter) cannot run and `1.1.0` must not be cut with its exit unproved (§0.5). **Built:** Phase 10 and the four LoadCoach documents, before any source; LC-E1 — `[providers.<name>]` with a declared `remote` flag, one tagged pool (`models.provider_name`, `models.is_remote`, migration 0008), per-candidate provider facts in routing, per-registration execution and residency, and a `doctor` that names each registration; and the adapter registry — `[adapters] directory`, SetSpec-validated manifests, the conversion into ModelRack's `AdapterRegistration` in the application, `adapters scan|list|show`, and two fail-closed refusals (a missing artifact, an edited one). **Four ADRs, because 0076 was not free — G3 took it the same day:** [0077](../adr/0077-a-named-provider-block-and-the-singular-block-are-one-registry.md) (both configuration forms together is a refusal, never a precedence rule), [0078](../adr/0078-a-shipped-response-field-is-superseded-beside-its-replacement.md) (`output.tool_calls_assembled` beside the fragments, removal named at 2.0 — **accepted, not yet built**), [0079](../adr/0079-an-adapter-classification-refusal-is-a-routing-rejection.md) (I19's recorded denial **is a LoadCoach explanation row**, not a `governance.egress_decision`; the roadmap's I19 row is amended) and [0080](../adr/0080-a-persisted-decision-names-the-subject-by-reference-and-by-string.md) (a decision names its subject by FK **and** by the string written at the time; the two unique keys moving onto the subject need an empty-string sentinel, because NULLs in a unique index do not constrain). `require_adapter_evidence` stays on and `base_switch_penalty` defaults to 0.10, **chosen not measured**, both recorded in `routing.md` rather than in a new ADR. **The `setspec` pin moved here** (§0.3's recommendation), with the `CapabilityEvidenceV1_1Out`/`In` adoption that keeps it green and one real addition: adapter-bearing evidence now binds to **nothing** rather than to the base's row, which would have raised the score of weights nobody measured the moment H4 exported a bundle. **H4's row loses** the pin move and that adoption; it keeps the bundle 1.1 export, the panel policy and Commissioner's widen. **Not true in the prompt:** 0076 was taken; `docs` was at `1c2b19c`, not `c8eb72f`; LoadCoach's venv is **Python 3.14.4**, not 3.13.15; and `content.fact_check` — ADR-0064's own example — is **not** a SetSpec capability (the real terms are `auditing.fact_check`, `coding.python`, `user.house_voice`). **Left, in order:** gates D–G (all buildable without artefacts; D carries the `adapters` table and migration 0009), gate H (explanations, UI, ADR-0078's field), then gate I once two LoRA GGUFs exist. **Decided by the operator 2026-09-05** (interview recorded in `docs/history/H1_HANDOFF.2.md` §2): **one migration per gate**, 0009–0012, not a single 0009; ADR-0078's `output.tool_calls_assembled` **stays at gate H**; ModelRack's `register_adapters()` now takes the **complete** set (a rescan restates the directory whole; a name absent is retired at the next idle), so `adapters scan` can express "the directory is the truth" without constructing a new provider; and **the artefacts gate I needs exist** — three adapters for `Qwen2.5-1.5B-Instruct.Q8_0` under `~/ai/models/adapters/llm/`, digests in that handoff §4. `modelrack 0.7.0` is prepared (not yet published), so the `>=0.7` pin resolves once the operator publishes. **Done 2026-09-05, second sitting** (`H2_HANDOFF.2.md`; LoadCoach `5768cdb`+`e4583c9`+`ddc910c`+`4f1ed08`+`659b1bf`+`2358301`+`9829361`+`eca8e96`, docs `b07c64a`+this commit; ran on Opus 5, daytime, no deviation). **Gates D–I are built, green and demonstrated live; `loadcoach 1.1.0` is prepared.** **Gate D** — subject expansion: a candidate is the triple `(identity, adapter | none, profile)`, expanded only where a registration's provider declares `adapter_hot_swap`, with `adapter_incompatible`, `adapter_unmeasured` and `adapter_classification_conflict` joining routing §4, each persisted and queryable; migration `0009` creates `adapters` and names the subject on every routing row by FK **and** by string. **An adapter subject inherits no evidence from its base** — its only signals are its manifest's declared vocabulary terms, because a benchmark on bare weights describes bare weights (ADR-0058 §4's rule, applied in the other direction). **Gate E** — `overrides.adapter` with `model`-pin semantics: it bypasses scoring and `require_adapter_evidence` (a pin is not routed selection) and never a hard constraint; `ADAPTER_NOT_FOUND` lists what does exist; migration `0010` records the subject and the classification pair on every attempt. A provider's `ADAPTER_NOT_FOUND`/`PROFILE_MISMATCH` now ends the job with its attempts written instead of falling back. **Gate F** — two-level residency (`resident_base` / `base_switch` / `nothing_resident` / `ignored`), each candidate carrying a `residency_detail`; migration `0011`. **Gate G** — reliability and the breaker key on the subject; migration `0012`, existing rows are base subjects with no count moved. **Gate H** — the models view groups adapter subjects under their base with each one's evidence source, and ADR-0078's `output.tool_calls_assembled` ships beside the superseded fragments. **Gate I, live on the real `llama-server`:** three `/generate` requests pinning three LoRA adapters were answered by **one** server pid with `list_resident` holding the base alone and three visibly different answers (I16); the same weights registered a second time under a registration **declared** remote left three `adapter_classification_conflict` rows carrying the full arithmetic while the bare base stayed servable (I19). **Two things this row had to build that were not in its plan:** `kind = "llamacpp"` was **not wired into LoadCoach's provider factory at all** (only `ollama` and `fake` were), so the only kind that can serve an adapter could not be configured — it now takes `model_directory` (required), `state_dir` and `server_path`; and **migrations now run with SQLite foreign keys off**, because adding a foreign key to `routing_decisions` is a table rebuild and dropping a parent with `foreign_keys=ON` cascades every stored routing candidate away — the explainability promise itself. `0010` also re-creates the claim index by hand, since alembic's reflection loses `0004`'s `DESC`. **Left for the operator:** push `docs` and LoadCoach. **The publish is held until H3 and H4 land**, decided at this row's interview: both consume the 1.1 contract, and publishing once after they have exercised it avoids a 1.1.1 within the week. **Two ADRs came out of that interview** — [0081](../adr/0081-an-adapter-subject-inherits-no-evidence-from-its-base.md) (an adapter subject inherits no evidence from its base, so `require_adapter_evidence` gates something real and a pin is how an adapter is used until LA3) and [0082](../adr/0082-a-migration-run-suspends-sqlite-foreign-key-enforcement.md) (a migration run suspends SQLite foreign-key enforcement, the one stated exception to database standards §2) — and the llama.cpp block stays at the three keys it has. | Routing semantics frozen into persisted explanations — the LoadCoach-P3 precedent, extended |
| **H3** | IdeaPress adapter pins → minor release | **Opus 5 · high** (deviation) | H2 | Per-stage adapter pins in config passed as the LoadCoach override; provenance columns on every attempt; direct/Ollama mode stays adapter-free. **This row also carries the IdeaPress three-stage demonstration**, moved here by H2's §0.5: the literal exit LA2 names is an IdeaPress project, and IdeaPress's pins are this row — so H2 proved the same property at the LoadCoach boundary (three pins, one base load, one recorded denial, all live) and the three-stage project is re-run here, on top of it. **H2 shipped the contract this row consumes:** `overrides.adapter` selects with `model`-pin semantics and bypasses `require_adapter_evidence` but no hard constraint; a pin naming an unregistered adapter is `ADAPTER_NOT_FOUND` (404) listing what does exist; every attempt records `subject_canonical_id`, `adapter_id` and the classification pair; and `kind = "llamacpp"` is now configurable, which is the only kind that can serve an adapter at all. **Also decided by the operator on 2026-09-05, from H2's interview: the caller half of the classification join lands here.** LoadCoach takes no caller classification today, so an attempt's `effective_data_classification` is the adapter's own value and I19's denial detail shows `caller_classification: null` — the adapter half of ADR-0065 rule 2, honestly recorded. IdeaPress is the first caller that has a classification to declare, so `data_classification` joins the `/generate` body here and LoadCoach computes `max(caller, adapter)` on the attempt and in the rejection detail. **Five decisions taken by the operator on 2026-09-05, at the kickoff interview, and folded into `history/h3-ideapress-adapter-pins.prompt.md`:** (1) **the LoadCoach caller field folds into the unpublished `1.1.0`**, not a `1.2.0` — `1.1.0` has never been published, so a second minor would record a distinction no user can observe; the changelog entry joins the same unreleased section and the tag still waits for H4. (2) **IdeaPress declares one application-level `data_classification`, and unset means the lowest class** — the true statement is about the installation, not about a stage, and the default keeps every shipped `1.0` configuration loading unchanged with the join `max(caller, adapter)` equal to the adapter's own value, exactly today's behaviour. Under-declaration is accepted: the adapter half still fails closed against a remote registration, which is the invariant that matters. (3) **The per-stage pin gets its own key, in effect wherever it is set** — no second boolean and not behind `honour_stage_bindings`, whose documented meaning is "give up routing"; an adapter pin does not surrender routing, so riding that flag would make the configuration lie, and a pin behind a default-off boolean would be the silent no-op the `job_stages` validator exists to prevent. **This is ADR-0083**, and it must state the both-set case: a model pin narrows to one base, the adapter pin selects among that base's subjects, and that combination is the one place an adapter pin does surrender routing — because the model pin already did. (4) **`data_classification` is optional on LoadCoach's wire**, never required — PromptCadence speaks the `1.0` wire. (5) **The row runs on Opus 5 · high**, a deviation from the scheduled `Sonnet 5 · standard` to be recorded under [model-assignment §3.5](model-assignment.md): the row was scheduled as one repository of configuration passthrough and now spans two repositories, a routing-semantics change, a migration and a live three-stage demonstration, and the classification join is a security-shaped invariant whose failure mode is quiet. **Verified 2026-09-05 before the kickoff:** the contract is built (`web/routes/routing.py:69`), the next free ADR is **0083**, IdeaPress's next migration is **0006**, and `ConstraintInputs.caller_data_classification` exists (`domain/routing/constraints.py:385`) but is **never passed** at `services/routing.py:660` — the caller half is dead-wired, which is precisely what this row closes. **Read:** `roadmap/adapter-roadmap.md` §4.4, accepted A-7, ADR-0065 rule 2, `H2_HANDOFF.2.md` §§4–5 **Done 2026-09-05** (`H3_HANDOFF.md`; docs `80f2100`+this commit, LoadCoach `5f0f254`+`cbe1ef9`+`8e72a8a`, IdeaPress `e1bfd2b`+`2ad19d6`+`8efc11e`+`bdd350c`+`eb2063b`+`30ba52f`; ran on **Opus 5 · high**, the operator's scheduled deviation, recorded in `model-assignment.md` §2.11). **All six gates built, green and demonstrated live; `ideapress 1.1.0` is prepared and `loadcoach 1.1.0` amended.** **Gate A** — Phase 10, the two IdeaPress documents, LoadCoach's `api.md`/`routing.md`, the naming note (`spec.md` §11: **backend** for the port sense, **adapter** for the LoRA sense) and **[ADR-0083](../adr/0083-an-adapter-pin-is-configured-on-by-being-configured.md)**: the pin gets its own table and no gating boolean, because `honour_stage_bindings` means "give up routing" — which an adapter pin does not — and a new default-off boolean would make a configured pin the silent no-op `job_stages` exists to prevent. **Gate B** — the caller half, in LoadCoach: an optional `data_classification` on `/generate` and `/jobs` reaches `ConstraintInputs.caller_data_classification`, which nothing had ever set, so an attempt's `effective_data_classification` is now `max(caller, adapter)` and the rejection detail's `caller_classification` is no longer `null`. **Gate C** — `[models.stage_adapters]`, sparse and beside `[models.stages]`, with four startup refusals (gate stage, unknown stage, empty name, any mode but `loadcoach`), plus `[inference] data_classification` defaulting to the lowest level; the compatibility golden is generated by **v1.0.0's own code in a worktree**, because a golden written by the version under test agrees with any change the two made together. **Gate D** — the passthrough, with the pin resolved in the gateway beside the model binding; `ADAPTER_NOT_FOUND`/`PROFILE_MISMATCH` map to two new IdeaPress codes on both the synchronous and the queued path, neither taking the recoverable route (which engages a fallback, and every fallback is the bare base), a pinned stage does not fall back at all, and an answer whose subject is not the pinned adapter is refused rather than recorded. **Gate E** — migration `0006`: `adapter_name`, `adapter_digest` and `subject_canonical_id` on `attempts`, written from what **answered**, `NULL` on pre-1.1 rows. **Gate F, live on the real `llama-server` through a real `loadcoach serve`:** three pinned stages answered by **one** server pid with **one** residency row, three visibly different answers to one prompt, every attempt naming its subject; the join proved with the *caller* raising it (adapter reviewed `public`, installation `confidential`, attempt records `confidential`); and I19's denial recorded with `caller_classification: "confidential"`. **Three cross-repository defects the plan did not predict, all found by the live journey and all silent:** (1) **`head_dim` was never reported by ModelRack's llama.cpp descriptor**, only its factors — so the theoretical KV figure was unknown, an unknown estimate is a refusal (ADR-0016), and **every llama.cpp candidate was rejected `insufficient_vram` on any machine with GPU telemetry**; the only provider kind that can serve an adapter could serve nothing at all through a running server. Reconstructed in LoadCoach as `embedding_dim // attention_heads` (LoadCoach `8e72a8a`); **the honest home is ModelRack's descriptor and that is a row for another day**. (2) **A queued job lost `overrides.adapter` and `ignore_residency`** — `JobSubmission`'s `request_json` round trip wrote neither and read neither back, so a pin submitted through `/jobs` (IdeaPress's default path for `draft`, `revise`, `repair` and `project_review`) was answered by the bare base with no error anywhere: exactly the fallback ADR-0064 rule 4 forbids. (3) **`loadcoach serve` leaks its supervised `llama-server` on shutdown** — six orphans from earlier runs were holding 13 GB of a 16 GB card, and the next candidate is then refused `insufficient_vram` before its classification is ever considered, which reads as a defect in the thing under test. **Two limitations recorded rather than fixed:** none of the eleven task profiles IdeaPress's stage map names allows a remote registration, and the two shipped profiles that do demand ≥128k context — so I19's setting is unreachable from a stock installation and the journey creates it by editing one stored profile in the operator's own database; and the same weights cannot be registered twice (a `models` row is keyed on identity, not on the registration that found it), so the demonstration's local and remote halves are two servers over two databases. **The closing interview took four decisions, all built:** (1) **`head_dim` moves to ModelRack** — the LoadCoach reconstruction is reverted and `modelrack` now reconstructs it in both the llama.cpp and the Ollama descriptor, folded into the unpublished `0.7.0`; (2) **the leaked `llama-server` is fixed** — the cause was that **LoadCoach never called `close()` on any provider**, so the lifespan now closes every registration, measured at zero orphans after `SIGTERM` where each run previously left one; (3) **`[routing] task_profiles_path`** lets a deployment name its own `task_profiles.toml`, so the journey supplies I19's remote-permitting profile through configuration instead of the SQL fixture; (4) **a full workflow on a larger base was attempted and found something better.** Three bases were driven through a real `loadcoach serve`: the 1.5 B failed `requirements`, the 9 B `500`s on every request because its chat template raises `System message must be at the beginning`, and the 14 B **produced a correct extraction wrapped in LoadCoach's `structured.extract` envelope** (`{"confidence": …, "data": {"requirements": […]}}`), which IdeaPress cannot parse. The transcript shows the mechanism: attempt 1 emitted IdeaPress's own shape and failed LoadCoach's `json` check on a `<think>` prefix; the corrective retry then coerced attempt 2 into the profile's shape, which `require_schema` and `required_fields = ('data',)` accepted. **So the profile's schema still governs the response even when the caller asked for bare `json`** — ADR-0041 took the caller's schema off the request but left the profile's validation on the answer, and for a stage whose output shape is the caller's own that is a hard failure rather than a degradation. **IdeaPress's `requirements` stage has therefore never completed against a real LoadCoach** (its live test exercises `critique`, a text stage), a larger base does not fix it, and no LoRAs were trained because the 14 B's content was already right. **That wants its own row** — it predates H3, involves no adapter, and blocks any full-workflow demonstration through LoadCoach. **Left for the operator:** push **four** repositories (`docs`, LoadCoach, IdeaPress, **ModelRack**); tag and publish `ideapress 1.1.0`, `modelrack 0.7.0` (which must carry the `head_dim` commit, or every GGUF-served candidate is ineligible again), and `loadcoach 1.1.0` once H4 lands. | The contract is settled, but the row now carries the caller half of ADR-0065 rule 2 — a classification join whose wrong answer permits an egress nobody sees |
| **H4** | FreeWeight 1.1 (LA3) | Sonnet 5 · high | H2 + B5 | `[adapters] directory`, subject enumeration, `--adapter` flag, the A-2 panel policy (declared + regression + performance), serving-mode A/B, evidence export with v1.1 fields (an import change — `CapabilityEvidenceV1_1Out`/`In`; the bare names still mean `1.0`, ADR-0068 rule 3 — and bundled export needs B5's `benchmark.evidence_bundle` `1.1`), comparison UI grouped by base. Exit I18 needs a GPU session. **Two items this row was written to carry were taken by H2 on 2026-09-05 and are struck here** (`docs/history/H2_HANDOFF.md` §4): ~~LoadCoach's `setspec` pin~~ moved at H2, and ~~the `CapabilityEvidenceV1_1Out`/`In` adoption~~ done at H2 **on the import side only** — LoadCoach's *reader*. FreeWeight's *writer* is untouched and is still this row's work: five `1.0` constants in `services/evidence.py` and `services/export.py`, plus their goldens. What remains of the folded-in pin work is **Commissioner's**, folded in here by decision on 2026-09-04. FreeWeight's own `setspec` pin needs nothing — E5 widened it to `>=0.4,<0.7` on 2026-09-04. `commissioner 0.1.0` pins `setspec>=0.5,<0.6`, which since F2 holds PromptCadence's resolved `setspec` at 0.5.x even though PromptCadence's own range admits 0.6 — the same shape as the `mirrorwall<0.5` cap E5 spent a whole row removing, and harmless only for as long as nothing in PromptCadence consumes a 0.6 payload. This row is where all the 0.6 movement happens, so widen Commissioner to `setspec>=0.5,<0.7` here, **as its own release** (`commissioner 0.1.1` — a second repository and a second publish inside a row otherwise scoped to FreeWeight, which is the cost of keeping the 0.6 movement together and is accepted deliberately). Commissioner adopts no payload in doing so: it owns the Python form of `governance.egress_decision` 1.0 and that is unchanged, so this is a range widen and a gate, the same shape MirrorWall's was. Its `docs/packages/commissioner/spec.md` §56 states the range deliberately and moves with it. Then re-resolve PromptCadence and confirm with `pip show` that it reaches 0.6.0 — the widen is only real if something actually resolves past 0.5.x. **Read:** `roadmap/adapter-roadmap.md` §4.3 + §7 I18, accepted A-2/A-3, `apps/freeweight/spec.md`, `docs/history/F2_HANDOFF.md` §7 (the cap, and the evidence it binds) **Substantially done 2026-09-06, and one gate short of complete** (`docs/history/H4_HANDOFF.md`; FreeWeight `43f98ca`…`cbe59ed`, docs `9589ca4`+`f199784`+`e2b5273`, Commissioner `ad1b248`; unpushed). Gates A–E built and green: the llama.cpp provider and `[adapters] directory`, subject enumeration by digest with migration `0008`, `--adapter`, the A-2 panel with its fixed regression panel, the serving-mode A/B, payload versions chosen by content, and the comparison grouping. `commissioner 0.1.1` is release-committed with `setspec>=0.5,<0.7`, and PromptCadence is confirmed resolving `setspec` 0.6.0 against it. **`freeweight 1.1.0` is deliberately not cut**, per this row's kickoff §0.1: I18 is unproved. **I18's producer half passes live** — a `1.1` bundle of three subjects' records exports, validates with `setspec` alone, crosses as a file, and its bare-base record imports and binds in LoadCoach with no shared code or database. **Its consumer half fails, and one step earlier than H2's handoff predicted**: the two adapter-bearing records were **rejected as `DUPLICATE_RECORD`**, not left `unmatched`, because [ADR-0022](../adr/0022-capability-evidence-record-contract.md) §3's uniqueness key predates the adapter axis and collapses a base and every adapter subject on it into one key. [**ADR-0085**](../adr/0085-the-evidence-uniqueness-key-carries-the-adapter.md) closes that decision and is the follow-on row's specification: LoadCoach's key gains `adapter_artifact_digest` **first**, then `capability_evidence` gains the adapter axis, then I18 runs whole and `freeweight 1.1.0` is cut. **Four things this row's own text said that were not true**, recorded in the handoff §6: FreeWeight's `setspec` **floor** did have to move (0.4 → 0.6 — `model.adapter_manifest` is 0.5.0 and `EvidenceBundleV1_1Fields` is 0.6.0), H2 had finished all of A–I rather than A–C, H2's gate D taught *routing* subjects and not the evidence store, and the first adapter-bearing bundle lands *rejected* rather than *unmatched*. **The measured serving-mode overhead is +0.9 %** (~+6 ms on a ~750 ms run, four repeats, sign changing between them) — not material, so ADR-0060's revisit trigger does not fire and the default does not move. **Pushed 2026-09-06**, with `commissioner 0.1.1` tagged `v0.1.1` and published; `freeweight 1.1.0` is still uncut and `loadcoach 1.1.0` is held untagged so the follow-on row rides it (§4, and row **H5**). | Measurement plumbing on an existing engine; panel composition is spec'd. The two pin widens are mechanical and proven twice |
| **H5** | **LoadCoach 1.1 + FreeWeight 1.1** (LA3's consumer half) → both published | **Opus 5 · high** | H4 | [ADR-0085](../adr/0085-the-evidence-uniqueness-key-carries-the-adapter.md), in the order it states. (1) LoadCoach's consumer-side uniqueness key gains `adapter_artifact_digest`, nullable, `NULL` meaning the bare base. It must come **first**: until it moves there are no surviving records to bind, and widening the key alone leaves the records `unmatched`, which is progress and **not** the feature working (`docs/history/H4_HANDOFF.md` §3). (2) `capability_evidence` gains the adapter axis, so those records bind and a subject is scored in its own right rather than under its base. Two migrations, one per step. (3) **I18 runs whole**: re-export H4's three-subject `1.1` bundle from FreeWeight, import it through `loadcoach evidence import`, and show an adapter subject's evidence changing a selected subject visibly in the explanation — the exit condition H4 could not reach. (4) **Then cut both releases.** `freeweight 1.1.0` is one version bump and one changelog move, deliberately left uncut at H4. `loadcoach 1.1.0` was **held unpublished by operator decision on 2026-09-06 so this row rides it**: fold `9829361`'s six follow-on commits (`eca8e96`, `e0d811a`, `5f0f254`, `cbe1ef9`, `8e72a8a`, `25ce551`) into the `1.1.0` changelog section rather than taking a `1.2.0`, and tag once. **Three follow-ups the same decision scheduled here**, all from `docs/history/H4_HANDOFF.md` §8.5 + §12: run FreeWeight's A-2 **regression panel against a real adapter** (the LA3 journey used `native.echo`, so the panel has never met one; `terse` is the likeliest to have lost instruction-following, and a negative result is `apps/freeweight/risks.md` T11's revisit trigger either way); **bound the comparison grouping** (`group_by_base` is O(records) and the template `rowspan`s every subject — a dozen renders, a hundred does not read); and **delete PromptCadence's stale `pyproject.toml` comment** saying Commissioner needs widening before adoption — `commissioner 0.1.1` widened it and was published on 2026-09-06. **Read:** ADR-0085, [ADR-0022](../adr/0022-capability-evidence-record-contract.md) §3 (the amended key), ADR-0058 (why the adapter stays in its own payload block) and ADR-0059, `docs/history/H4_HANDOFF.md` §3 + §12, `docs/history/H2_HANDOFF.md` §7, `apps/loadcoach/spec.md`, `apps/loadcoach/data-model.md`, `roadmap/adapter-roadmap.md` §7 I18 **Done 2026-09-06** (`docs/history/H5_HANDOFF.md`; docs `0ee88bb`+`66f3c46`, LoadCoach `5717b0f`…`f5f54d9`, FreeWeight `5b726ca`…`ac9a823`, PromptCadence `784e89f`; **unpushed**). All seven gates: [ADR-0086](../adr/0086-the-consumers-adapter-key-column-is-not-nullable.md) and Phase 11; migration `0013` (the key) and `0014` (the axis); routing keyed on the subject; **I18 whole**; the three follow-ups; both release commits. **ADR-0085's decision 2 was wrong for the consumer and is amended**: it spelled `adapter_artifact_digest` nullable, and LoadCoach writes this table through `weightsdb.upsert`, an `INSERT … ON CONFLICT` whose conflict target never fires on a `NULL` — verified on both dialects — so a nullable key column would insert a second row on every re-import of a bare-base record, silently. The column is `NOT NULL` with `''`, the sentinel ADR-0080 rule 5 already chose in the same application. **I18 passes**: three records, zero rejections, three subjects bound, a measured adapter subject selected on a `benchmark` signal with the unmeasured sibling rejected `adapter_unmeasured` in the same decision. **Running it found two real defects neither handoff predicted.** FreeWeight recorded `RuntimeProfile.adapters_registered = None` on every run including those it served with three LoRAs registered, so its evidence carried a `runtime_profile_hash` that never happened and LoadCoach — resolving the honest `True` — excluded all of it as `evidence_profile_mismatch`, correctly and silently ([ADR-0074](../adr/0074-adapter-enabled-serving-is-a-runtime-profile-field.md) rule 3, now implemented). And LoadCoach's `sync_adapters` wrote the adapters table without re-binding evidence, so a record naming a newly reviewed adapter sat `unmatched` until an unrelated model refresh. **The A-2 panel has met a real adapter**: no forgetting detected, and all three adapters moved `instruction_following` by exactly one case in eleven and `structured_output` not at all — a statement about n=11 and n=3 rather than about the adapters, recorded against T11's second revisit trigger. **`loadcoach 1.1.0` and `freeweight 1.1.0` are prepared, gated and wheel-verified, not tagged and not published.** | A shipped table's uniqueness key changing under a contract two applications already agree on, with a migration on each side and a release on each side behind it. H4 proved the trap sits one step earlier than either handoff predicted, and the `runtime_profile_hash` near-miss it records (§3) is exactly the plausible, passing, wrong join a cheaper model takes |
| **I1** — **done 2026-09-06** (`docs/history/I1_HANDOFF.md`) | PromptCadence P8 | **Opus 5 · high** | G1 + E1 | Materialization/invalidation of explanation revisions with the `materialize(rows) == compose_live(rows)` golden (D-13) — Opus; CutCtx wiring and the MirrorWall operator UI volume — Sonnet. **Read:** `apps/promptcadence/development-plan.md` Phase 8, `apps/promptcadence/lifecycle.md` §7 + §9.1, accepted D-8/D-13 | Cache-consistency reasoning vs. UI volume — a clean split |
| **I2** — **done 2026-09-06** (`docs/history/I2_HANDOFF.md`) | **PromptCadence P9** → **1.0.0 — M12** | **Opus 5 · xhigh** (consider Fable 5 for the injection corpus) | I1 + H2 | Hardening: the prompt-injection corpus as a release gate, spec §20 criteria, overhead budget (≤ 25 ms/turn), live remote-tier trajectory via LC-E1 (I13) *or* the recorded release-scope decision to ship with remote tiers refusing honestly. **Read:** `apps/promptcadence/development-plan.md` Phase 9, `apps/promptcadence/spec.md` §20, `roadmap/promptcadence-roadmap.md` §9 I13 + §10 **Done 2026-09-06** (`docs/history/I2_HANDOFF.md`; docs `6feda21`+`8b27f82`+this commit; PromptCadence `5c070b2`…`b4b67ac`, ten commits ending in the **1.0.0 release commit `b4b67ac`**; ran on Claude Fable 5.1, whole, daytime). **Held:** ADR-0095…0098; Security Standards §14 item by item with the two `[server]` limits enforced and a principal on every `/api/v1` route; the retention sweep incl. workspaces (migration `0011`); the ten-case injection corpus as a CI gate asserting the harness and never the model; the replay cap (16 KiB, ToolYard's bound); every §15 budget asserted — **per-turn overhead 13.45 ms median** (target 25, ceiling 100), the SSE row 10 ms median against a 5 ms target and 20 ms ceiling (the 20 ms poll — interview); I13's recorded-transport half in CI, the live half deferred by ADR-0098; `GET /tiers`; the operator set, `docs/openapi.json`, the generated configuration reference, `requirements/ci.lock` with CI installing from it, `pip-audit` and `gitleaks` clean, the clean-venv and 0.9.0b0→1.0.0 wheel upgrades proved. **Did not hold:** the independent brief — three runs on gpt-oss:20b with shipped defaults delivered no `summary.txt` (one `finish_reason=length` with 4 096 reasoning tokens and no text; two declared finishes without the work, one traceable to **F1: tool descriptions never name the configured `read_roots`**, a PromptCadence defect); the harness governed all three correctly. **Found:** LoadCoach 1.1.0 renders `is_remote` on the generate response and not in `GET /models` (ADR-0098's correction — a LoadCoach patch row); spec §7.1's `GET/PUT /settings` are unbuilt and undefined (recorded in `docs/troubleshooting.md`). **Released:** `v1.0.0` tagged on `b4b67ac`, both repositories pushed, and `promptcadence 1.0.0` published to PyPI — verified 2026-09-06, so ADR-0011's released-1.0 condition for the J rows holds. | The security half is pure adversarial and never overnight; measurement and docs are not |
| **H6** | **The evidence gate scores what it admits** + the damaged adapter | **Opus 5 · high** | H5 | Two decisions taken at the H5 interview on 2026-09-06, both deferred out of that row deliberately. **(1) Tighten `require_adapter_evidence`.** It reads `subject.signals` — the raw list — so a signal that *scoring* then excludes (`evidence_unbound`, `evidence_foreign_machine`, `evidence_profile_mismatch`) still satisfies the gate. Observed live at H5: two adapters whose manifests merely **declared** `reliability` scored `0.500 declared` and outranked the bare base, whose real measurement was excluded as `evidence_profile_mismatch` and therefore scored **absent** — a claim beating a measurement, with the gate silent, under the one constraint whose purpose is "no benchmark, no routed selection" ([ADR-0064](../adr/0064-adapters-are-selected-through-the-capability-vocabulary.md) rule 3). The gate must admit only a signal that survives profile and machine filtering. **Needs an ADR**: it changes a shipped constraint's meaning, and it makes an adapter unroutable when the runtime profile hash moves rather than degrading to a declared claim — which is the intended stricter reading, and must be written down as such. **Note the second, independent defect while you are there** (`domain/routing/scoring.py`'s `if excluded is not None: return excluded`, which precedes the band prior): a subject whose measurement was excluded scores strictly *worse* than one never measured. Deliberate — it names the reason rather than hiding it — but it is the other half of what let a claim win, and it deserves its own decision rather than being changed in passing. **(2) Run the A-2 panel against a deliberately damaged adapter.** H5's first real run produced identical numbers for three different LoRAs (`+0.091` on n=11, `0.000` on n=3), so the panel cannot currently be shown to separate a damaged adapter from an undamaged one — [risks](../apps/freeweight/risks.md) T11's second revisit trigger, in its quietest form. Train or corrupt one LoRA on purpose, run the panel, and record whether the two fixed suites move. Needs GPU time and a training run; `~/ai/tools/lora-train` is the existing setup. A negative result settles the trigger either way and is what T11 has been waiting for since the panel was designed. **Read:** `docs/history/H5_HANDOFF.md` §4 + §9, ADR-0064 rule 3, ADR-0023, `apps/freeweight/risks.md` T11 + its revisit triggers, `apps/loadcoach/routing.md` §5.1 **Done 2026-09-06** (`docs/history/H6_HANDOFF.md`; docs this commit, LoadCoach `93063bd`, FreeWeight this row's commits; **unpushed**). **(1) [ADR-0087](../adr/0087-the-evidence-gate-admits-only-a-signal-that-scores.md)**: the gate reads the *resolved* capability score rather than the raw signal list, so the gate and the scorer cannot disagree about what counts as measured and no second copy of the exclusion rules exists to drift; the `adapter_unmeasured` rejection keeps its one name (rule 3) and its detail now carries `resolved_source`, both profile hashes or the foreign machine's fingerprint, and the `freeweight run start …` remedy. An adapter goes unroutable when the runtime profile moves, which is the intended stricter reading, written down. **The second defect took its own decision, [ADR-0088](../adr/0088-an-excluded-measurement-falls-back-to-the-prior-it-displaced.md)**: an excluded measurement scores **the prior it displaced** while keeping its own source, note and remedy, so a benchmarked subject no longer ranks below one nobody measured and the explanation is unchanged. Both fold into the unpublished `1.1.0`. **(2) The damaged adapter found something bigger than the panel.** A LoRA trained on deliberately mismatched pairs scored `0.818` against the base's `0.727` — *better* — and 8 of its 11 responses were byte-for-byte the base's. **FreeWeight never set `GenerationRequest.adapter`**, so every adapter-subject run since H4 measured the bare base and filed the numbers under the adapter: risk **T12** from the direction its two-ended mitigation does not cover, invisible to both ends because the record was internally consistent. Fixed at the shared point (`_build_request`, warm-up and interaction turns alike, read back from the run's own row), with a creation-time refusal for a provider that cannot hot-swap and a unit test beside the one that asserts the *runtime profile* reaches the wire — the same defect on the subject's other axis. **Then the panel answered T11 properly**: base `0.727`/`1.000`, `+terse` `0.636`, `+pirate` `0.727`, `+verbose` `0.909`, `+damaged` **`0.182`/`0.000`**. Both fixed rows separate a damaged adapter from an undamaged one, the three healthy LoRAs now differ from each other, and **T11's second revisit trigger is closed** — `structured_output` was never a suite that does not move, it was a suite that had never met an adapter. Every adapter-bearing evidence record produced before this row measures the bare base — local only, since adapter support is new in the unpublished `1.1.0` of both applications. **(3) The closing interview added three things.** **[ADR-0089](../adr/0089-the-fixed-regression-rows-bound-their-own-output.md)**: the two fixed regression rows carry a 512-token per-turn cap, versioned with the catalogue rather than configurable, because a damaged adapter loses the instruction *to stop* along with every other instruction and the panel is most expensive on exactly the subject it exists to catch (72 s capped against roughly forty minutes uncapped; no healthy subject came within six times it). A **live canary** asserts the panel still separates the damaged adapter by at least 0.30 — the end-to-end guard the unit test cannot give, since sending the adapter and *serving* it are different claims. And **I18 was re-run whole, twice**: the shipped live pair unchanged, then again over the A-2 fixed rows under the *shipped* `adapters.measured` profile, where `+verbose` (0.909) ranked above the bare base (0.727) above `+damaged` (0.182), every score `benchmark`. LA3's exit is now met **on merit** rather than on a gate decision | The gate change is small code behind a decision that has to be argued properly, and it is exactly the failure H5 found by accident rather than by design. The damaged adapter is cheap to run and the only thing that answers a question the risk register has held open since LA3 was specified |
| **I3** — **done 2026-09-06** (`docs/history/I3_HANDOFF.md`) | **LoadCoach 1.1.1** — render `provider_name` and `is_remote` in `GET /models`; let a task profile ask for reduced thinking | Sonnet 5 · high | I2 (**before J1**) | Two findings from I2 (2026-09-06). (a) LoadCoach 1.1.0 records both columns (migration `0008`) and renders them on the generate response's `model` block only; `web/routes/models.py::_model_to_json` omits them, so PromptCadence's remote-provider fact — read from the listing when present (ADR-0098 and its correction) — is `False` against a real LoadCoach and remote tiers refuse honestly. Two lines, a test, the OpenAPI snapshot and api.md §2. (b) ModelRack 0.7.0 has the thinking control (H1 gate F, `SamplingParameters.think`) and no LoadCoach task profile can ask for it; gpt-oss:20b spent all 4 096 output tokens of `tools.agent.local_fast` reasoning and answered nothing (I2 §10 run 1, the G2 §5 shape). Decide the profile field, thread it through routing to the provider, and set it on the five harness profiles; record the empty-answer rate before and after on the reference machine. **Read:** `docs/history/I2_HANDOFF.md` §5 + §10, `docs/history/G2_HANDOFF.md` §5, `docs/history/H1_HANDOFF.md` gate F, `apps/loadcoach/api.md` §2 + §4, ADR-0055, ADR-0098 **Done:** docs `e8ef0ee` (ADR-0099, api.md §2/§4, routing.md §2/§4), LoadCoach `8a57472` (the render, CLI `--json` too), `13a0286` (`execution.think`, `sampling.think` over it, `thinking_control` required at routing), `cb1cfac` (the measurement, the five profiles, `ci.lock` → modelrack 0.7.1) and `2a7ac58` (`1.1.1`, untagged and unpushed). **What held:** the render is two lines and the snapshot did not move for it; the byte-identical golden holds; the rejection names `thinking_control` and `required_by`. **What did not:** `thinking_control` is not a SetSpec capability, so it travels beside `requires_capabilities` rather than inside it (ADR-0099 rule 5); the five ship with `think` **unset**, because gpt-oss:20b accepts the control and does not honour it — `tools.plan` delivered 3/6 unset and **0/6** at `think = false`, `tools.agent.local_fast` 6/6 either way; I2 §10 run 1's pathology did not reproduce from a cold agent turn; the singular `[provider]` block renders `provider_name = "ollama"`, not `""`. | A render fix plus one profile field; the decision is which field and its default |
| **I4** — **built 2026-09-06, release held** (`docs/history/I4_HANDOFF.md`) | **PromptCadence 1.0.1** — the vendored LoadCoach snapshots, and I2's two leftovers | Sonnet 5 · standard | I3 (**before I5**; see §3 on J1) | Scheduled at the I3 interview (2026-09-06) as I5; **renumbered I4 on 2026-09-06** when the review put it ahead of the 1.1 row, so the table reads in execution order. `tests/contract/loadcoach_openapi.json` and `tests/contract/loadcoach_task_profiles.toml` are hash-pinned (`test_loadcoach_contract.py:56`, `test_loadcoach_task_profiles.py:65`; the digest constants at `:45` and `:37`) and now differ from what LoadCoach 1.1.1 ships: `info.version` on the first, the rewritten `tools.plan` comment on the second. Refresh both and re-pin the digests. **F1 is not part of this row** — it was fixed before the tag and shipped in 1.0.0 (`6a0d55f`, CHANGELOG *Added*); what I2 actually left for a patch is two named findings, and this row takes them or records why not: **F2**, whether an empty declared `stop` should complete a step (contract 6 says it does), and **the `args_sha256` mismatch**, where `tool.call.started` digests the canonical *text* and ToolYard the *structure*, so one call carries two digests (I2 §5). Nothing beyond those two — "whatever else 1.0.1 should carry" is how a patch becomes a minor. **Read:** `docs/history/I3_HANDOFF.md` §9, `docs/history/I2_HANDOFF.md` §5 + §13 + §14 **Done:** PromptCadence `6356f73` (both snapshots re-copied from LoadCoach `2a7ac58` and re-pinned), `9b5fb44` (F2 kept, and the console render that makes an empty completion visible), `b96d2d8` (the event digest, plus the equality assertion) and `0ec2c36` (`1.0.1`, untagged and unpushed). **What held:** both decisions took their default, so no ADR and no spec edit; not one contract assertion moved on the refresh; the whole suite is green on CPython 3.13.15 at 91.51 % coverage; `docs/openapi.json` moved by `info.version` alone. **What did not:** the kickoff was wrong on three machine facts — `max_output_chars = 50000` was *already* in the `5c5aa1f` snapshot (a line-shift artefact of the rewritten comment), so the fake LoadCoach was never at risk and the real TOML diff is two things not three; `pip install -e ".[dev]"` does **not** move the venv to `ci.lock` (the ranges were already satisfied — the two pins had to be installed by name); and the release commit carries three files, not four, because the version is `dynamic`. The `args_sha256` decision turned out not to be a judgement call at all — `domain/tools.py` already documents the field as the sanitized arguments' digest *and* as the event↔row join key, and neither was true. The golden masks all three of its digests to `"<sha>"`, so it could never have caught it. **The interview took all three of its questions off their defaults**, which roughly doubled the row: the pre-existing e2e flake was fixed rather than deferred (`16beb22`); the private `toolyard._safe` import was resolved by making the name public rather than shipping as-is, so **`toolyard 0.1.1` is prepared** (ToolYard `1e14788`, `338f9c7`, unpushed) and PromptCadence imports from the package root (`dcade58`); and `upgrading.md` was corrected here rather than at I5. **The flake's first diagnosis was wrong** — `_closed_port()`'s TOCTOU race looked like the cause and was not; the real cause is `configure_logging` binding `sys.stderr` by value, so a `CliRunner` buffer closes while its handler survives on the process-global root logger and a later background record dumps logging's error report into the next invocation's captured output. An autouse fixture restores the root logger; the failing seed `4198236421` and ten consecutive full runs are green. **The release commit is deliberately not cut:** `pyproject.toml` now requires `toolyard>=0.1.1`, PyPI holds `0.1.0`, and `ci.lock` cannot regenerate without the artefact — so PromptCadence CI is expected red until the operator publishes ToolYard. Sequence in the handoff §10: publish `toolyard 0.1.1` → regenerate the lock → cut `1.0.1`. **I6 runs next, not I5** (decided at the I4 interview, 2026-09-06): I6 depends on I3, no release waits on it, and its probe half is read-only, so it is the one row that can run while 1.0.1 is held. I5 stays behind the publish sequence. | Two digests and two named findings; the risk is picking up more than a patch should |
| **I5** — **built 2026-09-06** (`docs/history/I5_HANDOFF.md`); tag and publish pending | **PromptCadence 1.1: runtime settings** — `GET /settings` and `PUT /settings` (spec §7.1), or the decision that PromptCadence has none | Opus 5 · high | I4 | Spec §7.1 lists both endpoints and §14 names `admin` as their scope; no route exists, nothing defines which keys are runtime-changeable, and `docs/troubleshooting.md` records them as unbuilt rather than stubbing them (I2 §5). The operator chose to schedule rather than strike (interview, 2026-09-06; scheduled as I4, **renumbered I5 on 2026-09-06** because the 1.0.1 row runs first). **Decide the runtime-changeable set** — LoadCoach's precedent is a registry, `loadcoach.services.settings.RUNTIME_SETTINGS`, with the security-relevant keys refused by name (`apps/loadcoach/api.md` §9) and retention hours and a few routing weights allowed. **The persistence already exists**: the `settings` table is Phase 1's (`infrastructure/db/models.py:332`, migration `0001_initial_schema`) and is unused, so this row adds no migration. **The work is the loader and the surfaces** — configuration standards §7's one precedence deviation, `defaults → file → database → env → CLI`, which `promptcadence.config.Settings` has no database layer for today; `config show` marking database-sourced values `(database)`; the console's settings form (ADR-0094 anticipated its CSRF); and the generated reference — `docs/configuration.md` is emitted by `promptcadence config reference` from the settings model and today states *"Runtime-changeable is `no` for every key"*, so the edit is to the model and `tests/unit/test_config_reference.py` fails on drift. **Spec §12 has no "Runtime-changeable" column** — it is a TOML block and prose; amend that prose and §7.1 to say what moves at runtime, per configuration standards §8. Ships **1.1.0, on top of I4's 1.0.1**. **Read:** `apps/promptcadence/spec.md` §7.1 + §10 + §12 + §14, `standards/configuration-standards.md` §7 + §8, LoadCoach `services/settings.py` + `web/routes/settings.py`, `apps/loadcoach/api.md` §9, `docs/history/I2_HANDOFF.md` §5 | Which keys may move at runtime is a security decision; the plumbing is LoadCoach's shape |
| **I6** — **built 2026-09-07, ships nothing** (`docs/history/I6_HANDOFF.md`) | **The thinking control on Ollama** — which local models honour it, and the `think: false` stream that ends without a terminal chunk (**absorbs the row scheduled as I7**) | Sonnet 5 · high | I3 (flexible) | Both halves came out of I3 gate D (2026-09-06), share one machine and one lever, and the probe is the defect's reproduction harness — so they are one row, merged 2026-09-06. **The probe first, read-only:** gpt-oss:20b accepts Ollama's `think: false` and ignores it — it emits its reasoning as `content` rather than on the thinking channel, which then fails `require_valid_json`. Probe each installed model with `think: false` and `think: true`, record which put reasoning on the `thinking` field and which relocate it into `content`, and commit the table where the next person reaching for `execution.think` will read it. Ten models are installed (twenty streamed requests, ~100 GB of loads) — cap the set to what a tier could plausibly route to, or unload between. **Then the defect, with the probe's runs as its sample:** four of six streamed `POST /api/chat` requests carrying top-level `think: false` to gpt-oss:20b on Ollama `0.32.13` failed after 1.3 s with `ProviderProtocolError: The stream from http://127.0.0.1:11434 ended without a terminal chunk`, while a hand-made stream of the same body completed normally with `done: true` and `done_reason: stop`. Reproduce, then decide which it is: `OllamaProvider`'s stream parser (`providers/ollama.py:960` raises it) dropping or mis-reading a mid-stream error object, or Ollama genuinely closing the stream. Fix the first; document the second and say what a caller sees. **Stop rules:** the probe changes no task profile and no code, and if the parser proves innocent the row ends in documentation. Nothing in the suite sets `think` today, so no release waits on either half. **Read:** `docs/history/I3_HANDOFF.md` §4 and §9 **Done:** `docs` `4b08a4b` (the table, the spec's error row, the handoff) and the roadmap commit beside it, `LoadCoach` `dca96e3` (the `routing.md` mirror), `py/ModelRack` `a4efd39` (the `spec.md` mirror) and the probe harness the interview kept. **The verdict: Ollama closes the stream, and the parser is innocent** — so there is no `modelrack 0.7.2` and the package stays at `0.7.1`. Raw bytes captured with no ModelRack in the path show **12 of 13** streams under `think: false` **and** `format: "json"` ending in a clean chunked EOF with no `done: true` line; most carried no error object at all, some carried Ollama's own `error parsing tool call`, which `extract_error_message` already surfaces as the provider's message. Reaching that exception at all requires the stream to have ended cleanly — a truncation is `httpx.RemoteProtocolError` through a different translator — so none of the four suspected parser defects survives the capture. **It is the pair that fails:** `think: false` alone completes 3 of 3 and `format: "json"` alone completes 3 of 3. **The probe went to all ten installed models, none skipped**, two streamed requests each under one prompt, in about 25 minutes rather than the night the kickoff priced — `keep_alive: 0` on each model's last request is one load per model. **Eight of the ten honour the control**; only `gpt-oss:20b` accepts it and relocates its reasoning into `content`, confirming I3 on a second path. **Six of the eight burn the whole 4 096 budget thinking under `think: true`** (`done_reason=length`, empty `content`), which is the failure mode the lever exists for. **What the kickoff got wrong:** the failure is not intermittent (I3's four-of-six is the LoadCoach-level count of something that fails 12 times in 13 at the wire), and I3's "a hand-made stream of the same body completed normally" did not reproduce even once in six — that probe was almost certainly missing `format: "json"`. **The line number was right** (`providers/ollama.py:960`). **One finding outside the brief:** `deepseek-coder-v2:latest` refuses `think: true` with a clean HTTP 400 and **takes the whole Ollama process down on `think: false`**, 3 of 3, with and without `format`; systemd restarts it. **LoadCoach needs no row** — `retry_policy.py:105` already classifies `ProviderProtocolError` as `PROTOCOL` and answers `RETRY_SAME` then `FALLBACK`; the retries simply cannot rescue a failure that follows the body. **The closing interview (2026-09-07) took four decisions:** the crash gets the `routing.md` warning and no code (revisit only if a profile ever sets `think`); neither Ollama defect is filed upstream; the probe harness is **committed** as `py/ModelRack/scripts/probe_thinking_control.py` with the 1 976-character prompt embedded byte-identically, so the table is re-measurable after an upgrade or a pull; and this row is closed here. | Intermittent, two candidate causes with different fixes; the probe is what tells them apart |
| **I8** — **done 2026-09-07** (`docs/history/I8_I9_HANDOFF.md`; `loadcoach 1.1.2` tagged `1a3e2a7`, superseded the same day by **1.1.3, published** — ADR-0112) | **LoadCoach 1.1.2: a stored setting stops beating the environment** | Sonnet 5 · standard | I5 | I5's finding (2026-09-06), and the operator scheduled it at the I5 interview. `loadcoach.services.settings.read_runtime_settings` takes the stored row whenever one exists, so a database row beats `LOADCOACH_*` in the environment and a `serve` flag — while LoadCoach's own generated `docs/configuration.md` publishes `defaults → file → database → env → CLI` (configuration standards §7). Ignore a stored row whose key `LoadedSettings.sources` reports as `env …` or `cli` — LoadCoach **has** a CLI layer, so consult `sources` rather than `os.environ` as PromptCadence does — and report the shadowed row in `GET /settings` (`stored`, `source`, `shadowed_by`) and beneath the field on the Settings page, so a row that does nothing is visible rather than dropped. **Read:** `apps/loadcoach/api.md` §9, `standards/configuration-standards.md` §7, ADR-0100 rules 5 and its Consequences, PromptCadence's `services/settings.py` as the transcription source | The shape is decided and already built once; this is transcription plus a patch release |
| **I9** — **done 2026-09-07** (`docs/history/I8_I9_HANDOFF.md`; `promptcadence 1.2.0` published `57b1e58`; **1.3.0 published** the same day with K1 and K4) | **PromptCadence 1.2: a `settings` CLI verb** | Sonnet 5 · standard | I5 | Skipped deliberately at I5 (handoff §7) and scheduled at the I5 interview. `promptcadence settings list|get|set`, client-mode over the API the way `approve`/`deny` are — `$PROMPTCADENCE_API_TOKEN` or `--token`, the same refusals surfaced with their codes — so a setting can be changed from a script or over ssh without curl. Spec §7.2 gains the verb; `config show` keeps answering the read-side question without a server. **Read:** `apps/promptcadence/spec.md` §7.2, `promptcadence/services/settings.py`, `cli/commands/approvals.py` as the client-mode precedent | One CLI surface over a registry that already exists and is tested |
| **J1** — **done 2026-09-07** (`docs/history/J1_HANDOFF.md`; IdeaPress `c8f3fa6`, ADR-0103) | IdeaPress — adopts LoadLedger (IP-A1) **+ Commissioner (IP-A2)**; ships nothing itself, IdeaPress is already `1.1.0` — **J2 cuts `1.2.0`** carrying all three adoption phases | Sonnet 5 · high | I2 | **Two adoptions merged into one row on 2026-09-07** (the Commissioner half was scheduled as J2); roadmap §6.1 and §6.2 keep their exit conditions unchanged and are proved separately, in that order. They were merged because they land on the same surface: `services/workspace.py::_backend_summary` is where the ad-hoc egress flag lives (`"egress": ... mode != "ollama" and _is_remote(runtime)`) and is also where per-project cost and position are rendered; both edit `web/templates/workspace/index.html`'s badge row and `config.py`; and both otherwise pay the same mounting tax twice — one migration on the `0006_attempt_adapter_subject` chain, one `pyproject`+`ci.lock` move, one gate, one handoff. Neither package forces a pin move: `loadledger` needs `baseaicore>=0.4,<0.5` and `commissioner` `baseaicore>=0.4.1,<0.5` + `setspec>=0.5,<0.7`, both already satisfied by IdeaPress's ranges. **(1) LoadLedger.** Mount the tables, debit per attempt with `pricing_hash`; a per-output ceiling (`PER_RUN`, the unit id) and a per-project ceiling (`PER_TAG`, `project:<id>`, lifetime) from IdeaPress's configuration, resolved per operation (LoadLedger plan Phase 2); per-unit/per-project cost and position in the UI (`—` + reason for unpriced local; "at least" for a floor). Delete the in-app equivalent. **(2) Commissioner.** The S4 egress badge reads recorded decisions; denials visible in project history; badge backed by rows, not a flag. **Read:** `roadmap/promptcadence-roadmap.md` §6.1 + §6.2, `packages/loadledger/spec.md` §2 + §21, `packages/commissioner/spec.md`, ADR-0030, ADR-0069 | Third and fourth mounting by now; the shape is adopt-delete-prove, twice, over one page |
| **J2** — **done 2026-09-07** (`docs/history/J2_HANDOFF.md`; IdeaPress `0776f1e`…`40904c4` merged as `0136660`, ADR-0104; **`ideapress 1.2.0` published, `dcb2a17`**; 1.3.0 published the same day with K2–K4) | IdeaPress — adopts CutCtx (IP-A3); cuts **`1.2.0`**, carrying J1's two adoptions too (IdeaPress is already `1.1.0`, tagged and published — corrected 2026-09-07; scheduled as J3 before the 2026-09-07 merge) | Sonnet 5 · high | I2 | `project_review` context assembly as a policy chain; in-app reduction code deleted; golden-tested identical output on fixture projects. **Read:** `roadmap/promptcadence-roadmap.md` §6.3, `packages/cutctx/spec.md`  **Deliberately not merged into J1** (2026-09-07): it touches `domain/context_assembly.py` (348 lines) and `services/project_review.py` and neither of the files J1 edits, its "budget" is the *token* budget and not LoadLedger's money ceiling, and it is the one J row that deletes working code under golden parity — a delete that size does not belong inside a mount-and-debit commit. | Golden-parity refactor — the safest kind, but worth `high` since it deletes working code |
| **K1** — **done 2026-09-07** (PromptCadence `16a686f` + `c6842f1` on top of the prepared 1.2.0; docs `6a55d1c`; 1280 passed, 91.64 %) | **PromptCadence: read `output.tool_calls_assembled`, and negotiate LoadCoach's API version on first contact** | Sonnet 5 · high | I9 | Two obligations the 2026-09-07 ADR audit (`ADR_GAP_REVIEW.md`, workspace root) found undischarged. (a) ADR-0078 rule 5 named PromptCadence's migration to the assembled field "at its next touch"; four releases later `infrastructure/loadcoach.py` still reads `output.tool_calls` and regroups locally, so the grouping has two implementations. Read the assembled field, fall back to local assembly only when a pre-1.1 server omits it. (b) ADR-0013 and api-and-contract-standards §12 require a client to check compatibility on first contact and fail with `API_VERSION_UNSUPPORTED`; IdeaPress does, PromptCadence defines `version()` and never calls it. One `GET /version` per TTL, never per turn. **Read:** ADR-0078, ADR-0013, `apps/loadcoach/api.md` §1 + §4, IdeaPress's `infrastructure/backends/loadcoach.py` (transcribe, never import). | Two transcriptions of shapes that exist; the judgement is the fallback and the refusal site |
| **K2** — **done 2026-09-07** (IdeaPress `d3392a4` + `137eb7a` on top of the prepared 1.2.0; the drift check rides the existing `contracts` job — no repo has a job named `docs` except FreeWeight) | **IdeaPress: a committed OpenAPI snapshot, and a `ci.lock`** | Sonnet 5 · standard | J2 | ADR-0108 makes every application owe a committed `docs/openapi.json` compared byte for byte in CI; IdeaPress commits none and has no test over its own surface (`web/app.py` serves `/api/v1/openapi.json` from nine route modules). Add the snapshot, the test in the shape of LoadCoach's `tests/contract/test_openapi_snapshot.py`, and the CI `docs` job that fails on drift. Same row: IdeaPress is the one repository without `requirements/ci.lock` (its `requirements/README.md` says so and calls it tracked work) — compile one with the header command the other applications use, switch `ci.yml` to `--require-hashes` + `--no-deps`, prove it in a clean venv. **Read:** ADR-0108, `standards/testing-standards.md` §8.4, LoadCoach's `ci.yml` and `requirements/README.md` | Mechanical against two precedents |
| **K3** — **done 2026-09-07** (`docs/history/K3_HANDOFF.md`; IdeaPress `3c9ce91`+`9b965fd`, docs `493b0b0`) | **IdeaPress: `project_review`'s prompt gets a budget** | Sonnet 5 · high | J2 | J2's D3 finding (`docs/history/J2_HANDOFF.md` §8): `services/project_review.py::render_units()` concatenates every committed unit, unbudgeted, so a project with enough units exceeds the served context and fails at the provider rather than with IdeaPress's own numbers. Route it through the same `cutctx` chain J2 built, with the review's own untouchable set, a settings key for the budget, and the `context.compacted` report. Also from J2: the revise loop's `assemble_context` call passes no `neighbouring_units`/`research_notes`, so the report-emission branch is unreachable in production — decide whether that is the intended shape and say so. **Read:** ADR-0104, `packages/cutctx/spec.md` §7 + §11, `apps/ideapress/workflows.md` §7 | New behaviour with a new failure mode — not a parity refactor |
| **K4** — **done 2026-09-07** (`docs/history/K4_HANDOFF.md`, ADR-0110; LoadLedger `3d2bc71`+`52163ca` = **`loadledger 0.3.0`**, PromptCadence `c9c3dff`, IdeaPress merged as `e8dceb9`; **`ideapress 1.3.0` published, `f377048`**, after `loadledger 0.3.0` published and both applications' `ci.lock` moved to it) | **IdeaPress: `TokenUsage` gains the cache classes, and the pricing loader's second reader** | Opus 5 · standard | J1 | Two J1 findings (`docs/history/J1_HANDOFF.md` §8). (a) IdeaPress's own `TokenUsage` has no `cache_write_tokens`/`cache_read_tokens`, so every rendered cost is permanently "at least" (ADR-0069's floor) and never a total; add the two classes to the domain type, the `attempts` columns (one migration, ADR-0070 rule 4 spelling) and the debit, so a priced attempt can total. (b) `services/pricing.py` is a ~250-line near-copy of PromptCadence's loader — ADR-0072's "second reader" trigger has fired; decide extraction into a layer-3 home (LoadLedger is the candidate: it already owns `Money` and `pricing_hash`) or record why two copies stay, as an ADR. **Read:** ADR-0069, ADR-0070, ADR-0072, `packages/loadledger/spec.md` §7 | The extraction question is a boundary decision, the migration is routine |
| **L1** — **done 2026-09-07** (nine repos, one `docs(readme)` commit each) | **Every README states the current version and status** (M9 Group 1) | Sonnet 5 · standard | — | `M9_AUDIT.md` (workspace root, 2026-09-07) found eleven of fourteen root `README.md`s wrong: IdeaPress "specified, not yet implemented" at 1.2.0; CutCtx and ToolYard "nothing here is on PyPI yet"; FreeWeight `1.0.0rc1`, LoadCoach and PromptCadence `1.0.0`. Fix `README.md`, `docs/quickstart.md`, `docs/README.md` per repo against `__about__.py`, `pip index versions` and the changelog. | Highest visibility per hour on the list |
| **L2** — **done 2026-09-07** (docs `ed0063d`…`6af2c08`; ADR-0113 packages stay 0.x, ADR-0114 the dependency budget is the enumerated set; §11 review re-run, 12 broken links repaired) | **The documentation set restated over fourteen components, and the package-1.0 decision** (M9 Group 7) | **Opus 5 · high** | — | (a) **Decide package 1.0** as an ADR: `master-roadmap` §6 says packages reach 1.0 at M9; all ten sit at 0.x. Coordinator's recommendation, pending the operator: packages stay 0.x; a package reaches 1.0 when its API has survived two minors with no breaking change *and* the compatibility matrix (L6) proves both ends of every application's range — M9 does not bump ten packages. (b) Restate `master-roadmap` §7 and §9 and `gold-standards` §4 over fourteen components and four applications. (c) A PromptCadence column in `architecture/graceful-degradation.md`, and an index from every row to its test. (d) Reconcile `gold-standards` §1.1's runtime dependency budget with what the four applications and MirrorWall actually declare (FreeWeight 10, IdeaPress 10, LoadCoach 9, PromptCadence 9, MirrorWall 3) — an ADR either way. (e) Re-run and re-date `docs/README.md` §11's consistency review. Then mirror. **Read:** `M9_AUDIT.md` Group 7, ADR-0011, ADR-0013, `standards/packaging-and-release-standards.md` §3 | Two architect's calls and a review that will find real drift |
| **L3** — **done 2026-09-07** (`docs/history/L3_HANDOFF.md`; three commits per application; found and fixed: IdeaPress and PromptCadence never detected a schema-ahead database, FreeWeight and LoadCoach never named the backup directory) | **Operations promises get their tests** (M9 Group 3) | Sonnet 5 · high | K3 + K4 (IdeaPress and PromptCadence trees free) | Per application: `tests/integration/test_downgrade_and_schema_ahead.py` — upgrade to head, write a row, `db backup`, set `alembic_version` ahead, assert the bootstrap raises `SchemaAhead` naming both revisions and the backup directory, `db restore`, assert the older revision starts; parametrize the existing backup/restore tests over `temporary_sqlite`/`temporary_postgres`; IdeaPress gets its backup/restore test from scratch; one `.sqlite3` fixture per released version in each application's migration test (FreeWeight's is from `1.0.0rc1` only; the other three start from empty). **Read:** `M9_AUDIT.md` Group 3, `standards/packaging-and-release-standards.md` §6.1, `standards/database-standards.md` | Four named 1.0 promises with no test behind them |
| **L4** — **done 2026-09-07** (fourteen `chore(ci)` commits; SweatMeter and IdeaPress already scheduled their nightlies inside `ci.yml`) | **CI job parity across fourteen repositories** (M9 Group 4) | Sonnet 5 · standard | — | Copy ModelRack's `nightly.yml` (performance + live) into the ten repositories without a schedule; `diff-cover --fail-under=90` in the twelve `coverage` jobs that lack it; MirrorWall's `--cov-fail-under=85` → `95` (its `pyproject` floor); each application's `install-check` also runs `<app> --version`, `python -m <app> --version`, installs `dist/*.whl[postgres]`, and `pipx install`s the wheel; FreeWeight's extra is `[postgresql]` where the other three and packaging §10 say `[postgres]` — alias it and document the canonical name. **Read:** `M9_AUDIT.md` Group 4, `standards/packaging-and-release-standards.md` §5 + §10. | Mechanical and high-leverage: ten repos start measuring what they assert |
| **L5** — **done 2026-09-07** (`docs/history/L5_HANDOFF.md`; PromptCadence api/data-model/risks docs, README compatibility tables with drift tests, SHA256SUMS, upgrade guides, doctor/troubleshooting and help tests, `pydantic-settings` removed from all four) | **The documents the checklist names and the repositories lack** (M9 Group 5) | Sonnet 5 · high | L2 | `docs/apps/promptcadence/api.md` (+ `data-model.md`, `risks.md`) and PromptCadence's `CONTRIBUTING.md`; a `## Compatibility` table in each application README generated from `pyproject.toml` with a test that it cannot drift; `sha256sum dist/* > SHA256SUMS` before `action-gh-release` in the four application `release.yml`s; the four `docs/upgrading.md` refreshed to the shipped versions; FreeWeight's `test_troubleshooting_covers_doctor.py` ported to the other three; a security-checklist test in IdeaPress; one `test_every_command_has_help` per application walking the Typer tree; the help/about page question settled against `ui-ux-standards` §12 first. **Also from L2 (2026-09-07):** `pydantic-settings` is declared by all four applications and imported by none — remove it from each `dependencies` and recompile the locks (ADR-0114 lists it as owed removal). **Read:** `M9_AUDIT.md` Group 5, ADR-0114 | Independent pieces; can be split |
| **L6** — **done 2026-09-07** (`docs/history/L6_HANDOFF.md`; docs-repo commit(s) only — `.github/workflows/compatibility-matrix.yml`, `scripts/compatibility_matrix.py`, `architecture/performance-results.md`, packaging §7; local proof run on `promptcadence` lowest/highest; found that every application's own declared `mirrorwall>=0.2,<0.3` floor resolves to `mirrorwall 0.2.0`/`0.2.1`, both of which cap `setspec<0.5` — below every application's own `setspec` floor — so no application's `lowest` cell can install today; fix (`mirrorwall>=0.2.2,<0.3` in all four apps) is a follow-up row) | **The two suite-wide jobs** (M9 Group 6) | Sonnet 5 · high | L2 + L4 | The cross-repository compatibility matrix (packaging §7): a nightly workflow (home: the docs repo or a new `suite-ci`) over four applications × {lowest, highest} pin of every suite package, clean venv, `pytest -m "contract or e2e"`. It is load-bearing: FreeWeight floors `setspec>=0.6` and `modelrack>=0.7` where IdeaPress floors `setspec>=0.5` and `modelrack>=0.5,<0.6`, and nothing proves any lower bound resolves. Plus `architecture/performance-results.md` from one nightly run, with the reference machine's `machine_fingerprint` beside the numbers. **Read:** `M9_AUDIT.md` Group 6, packaging §7, `architecture/performance-targets.md` | Needs L4's nightlies and L2's version decision first |
| **M1** — **done 2026-09-08** (`docs/history/M1_HANDOFF.md`; docs `7af3ff4`, IdeaPress `b2a7b2c`..`c145dbe` with `ideapress 1.4.0` prepared untagged/unpushed; ADR-0116) | **IdeaPress: the `research` stage runs its tools under ToolYard** (ToolYard's second consumer) | **Opus 5 · high** | L6 | Decided at the 2026-09-07 interview: ToolYard stays a package — the one place side-effecting tool calls run, with a refusal order and containment a second application must not reinvent (ADR-0053) — on condition that its second consumer gets a date; this row is that date. IdeaPress's `research` stage has been specified since M6 (`apps/ideapress/workflows.md` §2) and never built. Build it on ToolYard: `http_fetch` and `read_file` under the executor's validate → authorize → execute → record discipline, `TieredSandbox` per stage runner, a `ToolCallStore` implementation on IdeaPress's own tables (mount, never join — ADR-0050 for the shape), the stage's allowlist from configuration, every refusal a `ToolResult` on the unit's provenance table, Commissioner deciding egress before the fetch (ADR-0073), and the research notes flowing into `assemble_context` — which reopens J2's dead `research_notes` path on purpose. Exit: a project whose brief names a URL produces notes from it, with the call recorded; a disallowed host is refused as a result, visible on the unit. **Read:** `packages/toolyard/spec.md` §§7, 11, PromptCadence's `services/tools.py` as the precedent, `docs/history/E4_HANDOFF.md`, ADR-0053, ADR-0056, ADR-0111. **Built as ADR-0116**, on the operator's 2026-09-07 override of the schedule note below: `[research]` with every default closed, migration `0010`'s `tool_call_records`, one `ToolExecutor` over `PathContainment` per stage run (not `TieredSandbox` — neither shipped tool runs a subprocess), `http_fetch` registered **only** when a host is named because ToolYard reads an empty allowlist as loopback, a Commissioner verdict per host before the fetch enforced by the invocation's egress ceiling, notes written as `sources` rows (the table `fact_check` has read since P1 and nothing wrote), and `assemble_context` receiving `research_notes` for the first time. Exit demonstrated on a real loopback origin through the shipped CLI: a note with its citation, a refused host and a denied verdict each leaving both an `egress_decisions` row and a `tool_call_records` row with no exception, and an unconfigured install refusing a URL with `unknown_tool`. Full gate green, Python 3.13.15, 1221 passed / 6 skipped, coverage 89.15 %, lock proved in a clean venv. | New behaviour with a sandbox and an egress decision in it; never overnight |
| **L7** — **done 2026-09-07** (`docs/history/L7_HANDOFF.md`; fifteen commits, one per repository; README-version drift guard in all fourteen) | **M9 residue, the mechanical half** (`M9_REAUDIT.md` items I5, D1, D9, Q6's floor, and the nits) | Sonnet 5 · standard | L6 | (1) IdeaPress `baseaicore>=0.4.2,<0.5` — its `modelrack>=0.7` floor already requires it, so the declared lowest range cannot resolve; README table and lock follow. (2) `docs/upgrading.md` in all four applications carries every published version (LoadCoach 1.1.4/1.1.5, IdeaPress 1.3.1/1.3.2, PromptCadence 1.3.1/1.3.2, FreeWeight 1.1.1/1.1.2 — dependency-only patches get a one-line row saying so). (3) CutCtx and ToolYard READMEs gain `## Install` with the PyPI line and a `docs/quickstart.md` in the shape of LoadLedger's. (4) A README-version drift guard in every repository: `tests/unit/test_readme_version.py` asserting the version the README states equals `__about__`, so the next patch cannot leave eleven READMEs stale again; fix the eleven now. (5) IdeaPress `ci.yml` installs the `[telemetry]` extra in the job that tests it; IdeaPress `nightly.yml` (or its `ci.yml` schedule) gains the `live` job it lacks. (6) FreeWeight's `db-matrix` job sets `WEIGHTSDB_REQUIRE_POSTGRES=1`/`WEIGHTSDB_TEST_DSN` beside its `FWTEST_*` variables so the both-dialects backup test runs its PostgreSQL leg instead of skipping. (7) Nits: `standards/gold-standards.md` §1.1 no longer lists `pydantic-settings` as owed; `docs/README.md` §11 says four application API documents; `outstanding-work.md` §5's M9 row says fourteen. **Read:** `M9_REAUDIT.md` | Every item is a precedent copied into a repository that lacks it |
| **L8** — **done 2026-09-08** (`docs/history/L8_HANDOFF.md`; dependency-set tests in eleven repos, earliest-release fixtures, nine degradation tests, stubs implemented or deleted; two cells stay untested because the behaviour is unimplemented — rows M2 and M3) | **M9 residue, the tests** (`M9_REAUDIT.md` items Q1/G16, O1, O6) | Sonnet 5 · high | L7 | (1) G16: a dependency-set test in the eleven repositories without one (`tests/unit/test_packaging.py` asserting `pyproject.toml`'s runtime `dependencies` equals the enumerated set ADR-0114 / gold-standards §1.1 records — BaseAiCore, CutCtx and ToolYard have the precedent). (2) O1: a `.sqlite3` fixture from each application's **earliest published** release that has a schema (IdeaPress 1.1.0, PromptCadence 1.1.0, FreeWeight 1.0.0, LoadCoach already has 1.0.0) so the migration test actually migrates something, captured the way L3 did. (3) O6: the ~15 `untested` cells in `architecture/graceful-degradation.md` §2.1's row→test index get tests — disk full (all four), database locked (FreeWeight), migration failure (IdeaPress, PromptCadence), API major version (IdeaPress), IdeaPress's three machine rows — each proving the documented behaviour; update the index; then G20 can be a CI gate. (4) Twelve TODO-stub test files that collect zero tests (IdeaPress `tests/e2e/test_full_project_journey.py` + three in `tests/unit/`, FreeWeight `tests/e2e/test_full_journeys.py`, four in MirrorWall, two in SetSpec — found by the walkthrough guide): delete each, or make it collect the journey it names. **Read:** `M9_REAUDIT.md`, `architecture/graceful-degradation.md` §2.1, `docs/history/L3_HANDOFF.md` | Real tests of documented failure behaviour; the disk-full cases need care |
| **M2** — **done 2026-09-08** (`docs/history/M2_HANDOFF.md`; docs `2e1b38c`/`f037d50`, LoadCoach `733f4e0`/`78f8f63`, all unpushed) | **LoadCoach's `FreeWeightClient` negotiates FreeWeight's API version on first contact** | Sonnet 5 · standard | L8 | ADR-0013 and api-and-contract-standards §12 oblige every client; PromptCadence's got its check at K1 and IdeaPress's has had one since M8; LoadCoach's evidence client has none, so the degradation matrix's "incompatible API major version" cell for LoadCoach is `untested — behaviour not implemented` (L8). Transcribe PromptCadence's `LoadCoachClient.version()` TTL shape (`c6842f1`), refuse with the ADR-0013 code, one `GET /version` per TTL, tests for compatible/incompatible/cache. **Read:** ADR-0013, `docs/history/L8_HANDOFF.md`, PromptCadence `infrastructure/loadcoach.py`. `FreeWeightClient.version()` negotiates before every FreeWeight-specific pull (the CLI, the route, and `refresh_from_freeweight`) rather than inside `fetch()` itself, which stays byte-for-byte shared with ToolYard's `http_fetch` vectors; a served major excluding `v1`, or a 404 on `/version`, is `EvidenceSourceIncompatible` (`API_VERSION_UNSUPPORTED`), never cached; an unreachable FreeWeight still fails as before. `graceful-degradation.md`'s last `untested — behaviour not implemented` cell is closed. Full gate green, Python 3.14.4, 1063 passed / 5 skipped, coverage 90.81 %; loadcoach 1.2.0 release-committed, untagged. | The third transcription of one shape |
| **M3** — **done 2026-09-08** (`docs/history/M3_HANDOFF.md`; docs `43a9cd4`, IdeaPress `95813c1`, both unpushed) | **IdeaPress's telemetry bar: wire it or remove it from the documents** | Sonnet 5 · standard | L8 | `show_telemetry_bar` is hard-coded `False` and nothing renders IdeaPress's three machine rows of the degradation matrix (L8). Decide: either wire the bar to the existing SweatMeter reader the way LoadCoach's `base.html` does (then the three rows get their tests), or delete the rows and the flag and say in `apps/ideapress/spec.md` that IdeaPress shows no machine telemetry because it does not schedule work on the machine — the honest answer for an application that routes through LoadCoach. **Read:** `architecture/graceful-degradation.md` §2.1, `apps/ideapress/spec.md` §15, LoadCoach's telemetry bar. **The decision was already taken before this row ran** (ADR-0115, removal) — the row implemented it rather than choosing: `show_telemetry_bar` and its dead comment are deleted from `web/rendering.py` (no template ever branched on it), `graceful-degradation.md`'s three IdeaPress cells read `n/a — ADR-0115` in both §2 and §2.1 with footnote ⁴ dropped, `spec.md` §5 and §16 state the decision and keep the `[telemetry]` extra's real purpose (the VRAM-preflight presence probe), and a new e2e assertion proves the rendered page carries no `telemetry-bar` markup. The extra was kept (conservative default): `doctor`'s `telemetry` finding is a tested, user-visible capability flag, so `pyproject.toml`, `README.md` and the CI install-check comment were only re-worded, not removed. Full gate green, Python 3.13.15, 1164 passed / 6 skipped, coverage 88.68 %. | A decision with two small implementations |
| **N1** | **Docs: reconcile the application dependency diagrams and drop `pydantic-settings` from four specs** | Sonnet 5 · standard | M1 | `architecture/master-architecture.md` §2's graph still reads `IP --> MR & WD & MW & SS`; IdeaPress has depended on `cutctx`, `loadledger`, `commissioner` (J1/J2) and now `toolyard` (M1, ADR-0116). `architecture/executive-summary.md` line ~94 is short by `toolyard`. Reconcile both diagrams for all four applications at once against each `pyproject.toml`'s runtime set (ADR-0114), via an amendment record if master-architecture's freeze requires one. Same commit: `apps/<app>/spec.md` §5's third-party lists still name `pydantic-settings`, removed from every application by ADR-0114 decision 4 — one-word deletion in four specs. Mirror. **Read:** `docs/history/M1_HANDOFF.md` §8 items 2–3, ADR-0114, ADR-0116 | Documentation only, but four repositories' mirrors move |
| **N2** | **IdeaPress: the unit page renders the per-attempt egress decision, and `research` gets a web route** | Sonnet 5 · standard | M1 | (1) `unit_detail` has carried `attempts[].egress` since J1; the Provenance table has no column for it — render it (M1 handoff §8 item 5). (2) `ideapress research` runs only from the CLI (M1 decision D10, the largest surface left out unattended): a CSRF-protected form on the project page starts the stage, with the decision recorded in the spec that a browser click may cause a network fetch only to an allowlisted host; regenerate `docs/openapi.json`. Rides the next IdeaPress minor. **Read:** `docs/history/M1_HANDOFF.md` §4 D10, §8; ADR-0116; `docs/history/J1_HANDOFF.md` | Small surface; the fetch-from-a-click decision wants a person's eye |
| **N3** | **ToolYard: `http_fetch_tool` can be constructed closed** | Sonnet 5 · standard | M1 | `http_fetch_tool([])` means loopback-only, so both consumers withhold the tool from the registry to mean "no host" (PromptCadence E4 decision 1, IdeaPress M1 D2) — two consumers with the same three-line workaround. Either an explicit closed construction or a documented statement that the loopback reading is deliberate; `toolyard 0.1.2` if the API moves, then both consumers drop their workaround in their next patch. **Read:** `docs/history/M1_HANDOFF.md` §8 item 1, ADR-0116 "Revisit when", `packages/toolyard/spec.md` §7 | One small API decision in a package with two consumers |
| **N4** | ModelRack **0.8.0** → `llama-server` launched under a host-memory cap | Sonnet 5 · standard | — | `LlamaCppProvider(memory_max_bytes=, memory_high_bytes=)`, keyword-only, `None` default; when set the launcher prefixes argv with `systemd-run --user --scope -p MemoryMax= [-p MemoryHigh=] -p MemorySwapMax=0`; `systemd-run` missing while a cap is set is a typed launch error naming both, never a silent uncapped launch; the orphan sweep still finds the child by pid. Unit tests over a fake launcher asserting the exact argv prefix; one live test that fires the cap on purpose and sees a dead server, not a hung host. **Read:** ADR-0119, `MEMORY_SAFETY.md` §§1–3.1, `packages/modelrack/spec.md` (llama.cpp process supervision), ADR-0062 | One argv prefix and one refusal, against a wrapper whose semantics are the kernel's |
| **N5** | FreeWeight **1.2.0** → `[runtime] flash_attention`, `kv_cache_precision`, `fit_to_device`; `[benchmarks] max_fit_context_tokens`; `[provider] memory_max_bytes` | Sonnet 5 · high | N4 | (1) The two profile keys reach `to_profile()` and the existing `runtime_profiles` columns — no migration; `q8_0`/`q4_0` refuse without `flash_attention = true`; either key with `provider.kind = "ollama"` is refused at startup by name. (2) `fit_to_device` (default `false`) sets `provider_options["--fit"] = "off"` — existing hashes unchanged because the key was absent. (3) `max_fit_context_tokens` truncates `MAX_FIT_CONTEXT_TOKENS` like `long_context_max_tokens` truncates its ladder, hashed into `dataset_hashes`; `max_context_capped_by_configuration` reads it. (4) `provider.memory_max_bytes`/`memory_high_bytes` pass to N4's constructor. Regenerate `configuration.md`; rewrite the `[runtime]` docstring that explained the keys' absence; CHANGELOG. **Read:** ADR-0120, ADR-0121, `MEMORY_SAFETY.md` §3.2, `apps/freeweight/benchmark-catalog.md` §3.2, `benchmarks/long_context/benchmark.py` as the truncation precedent | Validation rules with named refusals and a hash that must not move — a checklist a person verifies against the stored hashes |
| **N6** | LoadCoach **1.3.0** → per-model `kv_cache_precision` and `flash_attention`; `memory_max_bytes` on llama.cpp registrations | Sonnet 5 · standard | N4 | `RuntimeModelOverride` gains the two keys, merged by the existing `_merge`; the three rules of ADR-0120 evaluated at resolve time, with an Ollama registration's refusal surfacing in `route explain` as a named rejection; `loadcoach models show` renders the resolved precision and flash-attention state. `--fit` stays at llama-server's default (ADR-0121 §3). Registration gains `memory_max_bytes`/`memory_high_bytes` for N4. Regenerate `configuration.md`; CHANGELOG. **Read:** ADR-0120, ADR-0121 §3, `MEMORY_SAFETY.md` §3.3, `domain/routing/subject.py`, ADR-0023 | Two fields through an existing merge and one rejection through an existing explanation |

### 1.1 M9 — Suite 1.0, outside the arcs

One outstanding milestone predates both arcs and appears in neither: **M9, the professional
delivery checklist** ([master-roadmap §7](master-roadmap.md)) — written over the nine pre-arc
components, and **declared over all fourteen since 2026-09-07**: the five arc components shipped
in the meantime and the checklist is exactly the walkthrough the operator does next.
It neither gates nor is gated by any row above — it can run any time, in parallel with anything.
Suggested treatment: one **Opus 5 · high** audit session that walks §7 against the repos as they
stand and emits a gap list (many boxes are likely already true and just unchecked), then ordinary
**Sonnet 5 · high** sessions per gap (docs generation, backup/restore and migration-path tests,
compatibility matrix); the declaration itself is a human step. Note for the A1 session: master
roadmap §9's "current state" is frozen at end-of-M7 and should be corrected to the true state
(all nine components tagged and published) in the same pass that adds the M10–M13 pointer row.

**M9 declared 2026-09-08.** The professional delivery checklist
([master-roadmap §7](master-roadmap.md), restated over fourteen components on 2026-09-07 by row
L2) is complete: all 32 boxes are met, save the package-level cells of
[`performance-results.md`](../architecture/performance-results.md), which are blank until a
nightly `performance` job has fired in the ten repositories that gained a schedule at row L4 —
the document's §5 states plainly that a blank cell means "not yet measured", and no session can
close it — and ToolYard's podman rung, waived by
[ADR-0111](../adr/0111-the-container-rung-is-proved-on-docker-and-podman-is-not-an-exit-condition.md).
Two degradation-matrix cells whose documented behaviour is not implemented are scheduled rather
than counted (rows M2 and M3). Suite 1.0 is declared over **fourteen components at the versions
they hold**: the four applications at `1.x` on ten `0.x` packages, deliberately and pinned, per
[ADR-0113](../adr/0113-packages-stay-0x-at-m9-and-1-0-is-earned-per-package.md) — M9 bumps no
package to 1.0, and each package earns its own on the criteria that record sets. The evidence is
rows L1–L8 and their handoffs, the audits `M9_AUDIT.md` and `M9_REAUDIT.md` (workspace root),
and the first fully green run of the cross-repository compatibility matrix (docs repository,
run `34187778093`, 2026-09-08), which proves every application's declared range for every suite
package resolves and passes its contract and e2e suites at **both** ends, and that the four
applications co-install: freeweight 1.1.2, loadcoach 1.1.6, ideapress 1.3.3, promptcadence 1.3.2.

## 2. The standing preamble for every kickoff prompt

Every session's prompt, regardless of row, should state:

* The component directory to work in (never the workspace root), its venv, and
  `pip install -e ".[dev]"` if the repo is fresh.
* The finish line: `ruff format --check . && ruff check . && mypy src tests && lint-imports &&
  pytest -m "not live and not performance"` green, `CHANGELOG.md` updated, one Conventional
  Commit per phase. Name the interpreter in the report (M5C-13).
* The house method: docstring-first, `from __future__ import annotations`, units in names,
  keyword-only optionals, injected clocks/providers/roots.
* Read `architecture/master-architecture.md` §§1–3 and the component's gold-standards section
  before the row's reading list.
* New packages pin unpublished suite deps as local path/editable installs with a
  `TODO: re-pin on publish` until the PyPI release lands.
* Overnight rules ([model-assignment §2.12](model-assignment.md)): Sonnet rows run at effort
  **high** overnight; **batches D and G and I2's security half never run overnight** — they are
  won by review and fail silently.

## 3. Which orderings are load-bearing

Hard edges — never reorder across these:

* **A1 before all code** — ADRs before implementation, the standing rule.
* **B1 before B3** — the payload publishes before its consumer pins it (the FreeWeight-P11 lesson).
* **B5 before H4** — a bundle cannot carry adapter evidence until the bundle has a minor that
  nests it; the same rule, one payload out (ADR-0068 rule 5).
* **C5 before D3** — the usage rule (ADR-0070) before the third adapter is written; a retrofit
  is a fixture re-annotation nobody wants.
* **C6 before F1** — all four token classes on LoadCoach's wire before the harness prices them;
  without it a strict money ceiling trips on every remote turn (ADR-0069 §"Not decided here").
* **D1/E2 before E4** — no tool executes in PromptCadence before the discipline is published
  (a security ordering).
* ~~**E5 before F2 and H4**~~ — **satisfied 2026-09-04 for F2.** `mirrorwall 0.2.1` pinned
  `setspec<0.5` and every application depends on mirrorwall, so no application could reach
  `governance.egress_decision` (setspec 0.5, needed at F2) or `capability.evidence` `1.1`
  (setspec 0.6, needed at H4) until MirrorWall widened and published. It has; PromptCadence,
  IdeaPress and FreeWeight are swept, and **F2 is unblocked** — `pip install -e PromptCadence
  commissioner==0.1.0` resolves, to `setspec 0.5.0`. LoadCoach's own pin remains the exception:
  it rides H4, because moving it turns three evidence-golden tests red until H4 adopts
  `CapabilityEvidenceV1_1Out`, **so H4 is still gated on that adoption, not on MirrorWall.**
  **Note for every later PromptCadence row:** `commissioner 0.1.0` pins `setspec>=0.5,<0.6`, so
  adding it at F2 pinned the resolved `setspec` to 0.5.x even though PromptCadence's own range
  admits 0.6. That costs nothing today — nothing in PromptCadence consumes a 0.6 payload — but it
  is the same shape as the `mirrorwall<0.5` cap this row removed. **Scheduled: H4 widens
  Commissioner to `setspec>=0.5,<0.7` and publishes `commissioner 0.1.1`**, alongside LoadCoach's
  own pin, so that all the 0.6 movement happens in one row. Until then PromptCadence resolves
  0.5.x by design, and reading that as a regression is the mistake this note exists to prevent.
* **PromptCadence P1→…→P9 strictly in order**; each package's phases in order.
* **H2 before I2's live-remote verification** — both touch LoadCoach; land LA2, then verify once.
* **J-rows after I2** — adoption targets a released 1.0, never a moving target (ADR-0011).
* **I4 before I5** — a hard edge on version numbers, not on code: I4 ships `promptcadence 1.0.1` and I5 ships
  1.1.0, so running I5 first leaves no 1.0.1 to cut short of a maintenance branch off `v1.0.0`, which nothing
  in this suite has ever done. Reverse the order and I4 becomes 1.1.1; say so in the row rather than leaving
  two rows claiming descending versions. The two rows were renumbered on 2026-09-06 so the table order and the
  numbering agree — the runtime-settings row was scheduled as I4, the snapshot row as I5 (I2 §14, I3 §10).
* ~~**I4 before J1**~~ (the 1.0.1 row; filed as "I5 before J1" before the renumbering) — **soft, and the stated reason does not survive reading.** It was filed hard on the
  grounds that a J row would otherwise open against a red contract suite, but the J rows are *IdeaPress* rows —
  LoadLedger tables, the Commissioner badge, CutCtx assembly — and the two vendored snapshots live in
  PromptCadence's own test suite, which no J row runs. Keep I4 early regardless, because it is small and I5
  waits on it. Restore the edge only if the practice is a workspace-wide gate before adoption work, and then
  say that is the reason (reviewed 2026-09-06).
* **I3 before J1** — the J rows adopt a PromptCadence whose remote-provider fact must be readable from a real LoadCoach (ADR-0098's correction); the two-line render lands first. **Satisfied 2026-09-06**: `loadcoach 1.1.1` renders `provider_name` and `is_remote` on every `/models` entry (LoadCoach `8a57472`), prepared but not yet tagged or published — a J row reading the fact from a *published* LoadCoach still waits on the operator's publish.
* **J1 before J2** — soft, and only because J1 cuts no release and J2 cuts `ideapress 1.2.0` (corrected 2026-09-07: IdeaPress is already `1.1.0`, so a J row cannot cut that version again). The two share no file (§ the J2 row), so the order is a version-numbering convenience, not a code edge: run J2 first and it has to cut 1.2.0 with one of the three adoption phases missing, which is not what M13's exit says.
* **G2 before I2's sandboxed-tool verification** — spec §20 #2's *sandboxed tools* clause needs
  tool definitions on LoadCoach's wire; until then it is exercised against the fake only, and
  the beta shipped saying so (G1, 2026-09-04). **Satisfied 2026-09-05 (G2):** the clause holds on
  the real stack — six declared sandboxed calls, all `ok`, across a planned and a bypassed
  trajectory on gpt-oss:20b — so I2 now verifies a path that runs rather than one that never has. **G3 before I1** — P8's `materialize(rows) ==
  compose_live(rows)` golden must see step attempts in the shape the record will keep.

* **F4 before G1** — G1 builds the M11 beta's dashboard, which spec §17 says shows "today's
  ledger position against the daily ceiling and against each configured project's ceiling". That
  works today through F1's reference-run workaround; the per-tier half of it does not, and a
  dashboard shipped in a beta with a column reading "1 debit(s) recorded" is a column somebody has
  to explain. Soft edge, not hard: nothing in G1 fails without it.
  **Status 2026-09-04: satisfied.** F4 is done end to end — `loadledger 0.2.0` is published and
  PromptCadence consumes it, so `--scope tier` reports spend and the count column is gone. G1
  builds its per-tier column against `tiers[].tokens_spent` / `money_spent` and the rendered
  strings beside them; `docs/history/F4_HANDOFF.md` §11 lists what it must not relitigate.

Everything else is convenience. Genuinely flexible rows, placed for model adjacency: **D3/F3/H1**
(the ModelRack stream can slot anywhere after C5, whenever harness work blocks), **E1** (only
I1 needs it), **B1** (only B3 needs it), **B5** (only H4 needs it), **H3** (any time after H2),
**F4** (any time after F1, but see the soft edge above). One resource rule: LA1/LA2
GPU sessions never share the machine with FreeWeight benchmark runs.

## 4. Not model sessions — the human/ops checklist

* Create the five repos (cutctx, toolyard, loadledger, commissioner, promptcadence) with the
  standard toolchain + CI; reserve the PyPI names; mirror each component's docs byte-identically
  on repo creation.
* Per release: tag, Release-workflow `pypi` environment approval, post-publish install check.
* **Prepared overnight 2026-09-08, untagged and unpushed, awaiting the operator:** `loadcoach 1.2.0`
  (`78f8f63`, row M2 — FreeWeight API-version negotiation) and `ideapress 1.4.0` (`c145dbe`, rows
  M3 + M1 — telemetry bar removed, `research` stage on ToolYard). Read `docs/history/M1_HANDOFF.md`
  §4 before tagging IdeaPress: fourteen decisions were taken unattended, and the fail-closed
  denial of a remote host with no `[research] max_data_classification` is the first thing a new
  user hits (two-line fix in IdeaPress `docs/upgrading.md`). Push `docs` first, then each app.
* ~~**`mirrorwall 0.2.2` is prepared and waiting**~~ — **done 2026-09-04.** Pushed
  (`6d50c3f..092af6e`), tagged `v0.2.2`, the `pypi` environment approved by the operator, and
  `mirrorwall 0.2.2` is on PyPI. Post-publish install check passed in two clean Python 3.13.15
  venvs, the second being the proof the cap is gone: `mirrorwall==0.2.2` and `setspec==0.6.0`
  install together. The pin sweep then ran — see the E5 row. **One thing this step taught, for
  every future release:** a single `git push origin <tag>` fired **two** identical Release runs
  one second apart, both waiting on the same environment. Approve exactly one and cancel the
  other; the second to reach `twine upload` fails on *file already exists* and leaves a red run
  against the tag for nothing.
* Review the morning diffs for every overnight batch; review Fable batch output same-day.
* After D1 (done and reviewed 2026-09-03, unpushed): push ToolYard `main` and docs `main`, confirm
  CI green (the `isolation` tests skip on the runner — a skip, visible, not a pass).
* ~~**Podman is unverified and is now a blocking item for M11.**~~ **Closed 2026-09-07 by [ADR-0111](../adr/0111-the-container-rung-is-proved-on-docker-and-podman-is-not-an-exit-condition.md):** M11's exit is the container rung exercised on the reference machine's runtime (docker, done at E4); the podman rung stays unexercised and says so, and the first podman host runs `pytest -m isolation -rs` before anything else. The original text follows for the record. It is *first* in ToolYard's ladder,
  so it is the rung that will actually run commands on any podman host, and no canary has ever run
  on one: `E2` published `toolyard 0.1.0` without it (the step was written before E2 and did not
  block it), and E4 exercised the container rung through **docker** on the reference machine.
  `pytest -m isolation -rs` on a real podman host, green, is an M11 exit condition — a skip is
  visible, and it is not a pass.
* GPU sessions on the reference machine for the LA1 exit (I16 zero-base-loads run), the LA2 exit
  (the three-stage one-base-load demo), and H4's serving-mode A/B — never beside a FreeWeight
  benchmark.
* Re-run the IdeaPress standalone journey and read the output (the M8 leftover) before IdeaPress
  1.1 work begins.
* After D2 (done 2026-09-03, unpushed): push LoadCoach `main` (`846348b`, the `finish_reason`
  render), PromptCadence `main` and docs `main`; confirm CI. After E4 ships the five harness
  profiles: run `pytest -m live` and `promptcadence run "…" --bypass-planning --follow` against a
  LoadCoach on **Ollama** with the shipped defaults and no `PROMPTCADENCE_TIERS__*` overrides —
  the fake provider declares `stop` for every answer, so `length` and `content_filter` on a real
  model are unexercised until this run. **Done at G1, 2026-09-04**, with the shipped defaults:
  bypassed and tool-free planned trajectories completed; `length` and `content_filter` are
  **still** unexercised — the model exercised `tool_calls` with invented names instead (G2).
  **After G2 (2026-09-05)** the model, told which tools exist, emits well-formed calls and no
  invented names; `length` was finally observed, on the **planning** call rather than an executing
  turn — every empty `tools.plan` answer comes back `done_reason=length` with `eval_count` equal to
  `max_output_tokens` (G2 gate E). `content_filter` remains unexercised.
* **After G1 (done 2026-09-04; `docs/history/G1_HANDOFF.md` §12), in this order — it is this package's
  first release, so §6 of the packaging standard applies in full:** (1) push PromptCadence
  `main` (the gate commits, the release commit and `952af18`, the release plumbing that gave
  `release.yml` the `pypi` environment, the TestPyPI dry run and the hashed build chain) and
  docs `main`; (2) on PyPI **and** TestPyPI add a *pending* trusted publisher for
  `promptcadence` — owner `JPKell`, repository `PromptCadence`, workflow `release.yml`,
  environment `pypi` on PyPI and blank on TestPyPI — and create the `pypi` environment in the
  GitHub repository with yourself as required reviewer; (3) Actions → Release → *Run workflow*
  for the TestPyPI dry run and confirm `promptcadence 0.9.0b0` on test.pypi.org; (4) tag
  `v0.9.0b0` (annotated, `PromptCadence 0.9.0b0 — the M11 beta`) and push the tag; approve the
  `pypi` environment **exactly once** (the mirrorwall lesson above — one tag push fires two
  Release runs); (5) `pip install promptcadence==0.9.0b0` in a clean venv, then
  `promptcadence doctor`. The first publish claims the unreserved PyPI name. The podman item
  below is still open and is not carried by this release.
* After D3 (done 2026-09-03, pushed, CI green; `docs/history/D3_HANDOFF.md`): review
  the diff same-day (handoff §11 and §13). llama.cpp `b10792` is **installed** on the reference
  machine (`~/ai/tools/llama.cpp`, CUDA 13.1, `~/.local/bin/llama-server`; a user-local CUDA
  include overlay works around the CUDA-13.1/glibc-2.43 `rsqrt` clash, llama.cpp #19100) and the
  live journey has **run and passed** — load, generate, stream with a shared prefix reported as
  `cache_read`, native completion, unload with no process left. Re-run it after any llama.cpp
  upgrade: `MODELRACK_LLAMACPP_MODELS=~/ai/models/llm MODELRACK_REQUIRE_LLAMACPP=1 pytest -m live
  tests/live/test_llamacpp_live.py -rs -s`. ModelRack stays at `0.6.0` until H1 — and, after H1's first pass on 2026-09-05, **still does**: gates A–F landed but LA1's exit could not be run for want of adapter artefacts, so `0.7.0` is unbumped and unpublished (see the H1 row). **Second sitting, same day: gate G passed live and `0.7.0` is bumped and release-committed, awaiting the operator's tag and publish** (`docs/history/H1_HANDOFF.2.md`). The exit found a per-adapter-switch host-memory leak in the b10792 CUDA build with CUDA graphs on; ModelRack disables them by default on adapter launches (spec §18).
* **After H4 (done 2026-09-06):** `docs`, `FreeWeight` (`43f98ca`…`cbe59ed`) and `py/Commissioner` (`ad1b248`) pushed; `v0.1.1` tagged and `commissioner 0.1.1` published — approve the `pypi` environment **exactly once** (the mirrorwall lesson above). Post-publish: `pip install commissioner==0.1.1` in a clean venv, and repeat H4 §8's PromptCadence resolution check against PyPI rather than the locally built wheel. **`loadcoach 1.1.0` is deliberately held and untagged** — by decision on 2026-09-06 the ADR-0085 work rides it (row H5), so LoadCoach's six post-release commits fold into its `1.1.0` changelog instead of taking a `1.2.0`. **`freeweight 1.1.0` stays uncut** until I18 runs whole.
* Optional: extend `suite-flowchart.drawio` with PromptCadence's final shape and the adapter arc.

* **Releases of 2026-09-07, all published:** `freeweight 1.1.0`, `loadcoach 1.1.3` (1.1.2 tagged and
  superseded; LoadCoach's Release workflow had been disabled manually and needed re-enabling before a
  tag could fire), `ideapress 1.2.0` then `1.3.0`, `promptcadence 1.2.0` then `1.3.0`,
  `loadledger 0.3.0`. Every repository's `main` is pushed and CI-green on the checked repos.

* **Patch releases of 2026-09-07 (evening), all tagged; the `pypi` approvals are the operator's:**
  `freeweight 1.1.1`, `loadcoach 1.1.4`, `ideapress 1.3.1`, `promptcadence 1.3.1`. They carry
  L3's schema-ahead fixes (IdeaPress and PromptCadence never detected a database ahead of the
  code; FreeWeight and LoadCoach never named the backup directory), the `pydantic-settings`
  removal (ADR-0114), IdeaPress's `modelrack` widen to `>=0.7,<0.8` (the four applications could
  not co-install — found by the post-publish check), and FreeWeight's SSE-replay test no longer
  waiting on the real idle-settle gate. **Still under `[Unreleased]` in all four:** the
  `mirrorwall` floor raised to `0.2.2` (L6's matrix found 0.2.0/0.2.1 pin `setspec<0.5`, so no
  application resolved at its declared lowest range) — it rides the next patch, and the matrix's
  `lowest` cells stay red until then.

## 5. Milestone map

| Milestone | Rows | Declared when |
|---|---|---|
| **M9** — Suite 1.0 (fourteen components) | §1.1 — audit (`M9_AUDIT.md`), rows L1–L8, re-audit (`M9_REAUDIT.md`) | **Declared 2026-09-08:** master-roadmap §7's 32 boxes met over fourteen components and four applications; podman waived (ADR-0111), packages stay 0.x (ADR-0113), package performance cells blank until the first nightly; compatibility matrix run `34187778093` green at both ends of every range |
| **M10** — foundations | A1–F2 package rows (A1, A2, B1–B3, C1–C3, C5, C6, D1, E1–E3) | All four packages at 0.1.0, clean-venv acceptance scripts pass |
| **M11** — beta | B4, C4, D2, E4, F1, F2, G1 | **Declared 2026-09-07 (ADR-0111):** the planned-vs-bypassed demo on real LoadCoach (G1) and the container rung exercised on the reference machine's runtime (docker, E4). The former condition — **`pytest -m isolation -rs` green on a real podman host** (see §4 — podman is first in ToolYard's ladder and has never been exercised; the reference machine has docker only, and E4 ran the container rung through docker); `0.9.0b0` — **cut 2026-09-04 (G1); the podman condition remains open** |
| **LA1/LA2/LA3** | D3, F3, H1 / H2, H3 / B5, H4, H5 | Adapter-roadmap §3 exit conditions |
| **M12** — 1.0 | I1, I2 (+ H2 as the LC-E1 dependency; G2 the tool wire and G3 per-step retry, both found at G1) | Spec §20; `promptcadence 1.0.0` published. **I1 and I2 done 2026-09-06; the 1.0.0 release commit is prepared (`b4b67ac`) and the tag/publish is the operator's, with I2's verification verdict (F1) in hand** |
| **M13** — adoption | J1–J2 (the three adoption phases; J1 carries two) | **Done 2026-09-07**: `ideapress 1.2.0` published (`dcb2a17`, then 1.3.0 the same day), every harness-arc package has two real consumers. K1–K4 are the follow-ups the rows found |
