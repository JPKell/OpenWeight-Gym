# ideapress.service — generated whole by WeightRoomGym 0.2.0 (ADR-0125 rule 1).
# Edits here are overwritten by `wr-gym units sync`. To change this unit, change
# WeightRoomGym's configuration: [apps.<app>] executable, [host] memory_high/memory_max.

[Unit]
Description=IdeaPress (Local AI Suite; loopback, fronted by WeightRoomGym)
After=network.target

[Service]
ExecStart=/home/op/ai/suite/venvs/ideapress/bin/ideapress serve
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
